# Phase 07.2: Motion Planning Problem Analysis — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 07.2-motion-planning-problem-analysis
**Areas discussed:** Problem framing, Evidence method, Trial rigor, Environment, Output shape, Scope (expanded)

---

## Problem Framing

| Option | Description | Selected |
|--------|-------------|----------|
| The 3 known flakiness items | Deep-dive the three items in 07-PHASE6-REGRESSION.md §Follow-up | ✓ |
| Broader: full motion-quality audit | Includes orientation, jerk, execution drift, collision-scene sync, wrist_roll conventions, OMPL tuning | |
| New problem I've observed | Something recent in testing, described in Other | |
| All of the above | 3 items first, then broader sweep | |

**User's choice:** The 3 known flakiness items (FC-1 no-IK at workspace edge, FC-2 OMPL error −2 post-release, FC-3 out-of-reach block spawns).
**Notes:** Grounds the phase in concrete, reproducible failures already documented by 07-PHASE6-REGRESSION.md.

---

## Evidence Method

| Option | Description | Selected |
|--------|-------------|----------|
| Fresh instrumented trials (initial recommendation) | Script pick-drop cycle via 07.1 registries, capture per-trial state | |
| Deterministic repro harness | Minimal reproducers with fixed seeds per class | |
| Retrospective log mining only | Grep existing logs for failure signatures | |
| Instrumented trials + targeted repros | Trials for rates, repros for no-IK / OMPL classes | |

**User's freeform response:** "think like a robotisict and tell me the options. we can record the target positon or inital psoiton where it doesnt work and work thsi throguh mathematicaly and work on the ik controler or the planner if required after going throguh the codebase and browsing online?"

**Follow-up roboticist options presented:**

| Option | Description | Selected |
|--------|-------------|----------|
| Workspace + config-space + N=10 trials | Analytical reachable set R + C-space cluster map + planning-scene audit + online refs | ✓ |
| Jacobian singularity + sensitivity sweep + N=10 | Heavier math: condition numbers at failing poses + OMPL param sweep | |
| Empirical + online reference only | Run trials, look up symptoms online, skip analytical work | |
| Option A + deterministic repro harness | Recommended + fixed-seed repros for regression | |

**User's choice:** Workspace + config-space + N=10 trials.
**Notes:** Chose the pragmatic roboticist baseline over the heavier-math option. Repro harness deferred but re-openable if N=10 variance is unusable.

---

## Trial Rigor

| Option | Description | Selected |
|--------|-------------|----------|
| N=10 random-seed | Roadmap minimum, ~±15% rate confidence | ✓ |
| N=20 random-seed | Tighter confidence (~±7%), doubles time | |
| N=10 random + N=5 fixed-seed regression | Rates + regression probes | |
| N=5 quick pilot, expand if noisy | Cheapest, risky | |

**User's choice:** N=10 random-seed (recommended).
**Notes:** Standard trial count; deterministic regression layer deferred.

---

## Environment Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Isaac Sim only | Full observability, all 3 classes are sim-observable | ✓ |
| Sim primary + real spot-checks | Sim for rates + 3-5 real-hardware sanity runs | |
| Sim + real in equal weight | N=10 per class in both, highest rigor | |

**User's choice:** Isaac Sim only (recommended).
**Notes:** Real hardware deferred; sim provides enough observability to root-cause all three classes.

---

## Output Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Findings + fix recipes routed to phases | Diagnose + recommend routing to 8/9/10 | |
| Findings + routing + trigger new insertions | Same + /gsd-insert-phase if class doesn't fit | |
| Findings only, no fix routing | Pure diagnosis | |

**User's freeform response:** "finding and fixing we are going to do this phase inline"

**Interpretation:** Phase 07.2 expanded from analysis-only to analysis + inline fix. 07.2 now owns the three fixes.

---

## Scope Confirmation (after inline-fix decision)

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, 07.2 owns all 3 fixes | All three items move from Phase 10 additions into 07.2 | ✓ |
| 07.2 owns only the 2 IK-class fixes | FC-1 + FC-3 in 07.2, FC-2 stays deferred | |
| Fix light items, defer heavy ones | Route to new phase via /gsd-insert-phase if fix balloons | |

**User's choice:** Yes, 07.2 owns all 3 fixes.
**Notes:** Phase 10 no longer inherits the three flakiness items. Its original scope (drop orientation, trajectory-based grasp, jerk reduction, camera-guided drop) stays intact.

---

## Claude's Discretion

- Reachable-set representation (voxel grid resolution, convex hull, point cloud).
- FC-2 fix selection (warmup move vs budget bump vs planning-scene fix) — data-driven after N=10 + C-space analysis.
- Online references to consult (OMPL tuning, MoveIt 2 Humble planning-scene API, 5-DOF IK with grip-angle parameterization).
- Whether to add a fixed-seed repro harness retroactively if N=10 variance proves unusable.

## Deferred Ideas

- Fixed-seed deterministic repro harness (option A+).
- Jacobian / condition-number singularity analysis (only if voxel-grid reachable set is insufficient).
- OMPL parameter sensitivity sweep (only if C-space + scene audit don't explain FC-2).
- Real-hardware validation — not in 07.2 scope.
