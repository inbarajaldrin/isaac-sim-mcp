---
phase: 01-foundation-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - exts/aic-dt/scripts/snapshot_aic_eval.sh
  - exts/aic-dt/docs/topic-parity-reference.md
  - .planning/phases/01-foundation-parity/aic_topics_live.txt
  - .planning/phases/01-foundation-parity/aic_frames_live.gv
  - .planning/phases/01-foundation-parity/snapshot/
autonomous: true
requirements: [PARITY-03, PARITY-04]
tags: [parity, snapshot, docker, ros2, gazebo]
must_haves:
  truths:
    - "Live aic_eval container can be brought up headlessly and produces a populated topic surface"
    - "snapshot_aic_eval.sh captures topic_list, joint_states echo, frames.gv, and image SHA-256 digest reproducibly"
    - "topic-parity-reference.md documents the live Phase-1 passive sensor surface (joint_states, tf, tf_static) with image digest pinned per D-14"
  artifacts:
    - path: "exts/aic-dt/scripts/snapshot_aic_eval.sh"
      provides: "Docker bringup + topic snapshot helper"
      min_lines: 50
    - path: "exts/aic-dt/docs/topic-parity-reference.md"
      provides: "Live aic_eval topic-parity reference doc with pinned image digest"
      contains: "ghcr.io/intrinsic-dev/aic/aic_eval@sha256:"
    - path: ".planning/phases/01-foundation-parity/aic_topics_live.txt"
      provides: "Captured 35-topic surface (already present from research; re-validated)"
    - path: ".planning/phases/01-foundation-parity/aic_frames_live.gv"
      provides: "Captured TF tree (already present; re-validated)"
  key_links:
    - from: "exts/aic-dt/scripts/snapshot_aic_eval.sh"
      to: "exts/aic-dt/docs/topic-parity-reference.md"
      via: "documents digest captured by script"
      pattern: "image_digest"
---

<objective>
Build the empirical parity reference. D-01 declares the live `aic_eval` Docker container as the canonical source of truth for "what Gazebo publishes" — this plan turns that into a reproducible snapshot script and a documented reference artifact (`topic-parity-reference.md`) pinned by image SHA-256 digest (D-14). Phase 1 success criteria #1, #2 (joint_states ordering), and #3 (TF tree diff) all measure against the artifacts captured here. The snapshot artifacts `aic_topics_live.txt` and `aic_frames_live.gv` already exist in the phase dir from the research step — this plan validates they are current and creates the script + doc that make them reproducible.

Purpose: Establish the empirical reference every other Phase 1 plan compares against.
Output: Reproducible snapshot helper script, capitalized image digest pin, and a documented parity reference.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-parity/01-CONTEXT.md
@.planning/phases/01-foundation-parity/01-RESEARCH.md
@.planning/phases/01-foundation-parity/01-PATTERNS.md
@.planning/phases/01-foundation-parity/aic_topics_live.txt
@.planning/phases/01-foundation-parity/aic_frames_live.gv
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/snapshot_aic_eval.sh — reproducible Docker snapshot helper</name>
  <files>exts/aic-dt/scripts/snapshot_aic_eval.sh</files>
  <read_first>
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "Code Examples" lines 569-643, "Live Reference Snapshot" lines 103-256, D-14 image digest pin)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "snapshot_aic_eval.sh (NEW — D-01, D-14)" lines 452-485)
    - exts/aic-dt/scripts/launch_postload.py (existing script header style)
    - CLAUDE.md (sections "Launching the AIC Gazebo reference" + "Inspecting Gazebo's actual topic surface")
  </read_first>
  <action>
Create `exts/aic-dt/scripts/snapshot_aic_eval.sh` as an executable bash script. Use `set -euo pipefail`. Accept one positional arg `DEST` (default: `.planning/phases/01-foundation-parity/snapshot`). Implement these steps in order, exactly as specified by RESEARCH.md lines 575-643:

1. `mkdir -p "$DEST"`
2. `docker rm -f aic_eval 2>/dev/null || true`
3. `docker run -d --name aic_eval --gpus all --runtime=nvidia -e NVIDIA_VISIBLE_DEVICES=all -e NVIDIA_DRIVER_CAPABILITIES=all --net=host --privileged ghcr.io/intrinsic-dev/aic/aic_eval:latest ground_truth:=true start_aic_engine:=true gazebo_gui:=false`
4. `echo "Waiting 35s for Gazebo + controllers..." && sleep 35`
5. Capture image digest (D-14): `docker inspect --format='{{index .RepoDigests 0}}' aic_eval | tee "$DEST/image_digest.txt"`
6. Capture topic list (Zenoh env required — exec inside container):
   ```bash
   docker exec aic_eval bash -c '
     source /opt/ros/kilted/setup.bash
     export RMW_IMPLEMENTATION=rmw_zenoh_cpp
     export ROS_AUTOMATIC_DISCOVERY_RANGE=SUBNET
     export ZENOH_CONFIG_OVERRIDE=";transport/shared_memory/enabled=false"
     ros2 daemon stop 2>/dev/null
     sleep 1
     ros2 topic list
   ' | sort | tee "$DEST/topic_list.txt"
   ```
7. Per-topic info loop for `/joint_states /tf /tf_static /clock /fts_broadcaster/wrench` — each saved to `$DEST/topic_info_<sanitized_topic>.txt` (replace `/` with `_`).
8. Capture `/joint_states --once` echo to `$DEST/joint_states_sample.yaml` (Zenoh env again).
9. `view_frames` capture: `docker exec aic_eval ... cd /tmp && ros2 run tf2_tools view_frames -o aic_frames` then `docker cp aic_eval:/tmp/aic_frames.gv "$DEST/aic_frames_live.gv"` and best-effort copy `.pdf`.
10. Tear down: `docker stop aic_eval && docker rm aic_eval`.
11. Print final message: `echo "Snapshot saved to $DEST/"`.

Add a header comment block:
```
#!/usr/bin/env bash
# scripts/snapshot_aic_eval.sh — capture live Gazebo aic_eval topic surface (D-01, D-14)
#
# Usage: ./snapshot_aic_eval.sh [DEST]
#   DEST defaults to .planning/phases/01-foundation-parity/snapshot
#
# Requirements: Docker with NVIDIA runtime, ~35s startup wait, ~6.4 GB image already pulled.
# Idempotent: removes any prior aic_eval container before starting.
```

Make script executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -n exts/aic-dt/scripts/snapshot_aic_eval.sh && test -x exts/aic-dt/scripts/snapshot_aic_eval.sh</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `exts/aic-dt/scripts/snapshot_aic_eval.sh` and is executable (`test -x` passes)
    - `bash -n exts/aic-dt/scripts/snapshot_aic_eval.sh` exits 0 (syntax-valid)
    - Script contains literal string `ghcr.io/intrinsic-dev/aic/aic_eval:latest` (grep finds it once)
    - Script contains literal string `RMW_IMPLEMENTATION=rmw_zenoh_cpp`
    - Script contains literal string `view_frames -o aic_frames`
    - Script contains literal string `docker inspect --format='{{index .RepoDigests 0}}'`
    - Script contains the `set -euo pipefail` directive
    - Script header references D-01 and D-14
  </acceptance_criteria>
  <done>Reproducible Docker snapshot helper script lives under `exts/aic-dt/scripts/` and follows the spec from RESEARCH.md verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Capture or re-validate live snapshot artifacts</name>
  <files>.planning/phases/01-foundation-parity/snapshot/, .planning/phases/01-foundation-parity/aic_topics_live.txt, .planning/phases/01-foundation-parity/aic_frames_live.gv</files>
  <read_first>
    - .planning/phases/01-foundation-parity/aic_topics_live.txt (existing artifact, 35 topics)
    - .planning/phases/01-foundation-parity/aic_frames_live.gv (existing TF tree, 31 frames)
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "Live Reference Snapshot — `aic_eval` Topic Surface" lines 103-256 — full topic + frame list to compare against)
  </read_first>
  <action>
Run the snapshot script created in Task 1 to produce a fresh capture under `.planning/phases/01-foundation-parity/snapshot/`. Then compare the freshly captured `topic_list.txt` and `aic_frames_live.gv` against the existing artifacts in the phase directory.

Concrete steps:
1. Pre-flight: ensure Docker is running and `aic_eval:latest` image is pulled. If not, run `docker pull ghcr.io/intrinsic-dev/aic/aic_eval:latest`.
2. Execute: `bash exts/aic-dt/scripts/snapshot_aic_eval.sh .planning/phases/01-foundation-parity/snapshot`
3. Compare: `diff <(sort .planning/phases/01-foundation-parity/aic_topics_live.txt) <(sort .planning/phases/01-foundation-parity/snapshot/topic_list.txt)` — expect no diff (or a documented diff if the upstream image has drifted).
4. Compare frame edges: `diff <(grep -E '"[^"]+"\s*->\s*"[^"]+"' .planning/phases/01-foundation-parity/aic_frames_live.gv | sort) <(grep -E '"[^"]+"\s*->\s*"[^"]+"' .planning/phases/01-foundation-parity/snapshot/aic_frames_live.gv | sort)` — expect no diff.
5. If snapshot has drifted: update `aic_topics_live.txt` and `aic_frames_live.gv` in the phase directory to the freshly captured versions, and note the drift in `topic-parity-reference.md` (Task 3 below).
6. If snapshot matches: leave the existing artifacts in place and proceed.

This is a verification + freshness task — fast if cache is warm, otherwise budget ~3 minutes for the Docker bringup + 35s sleep + capture cycle.

Note (D-13): if Docker is not running or the image is missing, log a clear message and abort — do NOT attempt to fall back to mocking. The reference snapshot must come from a live container.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation-parity/snapshot/image_digest.txt && test -f .planning/phases/01-foundation-parity/snapshot/topic_list.txt && test -f .planning/phases/01-foundation-parity/snapshot/aic_frames_live.gv && test -f .planning/phases/01-foundation-parity/snapshot/joint_states_sample.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-foundation-parity/snapshot/image_digest.txt` exists and contains the literal string `ghcr.io/intrinsic-dev/aic/aic_eval@sha256:`
    - `.planning/phases/01-foundation-parity/snapshot/topic_list.txt` exists and `wc -l` returns >= 30 lines (live container has 35 topics)
    - `.planning/phases/01-foundation-parity/snapshot/aic_frames_live.gv` exists and contains literal string `base_link` and `wrist_3_link` and `gripper/hande_base_link`
    - `.planning/phases/01-foundation-parity/snapshot/joint_states_sample.yaml` exists and contains literal `elbow_joint` and `gripper/left_finger_joint`
    - Diff between phase-dir `aic_topics_live.txt` and freshly captured `snapshot/topic_list.txt` is empty (or any drift is recorded in topic-parity-reference.md per Task 3)
  </acceptance_criteria>
  <done>Live snapshot artifacts are validated current; image digest pinned in `snapshot/image_digest.txt`.</done>
</task>

<task type="auto">
  <name>Task 3: Write topic-parity-reference.md with pinned digest and Phase-1 surface table</name>
  <files>exts/aic-dt/docs/topic-parity-reference.md</files>
  <read_first>
    - exts/aic-dt/docs/README.md (existing extension-doc tone/style — first 60 lines)
    - exts/aic-dt/docs/CHANGELOG.md (existing-doc style)
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (sections "Live Reference Snapshot — `aic_eval` Topic Surface" lines 103-256, "/joint_states echo" lines 179-202, "Live TF tree" lines 204-241)
    - .planning/phases/01-foundation-parity/01-CONTEXT.md (D-01, D-04 — cable Phase-3 callout, D-14 digest pin)
    - .planning/phases/01-foundation-parity/snapshot/image_digest.txt (the captured digest from Task 2)
  </read_first>
  <action>
Create `exts/aic-dt/docs/topic-parity-reference.md` as the canonical doc for "what Gazebo publishes" against which Isaac Sim must match. Use Markdown headers and tables, matching the tone of existing `exts/aic-dt/docs/README.md`.

Required sections in this exact order:

1. `# Topic Parity Reference (aic_eval Live Snapshot)` — H1 title.
2. `## Provenance` — paragraph stating the source is the live `aic_eval` Docker container per D-01, with the literal pinned digest from `snapshot/image_digest.txt` (e.g. `ghcr.io/intrinsic-dev/aic/aic_eval@sha256:be08f28709acc4662da7378e94c5efccb66a8a0fff27ffccdc68e471d8967433`). Reference the snapshot script: "Re-capture with `bash exts/aic-dt/scripts/snapshot_aic_eval.sh`".
3. `## Phase 1 Passive Sensor Surface` — table with columns `Topic | Type | QoS | Rate | Phase 1 source`. Three rows for `/joint_states`, `/tf`, `/tf_static` filled from RESEARCH.md lines 173-176:
   - `/joint_states` | `sensor_msgs/msg/JointState` | RELIABLE / VOLATILE / KEEP_LAST(42) | 500 Hz | Isaac Sim ROS2PublishJointState
   - `/tf` | `tf2_msgs/msg/TFMessage` | RELIABLE / VOLATILE / KEEP_LAST(10) | 500 Hz | Isaac Sim ROS2PublishTransformTree (staticPublisher=False)
   - `/tf_static` | `tf2_msgs/msg/TFMessage` | RELIABLE / **TRANSIENT_LOCAL** / KEEP_LAST(1) | static one-shot | Isaac Sim ROS2PublishTransformTree (staticPublisher=True)
4. `## /joint_states Joint Name Set + Ordering` — fenced YAML block reproducing the live echo from RESEARCH.md lines 181-196. State explicitly: "**7 joints, alphabetical order, header.frame_id = base_link**." List the joints verbatim:
   ```
   - elbow_joint
   - gripper/left_finger_joint
   - shoulder_lift_joint
   - shoulder_pan_joint
   - wrist_1_joint
   - wrist_2_joint
   - wrist_3_joint
   ```
5. `## TF Tree (frames.gv)` — embed the parent→child edge list from RESEARCH.md lines 211-241 (31 frames, 30 edges). State the dynamic part: 6 dynamic joints + 2 gripper finger joints publish at 440 Hz on `/tf`; the rest are static on `/tf_static`. Link to the captured artifact `.planning/phases/01-foundation-parity/aic_frames_live.gv`.
6. `## Phase-3 Work Items (Out of Phase 1)` — D-04 callout: "The cable subtree is `SetActive(False)` for Phase 1 per D-04. Cable physics is Phase 3 work (SCENE-05). Cable assets ARE inspected for textures during the Phase 1 sweep — only simulation is disabled." Plus deferred items: object TF frames CheatCode reads (Phase 3 SCENE-06), full ros2_control surface (Phase 2).
7. `## Reproducing This Snapshot` — bash code block: `bash exts/aic-dt/scripts/snapshot_aic_eval.sh`. Note D-13: works without `~/Documents/aic` checked out (Docker only).
8. `## Out-of-Phase-1 Topics in This Snapshot` — list (table) the other ~30 topics from `aic_topics_live.txt` and which phase they belong to (PARITY-05/06/09/10/11 → Phase 2; SCENE-06 / PARITY-07/08 → Phase 3; trial loader → Phase 4). Use REQUIREMENTS.md traceability table as the source.

Doc must be plain Markdown, no frontmatter (this is shipping doc, not GSD planning artifact).
  </action>
  <verify>
    <automated>test -f exts/aic-dt/docs/topic-parity-reference.md && grep -q "ghcr.io/intrinsic-dev/aic/aic_eval@sha256:" exts/aic-dt/docs/topic-parity-reference.md && grep -q "gripper/left_finger_joint" exts/aic-dt/docs/topic-parity-reference.md && grep -q "TRANSIENT_LOCAL" exts/aic-dt/docs/topic-parity-reference.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `exts/aic-dt/docs/topic-parity-reference.md`
    - File contains the literal string `ghcr.io/intrinsic-dev/aic/aic_eval@sha256:` (digest pin per D-14)
    - File contains the exact 7-joint alphabetical list (one occurrence each: `elbow_joint`, `gripper/left_finger_joint`, `shoulder_lift_joint`, `shoulder_pan_joint`, `wrist_1_joint`, `wrist_2_joint`, `wrist_3_joint`)
    - File contains the literal string `TRANSIENT_LOCAL` (the /tf_static QoS marker)
    - File contains a section titled `Phase 1 Passive Sensor Surface` (or near-identical)
    - File contains a Phase-3 callout mentioning `SetActive(False)` and `SCENE-05`
    - File references `bash exts/aic-dt/scripts/snapshot_aic_eval.sh` for reproducibility
    - File contains parent-child frame edges including `base_link → base_link_inertia` and `gripper/hande_base_link → gripper/hande_finger_link_l`
  </acceptance_criteria>
  <done>Topic-parity reference doc shipping under `exts/aic-dt/docs/` with pinned digest and full Phase-1 surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker container | Docker run with `--privileged --net=host --gpus all`. Container is the upstream `aic_eval` image (read-only consumer; we don't modify it). Image is pinned by SHA-256 digest (D-14) in the captured snapshot. |
| Container → host | `docker cp` of `frames.gv` and `frames.pdf` from container `/tmp` to host phase dir. Both are plain-text/PDF artifacts, not executed. |
| Snapshot script → file system | Script writes to `.planning/phases/01-foundation-parity/snapshot/` and `exts/aic-dt/docs/`. No network exposure beyond Docker pull. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Supply chain | `ghcr.io/intrinsic-dev/aic/aic_eval:latest` image pull | mitigate | D-14: pin image by SHA-256 digest in `topic-parity-reference.md` after first capture. Future re-snapshots verify digest matches expected; drift triggers explicit review. |
| T-01-02 | Information disclosure | Docker `--privileged --net=host` exposes host network to container | accept | Container is local-only, ephemeral, removed at script end. No PII or secrets in this repo. AIC image is upstream-vetted. ASVS L1 sufficient for an internal sim-side reproducibility script. |
| T-01-03 | Denial of service | Docker bringup may hang on cold image pull or NVIDIA runtime issues | accept | Script has 35s sleep + interactive terminal feedback; not run in CI. User aborts with Ctrl-C if hung. |
| T-01-04 | Tampering / Untrusted output | Captured `frames.gv` from container goes through `view_frames` parser later | mitigate | Parser (`diff_tf_tree.py` in Plan 06) uses regex `re.findall(r'"([^"]+)" -> "([^"]+)"', ...)` — no eval, no shell exec. Plain-text consumption only. |
</threat_model>

<verification>
- `bash -n exts/aic-dt/scripts/snapshot_aic_eval.sh` exits 0
- `test -x exts/aic-dt/scripts/snapshot_aic_eval.sh`
- Snapshot artifacts exist under `.planning/phases/01-foundation-parity/snapshot/`
- `grep -c "ghcr.io/intrinsic-dev/aic/aic_eval@sha256:" exts/aic-dt/docs/topic-parity-reference.md` returns >= 1
- `grep -c "gripper/left_finger_joint" exts/aic-dt/docs/topic-parity-reference.md` returns >= 1
</verification>

<success_criteria>
1. Reproducible snapshot: any future session can run `bash exts/aic-dt/scripts/snapshot_aic_eval.sh` and produce a fresh artifact set.
2. Image digest pinned in shipping doc — drift detection cheap.
3. Phase 1's passive sensor surface (joint_states, tf, tf_static) documented with QoS, rate, joint-name list, and frame tree — used as the empirical reference by Plans 04/05/06.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parity/01-01-SUMMARY.md` with:
- Image digest captured
- Whether artifacts drifted from research-time capture
- Path to the snapshot dir
- Any container/Docker setup gotchas encountered
</output>
