---
phase: 01-foundation-parity
plan: 08
type: execute
wave: 6
depends_on: [01-03, 01-04, 01-05, 01-06, 01-09]
files_modified:
  - exts/aic-dt/aic_dt/extension.py
  - exts/aic-dt/docs/CHANGELOG.md
autonomous: true
requirements: [PARITY-01, PARITY-03, PARITY-04, SCENE-01, SCENE-04, DX-01, DX-02]
tags: [quick-start, refactor, changelog, integration]
must_haves:
  truths:
    - "After quick_start completes successfully, `ros2 topic list` from the live extension includes /joint_states, /tf, and /tf_static — confirming Phase 1 SC #1"
    - "`grep -E '(_sim|_real)\\b' exts/aic-dt/aic_dt/extension.py` returns zero production-topic matches (allowlisted false positives like `initialize_simulation_context_async` excluded) — confirming Phase 1 SC #5"
    - "quick_start calls setup_tf_publish_action_graph and setup_joint_state_publish_action_graph in the documented order (load scene → load robot → setup_tf_publisher → setup_joint_state_publisher → setup_action_graph (subscribe) → wrist cameras → force publisher → add objects → randomize lighting → play)"
    - "quick_start no longer calls create_pose_publisher (deleted in Plan 04) — no orphan call remains"
    - "Early-play pattern (timeline.play() right after load_robot per the existing comment block) is preserved — non-negotiable per PATTERNS.md"
    - "CHANGELOG.md has a Phase 1 entry recording: TF + JointState publishers added, _sim/_real cleanup completed, RG2→Hand-E correction, asset vendoring, prim-path bug fix, per-component spawn atoms (SCENE-01) + pose params (SCENE-04), full PARITY-12 audit table"
    - "quick_start clubbing path is the canonical default-spawn route — calls the new per-component atoms with spawn_task_board.launch.py default values (via add_objects clubbing) OR directly inlined; either way, the resulting scene is identical to pre-Phase-1 behavior + the new TF/JointState publishers"
    - "DX-02 4-surface enforcement is final: all 9 net-new atoms (setup_tf_publisher, setup_joint_state_publisher, optionally setup_joint_state_reorder, spawn_task_board_base, spawn_lc_mount_rail, spawn_sfp_mount_rail, spawn_sc_mount_rail, spawn_sc_port, spawn_nic_card_mount, spawn_nic_card) all have ALL 4 surfaces; 2 deleted atoms (setup_pose_publisher, sync_real_poses) have ALL 4 surfaces removed; CHANGELOG records the DX-02 invariant"
  artifacts:
    - path: "exts/aic-dt/aic_dt/extension.py"
      provides: "quick_start refactored per D-12 ordering"
      contains: "setup_tf_publish_action_graph|setup_joint_state_publish_action_graph"
    - path: "exts/aic-dt/docs/CHANGELOG.md"
      provides: "Phase 1 changelog entry"
  key_links:
    - from: "extension.py:quick_start"
      to: "setup_tf_publish_action_graph + setup_joint_state_publish_action_graph"
      via: "method call ordering after load_robot + early play()"
      pattern: "self\\.setup_tf_publish_action_graph\\(\\).*self\\.setup_joint_state_publish_action_graph\\(\\)"
---

<objective>
Wire the new TF + JointState publisher atoms (created in Plan 06 (previously Plan 05 before renumbering)) into `quick_start` per D-12's refactored ordering, and ship the Phase 1 CHANGELOG entry. This is the final integration step — without this plan, the new atoms exist but the common-path `quick_start` orchestration doesn't call them, so `verify_phase_1.sh` would have to invoke them individually (and Plan 07 does — but the goal of Phase 1 is for `quick_start` alone to produce the matching topic surface).

Per D-12, `quick_start`'s scope is broader than the minimal "drop deleted atom calls" — it reorganizes to a documented common-path order that downstream phases (Phase 2 controller-loop, Phase 3 cable + GT-TF, Phase 4 trial loader) can drop new atoms into cleanly. This partly does Phase 3's DX-03 work early. User's explicit call (deviates from the recommended minimal-scope option in favor of less churn over the milestone).

Per PATTERNS.md, the early-play pattern at step 2b (`self._timeline.play()` right after `load_robot` and before any action graphs / cameras / objects exist) is **non-negotiable** — its rationale (PhysX cooking + many graphs lock-contend if all ordered together) is encoded in the existing comment block at lines 839-843. Preserve verbatim.

This plan also writes the Phase 1 entry in `exts/aic-dt/docs/CHANGELOG.md`. The CHANGELOG was previously updated for the RG2→Hand-E line in Plan 03 (Wave 1) — Plan 08 (this plan) adds the comprehensive Phase 1 milestone entry.

Purpose: Land the final code+doc integration. After this plan, `quick_start` produces the full Phase 1 passive sensor surface in one call.
Output: extension.py with refactored quick_start; CHANGELOG.md with Phase 1 entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-parity/01-CONTEXT.md
@.planning/phases/01-foundation-parity/01-RESEARCH.md
@.planning/phases/01-foundation-parity/01-PATTERNS.md
@.planning/phases/01-foundation-parity/01-04-SUMMARY.md
@.planning/phases/01-foundation-parity/01-05-SUMMARY.md
@.planning/phases/01-foundation-parity/01-06-SUMMARY.md
@exts/aic-dt/aic_dt/extension.py
@exts/aic-dt/docs/CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor quick_start ordering per D-12 — insert TF + JointState publisher calls</name>
  <files>exts/aic-dt/aic_dt/extension.py</files>
  <read_first>
    - exts/aic-dt/aic_dt/extension.py (lines 824-929 — current `quick_start` method body in full; pay attention to the early-play comment block at lines 839-843 which is non-negotiable)
    - exts/aic-dt/aic_dt/extension.py (the just-added builder methods from Plan 06 (previously Plan 05): `setup_tf_publish_action_graph` and `setup_joint_state_publish_action_graph`)
    - .planning/phases/01-foundation-parity/01-CONTEXT.md (D-12 — broader-scope refactor spec)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "quick_start refactor" lines 223-258)
    - .planning/phases/01-foundation-parity/01-04-SUMMARY.md (note about the gap left in quick_start when create_pose_publisher was deleted)
  </read_first>
  <action>
Refactor `quick_start` (currently at lines 824-929 in extension.py) per D-12's documented ordering:

```
load scene
→ load robot (timeline.stop() at end)
→ self._timeline.play()              [EARLY PLAY — non-negotiable, see comment block 839-843]
→ setup_tf_publisher (NEW — Plan 06)
→ setup_joint_state_publisher (NEW — Plan 06)
→ setup_action_graph (kept — joint state SUBSCRIBE side for Phase 2)
→ setup_wrist_cameras
→ setup_force_publisher (renamed wrench topic per Plan 04)
→ add_objects (uses capitalized vendored paths from Plan 04)
→ randomize_lighting (if present)
→ create_workspace_camera + setup_workspace_camera_action_graph
→ (already playing from step 2b)
```

**Concrete edit steps:**

1. Read the current `quick_start` method body (lines 824-929). Identify:
   - The current step list (1. load_scene, 2. load_robot, 2b. play, 3. setup_action_graph, 4. setup_force_publisher, 5. setup_wrist_cameras, 6. add_objects, 7. create_pose_publisher [DELETED in Plan 04 — orphan call exists if Plan 04's deletion missed this caller], 8/9. workspace camera).
   - The comment block at lines 839-843 explaining early-play rationale — **preserve verbatim**.
   - The `await app.next_update_async()` pump-the-loop calls between steps — preserve.

2. Apply edits:

   - **Before** the existing `setup_action_graph` call (current step 3), insert two new calls:
     ```python
     # 3a. Setup TF publisher (Plan 06 / Phase 1 D-10) — /tf + /tf_static
     self.setup_tf_publish_action_graph()
     await app.next_update_async()

     # 3b. Setup JointState publisher (Plan 06 / Phase 1 D-11) — /joint_states
     self.setup_joint_state_publish_action_graph()
     await app.next_update_async()
     ```

   - Renumber the existing comments accordingly (3 → 4, 4 → 5, etc.) — purely a comment refresh, no behavior change.

   - **Verify** that the orphan `self.create_pose_publisher()` call from the original step 7 was indeed removed in Plan 04. If it's still present (it shouldn't be), delete it now along with its `await app.next_update_async()` pump.

   - **CONDITIONAL — JointState reorder bridge integration (Plan 05 Task 4 contract):** Read `.planning/phases/01-foundation-parity/joint_ordering_probe.txt`. If `Action: ADD-TASK-4-WRAPPER`, insert a call to `self.setup_joint_state_reorder()` IMMEDIATELY AFTER the `setup_joint_state_publish_action_graph()` call. Otherwise (if `Action: NO-WRAPPER-NEEDED`), no extra call needed.
     ```python
     # Conditional — only if Plan 05 Task 4 was executed:
     # 3c. Bridge to reorder /joint_states_isaac_raw → /joint_states alphabetical (D-11 / PARITY-03)
     if hasattr(self, 'setup_joint_state_reorder'):
         self.setup_joint_state_reorder()
         await app.next_update_async()
     ```
     The `hasattr` guard makes this defensive — if Plan 05 Task 4 was skipped (probe verdict was NAME-INDEXED), the method doesn't exist and the call is silently skipped.

   - **Verify** the workspace_camera prim path uses `/World/workspace_camera` (not `/World/workspace_camera_sim`) per Plan 04 renames. If any orphan `_sim` references remain in `quick_start`, fix them now.

3. Update the `quick_start` docstring to reflect the new ordering. Example:
   ```python
   async def quick_start(self):
       """Quick start: load scene → load robot → play → TF/JointState/JointSubscribe graphs → cameras → wrench → objects → workspace cam.

       Per Phase 1 D-12: refactored ordering with TF + JointState publishers (PARITY-03/04)
       inserted after the early-play step. Future phases drop new atoms in via this common path:
         - Phase 2 controller-loop: subscribes between JointState and Force publisher
         - Phase 3 GT-TF: between objects and workspace cam
         - Phase 4 trial-loader: replaces the static add_objects with a parametric variant
       """
   ```

4. After all edits, sanity-check via grep:
   - `grep -c "self\.setup_tf_publish_action_graph()" exts/aic-dt/aic_dt/extension.py` returns 1 (called from quick_start)
   - `grep -c "self\.setup_joint_state_publish_action_graph()" exts/aic-dt/aic_dt/extension.py` returns 1
   - `grep -c "self\.create_pose_publisher" exts/aic-dt/aic_dt/extension.py` returns 0 (deleted in Plan 04, no orphan)
   - `grep -c "_sim\|_real" exts/aic-dt/aic_dt/extension.py | grep -v initialize_simulation_context_async` is empty
   - File parses as valid Python

**Important — preservation rules:**
- The early-play step (`self._timeline.play()` right after `load_robot()`) MUST stay where it is. Do NOT move it later. PATTERNS.md and the inline comment at lines 839-843 explain why.
- The `await app.next_update_async()` pumps between steps MUST stay (one between each step). They give the OmniGraph evaluator time to process before the next graph creation queues up.
- The existing line numbers in this plan's read_first are approximate — use the method signature `async def quick_start(self)` and the `await self.load_scene()` first call as anchor points.

**Out of scope for this task** (deferred to future phases):
- The "load AIC enclosure" step that ROADMAP Phase 1 success criterion #4 mentions — the enclosure is loaded via `load_scene`/`add_objects` already; no separate enclosure step needed. Verify that the existing scene loading covers it.
- Adding new atoms beyond TF + JointState (Phase 2/3 atoms).
  </action>
  <verify>
    <automated>python3 -c "
import ast, re
src = open('exts/aic-dt/aic_dt/extension.py').read()
ast.parse(src)
# quick_start calls both new atoms
assert 'self.setup_tf_publish_action_graph()' in src, 'setup_tf_publish_action_graph not called from quick_start'
assert 'self.setup_joint_state_publish_action_graph()' in src, 'setup_joint_state_publish_action_graph not called from quick_start'
# No orphan deleted-method calls
assert 'self.create_pose_publisher' not in src
assert 'self.sync_real_poses' not in src
# _sim/_real cleanliness preserved
hits = []
for m in re.finditer(r'_sim\b|_real\b', src):
    line_no = src[:m.start()].count(chr(10)) + 1
    line = src.split(chr(10))[line_no-1]
    if 'initialize_simulation_context_async' in line:
        continue
    hits.append((line_no, line.strip()))
assert not hits, f'_sim/_real hits: {hits}'
# Early-play preserved
assert '_timeline.play()' in src
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python
    - `grep -c "self\.setup_tf_publish_action_graph()" exts/aic-dt/aic_dt/extension.py` returns 1
    - `grep -c "self\.setup_joint_state_publish_action_graph()" exts/aic-dt/aic_dt/extension.py` returns 1
    - `grep -c "self\.create_pose_publisher" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "self\.sync_real_poses" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "_timeline\.play()" exts/aic-dt/aic_dt/extension.py` returns >= 1 (early-play step preserved)
    - `grep -c "self\.setup_force_publish_action_graph\|self\.setup_force_publisher" exts/aic-dt/aic_dt/extension.py` returns >= 1 (force publisher call preserved in quick_start)
    - `grep -c "self\.setup_wrist_cameras" exts/aic-dt/aic_dt/extension.py` returns >= 1 (wrist cameras call preserved)
    - `grep -c "self\.add_objects" exts/aic-dt/aic_dt/extension.py` returns >= 1 (add_objects call preserved)
    - `grep -c "self\.setup_action_graph" exts/aic-dt/aic_dt/extension.py` returns >= 1 (joint-state subscribe graph still in quick_start)
    - In `quick_start`, the order of method calls matches D-12 spec: load_scene → load_robot → _timeline.play() → setup_tf_publish_action_graph → setup_joint_state_publish_action_graph → setup_action_graph → setup_wrist_cameras → setup_force_publish_action_graph → add_objects → workspace camera (verifiable by reading the method body and confirming sequence)
    - The early-play comment block (rationale at lines ~839-843) is preserved verbatim — check by grepping for a unique substring from the original comment (e.g., "PhysX" or "lock-contend")
    - No `_sim`/`_real` regression: `grep -E '(_sim|_real)\b' exts/aic-dt/aic_dt/extension.py | grep -v 'initialize_simulation_context_async'` returns no matches
  </acceptance_criteria>
  <done>quick_start refactored per D-12; TF + JointState publishers in the common path; early-play preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 1 entry in exts/aic-dt/docs/CHANGELOG.md</name>
  <files>exts/aic-dt/docs/CHANGELOG.md</files>
  <read_first>
    - exts/aic-dt/docs/CHANGELOG.md (current state — read full file to understand the existing changelog format/tone)
    - .planning/REQUIREMENTS.md (PARITY-01..04, TEX-01..03, DX-01 — for the requirement IDs to credit)
    - .planning/phases/01-foundation-parity/01-CONTEXT.md (decisions D-01 through D-15 — for the highlights)
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (Sources section — for the empirical evidence sources to cite)
  </read_first>
  <action>
Add a Phase 1 milestone entry to `exts/aic-dt/docs/CHANGELOG.md`. Follow the existing changelog's format and tone (read the file first to match it).

If the existing CHANGELOG follows Keep-a-Changelog conventions, the entry might look like:

```markdown
## [Phase 1: Foundation Parity] — 2026-05-XX

Milestone 1 toward platform-transfer parity with the AIC competition Gazebo stack. The
unmodified `aic_controller` + `aic_engine` + `CheatCode.py` from `~/Documents/aic` will
run against this Isaac Sim digital twin without code changes (M1 success bar).

### Added

- **TF publisher (PARITY-04, D-10)**: New MCP atom `setup_tf_publisher` creates an OmniGraph
  with two `isaacsim.ros2.bridge.ROS2PublishTransformTree` nodes — one for `/tf` (dynamic),
  one for `/tf_static` (TRANSIENT_LOCAL via `staticPublisher=True`). Articulation root targets
  `/World/UR5e/aic_unified_robot`.
- **JointState publisher (PARITY-03, D-11)**: New MCP atom `setup_joint_state_publisher`
  creates an OmniGraph with `isaacsim.ros2.bridge.ROS2PublishJointState` publishing
  `sensor_msgs/JointState` on `/joint_states`. Live `aic_eval` reference: 7 joints
  alphabetical (incl. `gripper/left_finger_joint`), `header.frame_id=base_link`.
- **Live `aic_eval` topic-parity reference (D-01, D-14)**: `exts/aic-dt/scripts/snapshot_aic_eval.sh`
  + `exts/aic-dt/docs/topic-parity-reference.md`. Image SHA-256 digest pinned per D-14.
- **Phase 1 verify harness (D-15)**: `exts/aic-dt/scripts/verify_phase_1.sh` — hybrid-runtime
  (attached or cold-launch via `launch_postload.py`); integrates `prime_usd_cache.py` cache
  discipline (CLAUDE.md). Plus `diff_tf_tree.py` (D-08) for view_frames .gv diff.
- **Texture sweep tooling (D-07, TEX-03)**: `exts/aic-dt/scripts/sweep_textures.py` +
  `exts/aic-dt/docs/texture-sweep.md` (log-grep loop; iterative).

### Changed

- **Robot reload prim-path bug fix (PARITY-01)**: `load_robot` now looks up joint drives at
  `/World/UR5e/aic_unified_robot/joints/<name>` (was `/World/UR5e/joints/<name>`). The unified
  USD's default prim is `/World`, so the actual joints landed under `aic_unified_robot/`. Joint
  drives now apply correctly; the Kit-log "Joint not found" warnings (visible since the unified
  USD was adopted) are resolved.
- **AIC_OBJECTS paths updated to AIC's capitalized layout (PARITY-02, D-05)**: Snake_case
  `objects/sc_port/sc_port.usd` → `assets/SC Port/sc_port_visual.usd`. `_local_asset` resolver
  unchanged; only the dict values updated. Sibling `textures/` folders now present.
- **`quick_start` reordered per D-12**: Now calls `setup_tf_publish_action_graph` and
  `setup_joint_state_publish_action_graph` after the early-play step and before
  `setup_action_graph` (joint-state subscribe). Future phases drop new atoms in via this
  common path. Early-play step preserved (PhysX cooking + lock-contention rationale).
- **Robot identification — RG2 → Robotiq Hand-E (D-03)**: Confirmed via AIC's
  `aic_description/urdf/ur_gz.urdf.xacro` (`xacro:include` of `Robotiq Hand-E/robotiq_hande_macro.xacro`).
  Code (extension.py docstrings, log lines, return messages) and docs (`PROJECT.md`,
  `REQUIREMENTS.md`, `ROADMAP.md`, `CLAUDE.md`, `docs/README.md`) updated.
- **`/tf_static` QoS = TRANSIENT_LOCAL**: Late-joining subscribers (CheatCode, aic_engine) now
  see static frames. Implementation: `staticPublisher=True` on the static ROS2PublishTransformTree node.
- **Camera topic names match live aic_eval (DX-01, D-09)**: `_sim` suffix removed; topics now
  `/center_camera/image`, `/left_camera/image`, `/right_camera/image` (with `/camera_info` siblings).
- **Wrench topic name matches live aic_eval (DX-01)**: `force_torque_sensor_broadcaster/wrench_sim`
  → `fts_broadcaster/wrench`. (PARITY-05 — formal acceptance is Phase 2 with the contact loop.)
- **Asset vendoring (D-05)**: One-time vendoring of `Intrinsic_assets/<Object>/` subset into
  `exts/aic-dt/assets/assets/<Capitalized>/` with sibling `textures/` directories. Snake_case
  `objects/` tree retired. After Phase 1, the AIC repo is the documented origin but not a
  runtime dependency (D-13).

### Removed

- **`setup_pose_publisher` MCP atom (DX-01, D-09)**: Placeholder bridge from M2 era. Phase 3
  publishes GT directly into `/tf` (SCENE-06). 4 surfaces deleted: registry / handler-map /
  `_cmd_*` method / UI button.
- **`sync_real_poses` MCP atom (DX-01, D-09)**: Placeholder `/objects_poses_real` subscriber.
  4 surfaces deleted.
- **All `_sim` / `_real` topic suffixes on production surface**: 33-row rename table in
  `01-RESEARCH.md` lines 731-766. Zero matches remain in `extension.py` after this milestone.
- **Snake_case `assets/objects/` tree**: Replaced by capitalized `assets/assets/` per D-05.

### Known Phase-3 work items

- **Cable subtree `SetActive(False)` workaround (D-04)**: Persists for Phase 1 — cable physics
  is Phase 3 (SCENE-05). Cable assets ARE inspected for textures during the Phase 1 sweep;
  only simulation is disabled.
- **Object TF frames CheatCode reads (`{cable_name}/{plug_name}_link`, port frames)**: Phase 3
  (SCENE-06).
- **Frame name mapping for slashed AIC frame names (`gripper/hande_base_link` etc.)**: If
  `diff_tf_tree.py` surfaces frame name mismatches (USD prim names use underscores), a follow-up
  plan addresses via per-frame `ROS2PublishRawTransformTree` overrides or USD prim renaming.
- **Joint name ordering**: Live `aic_eval` publishes alphabetical 7 joints. Isaac Sim's
  `ROS2PublishJointState` uses articulation-discovery order (no `jointNames` input — A1).
  Plan 05 (the new probe plan) probes whether `aic_controller`'s subscriber reads by name-index
  vs positional — if positional, follow-up plan adds reordering wrapper.

### Source materials

- AIC repo (read-only): `~/Documents/aic` — `aic_controller/`, `aic_engine/`, `aic_example_policies/ros/CheatCode.py`,
  `aic_description/urdf/ur_gz.urdf.xacro`, `Intrinsic_assets/`.
- Live `aic_eval` Docker container: `ghcr.io/intrinsic-dev/aic/aic_eval:latest`
  (digest pinned in `topic-parity-reference.md`).
- Isaac Sim 5.0 install at `~/env_isaaclab/`.
```

**Adapt to existing CHANGELOG.md format** — if it uses different headers (e.g., date-only headers, no Keep-a-Changelog sections), match that style. The content above is the spec; the format follows the file.

If CHANGELOG.md doesn't exist or is empty, create it with a minimal Keep-a-Changelog header:
```markdown
# Changelog

All notable changes to the aic-dt extension are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to phase-based milestone releases.
```

After writing the entry, verify:
- `grep -c "Phase 1: Foundation Parity" exts/aic-dt/docs/CHANGELOG.md` returns >= 1
- `grep -c "PARITY-04" exts/aic-dt/docs/CHANGELOG.md` returns >= 1
- `grep -c "Robotiq Hand-E" exts/aic-dt/docs/CHANGELOG.md` returns >= 1 (per D-03 minimum target list)
- `grep -c "RG2" exts/aic-dt/docs/CHANGELOG.md` returns 0 OR returns matches only inside historical context (e.g., "RG2 → Robotiq Hand-E" describing the correction itself is acceptable; standalone "RG2" as a current identifier is not). Inspect manually if the count is non-zero.
  </action>
  <verify>
    <automated>test -f exts/aic-dt/docs/CHANGELOG.md && grep -q "Phase 1: Foundation Parity" exts/aic-dt/docs/CHANGELOG.md && grep -q "PARITY-04" exts/aic-dt/docs/CHANGELOG.md && grep -q "Robotiq Hand-E" exts/aic-dt/docs/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `exts/aic-dt/docs/CHANGELOG.md`
    - Contains the literal section title `Phase 1: Foundation Parity`
    - Contains all 13 Phase 1 requirement IDs as literal strings: PARITY-01, PARITY-02, PARITY-03, PARITY-04, PARITY-05, PARITY-12, TEX-01, TEX-02, TEX-03, SCENE-01, SCENE-04, DX-01, DX-02 (each appears at least once in the entry)
    - Automated check: `for ID in PARITY-01 PARITY-02 PARITY-03 PARITY-04 PARITY-05 PARITY-12 TEX-01 TEX-02 TEX-03 SCENE-01 SCENE-04 DX-01 DX-02; do grep -q "$ID" exts/aic-dt/docs/CHANGELOG.md || { echo "MISSING: $ID"; exit 1; }; done`
    - Contains literal `Robotiq Hand-E` (the corrected gripper name)
    - Contains references to: TF publisher, JointState publisher, asset vendoring, prim-path fix, _sim/_real cleanup, RG2→Hand-E, quick_start refactor, snapshot reference doc, verify harness, texture sweep
    - Contains a "Known Phase-3 work items" subsection mentioning the cable `SetActive(False)` workaround per D-04
    - The entry follows the existing CHANGELOG.md format/tone (verified by reading the existing file first; the new entry is consistent in style)
    - No standalone "RG2" occurrence as a current/active identifier (occurrences inside descriptions of the correction itself are acceptable)
  </acceptance_criteria>
  <done>Phase 1 milestone entry written in CHANGELOG.md with full requirement-ID coverage and Phase-3 known-issues callout.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm SCENE-01/04 clubbing + DX-02 final 4-surface audit across the whole extension</name>
  <files>exts/aic-dt/aic_dt/extension.py</files>
  <read_first>
    - exts/aic-dt/aic_dt/extension.py (after Tasks 1 + 2 complete — full extension.py state)
    - .planning/phases/01-foundation-parity/01-09-SUMMARY.md (Plan 09's atom inventory + DX-02 surface counts)
    - .planning/phases/01-foundation-parity/01-04-SUMMARY.md (Plan 04's deletion-side DX-02 confirmation)
    - .planning/phases/01-foundation-parity/01-06-SUMMARY.md (Plan 06's TF + JointState atom confirmation)
    - ~/.claude/skills/isaac-sim-extension-dev/SKILL.md (4-surface contract reference)
  </read_first>
  <action>
This task is the **final DX-02 audit** for the entire phase: confirm every atom present has all 4 surfaces, and every deleted atom has 0 surfaces. It also verifies the SCENE-01/SCENE-04 clubbing path is wired into `quick_start` correctly.

**Step 1 — Confirm clubbing in quick_start.** Re-read `quick_start` (already refactored by Task 1 of this plan). The default-spawn step calls `self.add_objects()` (which Plan 09 Task 3 refactored to club the per-component atoms with AIC_OBJECTS defaults). No code change needed here — only verification:
- `quick_start` has a step that calls either `self.add_objects()` (preferred — preserves backwards compat) OR directly calls 4+ per-component atoms with default args (acceptable but more verbose).
- The result IS the same scene state pre-Phase-1 had: task board base + 2 SC ports + 1 NIC card.

If quick_start does NOT call `self.add_objects()` after Task 1's refactor, add it back at the appropriate position (between `setup_force_publish_action_graph` and the workspace-camera setup, per the D-12 ordering documented in Task 1 above). This is a one-line edit.

**Step 2 — DX-02 4-surface audit script.** Author or run an inline audit (no new file needed; the audit lives in this task's verify command):

```python
import re
src = open('exts/aic-dt/aic_dt/extension.py').read()

# Atoms expected to be PRESENT with all 4 surfaces
PRESENT_ATOMS = [
    # Existing pre-Phase-1 atoms (preserved)
    'load_robot', 'setup_action_graph', 'setup_force_publisher', 'setup_wrist_cameras',
    'add_objects', 'execute_python_code',
    # New in Phase 1
    'setup_tf_publisher', 'setup_joint_state_publisher',
    'spawn_task_board_base', 'spawn_lc_mount_rail', 'spawn_sfp_mount_rail',
    'spawn_sc_mount_rail', 'spawn_sc_port', 'spawn_nic_card_mount', 'spawn_nic_card',
]

# Atoms expected to be ABSENT (deleted in Plan 04)
ABSENT_ATOMS = ['setup_pose_publisher', 'sync_real_poses']

failures = []
for atom in PRESENT_ATOMS:
    # Surface 1: registry entry — string in quotes followed by colon
    if not re.search(rf'"{atom}":\s*\{{', src):
        failures.append(f'{atom}: registry entry MISSING')
    # Surface 3: _cmd_ method
    if f'def _cmd_{atom}' not in src and atom != 'execute_python_code':  # execute_python_code may have a different naming convention
        # Allow alternate dispatcher patterns
        if f'_cmd_{atom}' not in src:
            failures.append(f'{atom}: _cmd_ handler MISSING')

for atom in ABSENT_ATOMS:
    if f'_cmd_{atom}' in src:
        failures.append(f'{atom}: _cmd_ handler should NOT exist (deleted in Plan 04)')
    if f'"{atom}"' in src:
        failures.append(f'{atom}: registry/handler-map entry should NOT exist (deleted in Plan 04)')

if failures:
    raise SystemExit(f'DX-02 audit failures: {failures}')
print(f'DX-02 audit PASS: {len(PRESENT_ATOMS)} present atoms confirmed, {len(ABSENT_ATOMS)} absent atoms confirmed')
```

**Step 3 — Reconcile any audit failures.** If Step 2's audit fails for a known reason (e.g., the `setup_force_publisher` atom is registered as `setup_force_publish_action_graph` instead — naming convention difference), update the PRESENT_ATOMS list above to match the actual canonical names, OR fix the naming inconsistency in extension.py to match the convention. Document the choice in the SUMMARY.

If the audit fails for an unknown reason (a new atom missing a surface), STOP and fix the missing surface — that's a DX-02 violation per D-02.

**Step 4 — Inventory the audit result in CHANGELOG.md.** After Task 2 (this plan's CHANGELOG write), append a final paragraph to the Phase 1 entry:

```markdown
### DX-02 4-surface contract — final audit (Phase 1 ship)

| Atom | Registry | Handler-map | `_cmd_*` | UI button | Status |
|------|----------|-------------|----------|-----------|--------|
| setup_tf_publisher | yes | yes | yes | yes | PRESENT (new) |
| setup_joint_state_publisher | yes | yes | yes | yes | PRESENT (new) |
| spawn_task_board_base | yes | yes | yes | yes | PRESENT (new, SCENE-01) |
| spawn_lc_mount_rail | yes | yes | yes | yes | PRESENT (new, SCENE-01) |
| spawn_sfp_mount_rail | yes | yes | yes | yes | PRESENT (new, SCENE-01) |
| spawn_sc_mount_rail | yes | yes | yes | yes | PRESENT (new, SCENE-01) |
| spawn_sc_port | yes | yes | yes | yes | PRESENT (new, SCENE-01) |
| spawn_nic_card_mount | yes | yes | yes | yes | PRESENT (new, SCENE-01) |
| spawn_nic_card | yes | yes | yes | yes | PRESENT (new, SCENE-01) |
| setup_pose_publisher | no | no | no | no | DELETED (DX-01) |
| sync_real_poses | no | no | no | no | DELETED (DX-01) |
```

This table makes the DX-02 invariant explicit in the public-facing changelog.

**Sequencing**: Task 3 runs LAST in Plan 08 (after Tasks 1 and 2). Plan 08 is the last plan in Phase 1; this task's audit is the final gate before phase verification.
  </action>
  <verify>
    <automated>python3 -c "
import re
src = open('exts/aic-dt/aic_dt/extension.py').read()
# All 7 SCENE-01 atoms present
for atom in ['spawn_task_board_base', 'spawn_lc_mount_rail', 'spawn_sfp_mount_rail',
             'spawn_sc_mount_rail', 'spawn_sc_port', 'spawn_nic_card_mount', 'spawn_nic_card']:
    assert f'def _cmd_{atom}' in src, f'Missing _cmd_{atom}'
# Both Phase 1 publisher atoms present
for atom in ['setup_tf_publisher', 'setup_joint_state_publisher']:
    assert f'_cmd_{atom}' in src, f'Missing _cmd_{atom}'
# Both deleted atoms absent
for atom in ['setup_pose_publisher', 'sync_real_poses']:
    assert f'_cmd_{atom}' not in src, f'Deleted atom still present: _cmd_{atom}'
# Clubbing in quick_start (add_objects called)
assert 'self.add_objects' in src or '_cmd_spawn_task_board_base' in src.split('quick_start')[1] if 'quick_start' in src else False
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 SCENE-01 spawn atoms have a `_cmd_<name>` method: `for atom in spawn_task_board_base spawn_lc_mount_rail spawn_sfp_mount_rail spawn_sc_mount_rail spawn_sc_port spawn_nic_card_mount spawn_nic_card; do grep -c "def _cmd_$atom" exts/aic-dt/aic_dt/extension.py; done` returns 1 for each
    - Both Phase 1 publisher atoms have `_cmd_*`: `grep -c "def _cmd_setup_tf_publisher\|def _cmd_setup_joint_state_publisher" exts/aic-dt/aic_dt/extension.py` returns 2
    - Both deleted atoms have ZERO `_cmd_*`: `grep -c "def _cmd_setup_pose_publisher\|def _cmd_sync_real_poses" exts/aic-dt/aic_dt/extension.py` returns 0
    - quick_start clubbing path: `quick_start` body (extracted from extension.py via the function-extraction logic in the verify) contains either `self.add_objects()` OR direct calls to 4+ per-component spawn atoms
    - CHANGELOG has the DX-02 final-audit table: `grep -c "DX-02 4-surface contract — final audit" exts/aic-dt/docs/CHANGELOG.md` returns >= 1
    - CHANGELOG audit table includes at least: `setup_tf_publisher`, `setup_joint_state_publisher`, `spawn_task_board_base`, `setup_pose_publisher`, `sync_real_poses` (mix of present + deleted) — `grep -c "spawn_task_board_base\|setup_tf_publisher\|sync_real_poses" exts/aic-dt/docs/CHANGELOG.md` returns >= 3
    - No `_sim`/`_real` regression: `grep -E '(_sim|_real)\b' exts/aic-dt/aic_dt/extension.py | grep -v 'initialize_simulation_context_async'` returns no matches
  </acceptance_criteria>
  <done>DX-02 final audit passes — all expected atoms have 4 surfaces, all deleted atoms have 0 surfaces; clubbing path verified; CHANGELOG records the audit table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `quick_start` orchestration → MCP socket | No new external surface; same MCP atoms invoked in the documented order. |
| CHANGELOG.md → file system | Append-only doc edits, plain Markdown. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Reordering quick_start could change scene state semantics if a downstream consumer depends on a specific call order between `setup_action_graph` and `setup_force_publisher` | mitigate | The ordering matches D-12's documented spec; rationale embedded in PATTERNS.md. Plan 07's verify harness exercises the full quick_start in `verify_phase_1.sh` — any regression surfaces there. |
| T-07-02 | DoS | If the new TF or JointState graph creation calls hang (e.g., articulation root not yet ready), quick_start blocks | mitigate | Both new builders are sync (not async) and follow the existing `setup_force_publish_action_graph` pattern which is proven stable. Idempotent cleanup (RemovePrim) before recreate. The `await app.next_update_async()` between calls gives the OmniGraph evaluator time to settle. |
| T-07-03 | Information disclosure | CHANGELOG.md content includes path references like `~/Documents/aic` and Docker image digests | accept | Public information by design. The CHANGELOG is the public-facing milestone record. No PII or secrets. |
| T-07-04 | Repudiation | CHANGELOG entry committed without signed-off-by | accept | Project doesn't require sign-off. Git commit is the audit trail. |
</threat_model>

<verification>
- File parses as valid Python (`python3 -c "import ast; ast.parse(...)"` exits 0)
- `quick_start` calls both new TF/JointState builder methods in the documented order
- `_sim`/`_real` cleanliness preserved (no regression introduced by ordering edits)
- CHANGELOG.md has Phase 1 entry with all 8 requirement IDs, Robotiq Hand-E, and Phase-3 known-issues callout
</verification>

<success_criteria>
1. `quick_start` produces the full Phase 1 passive sensor surface in one MCP call (verified end-to-end by `/gsd-verify-work` running `verify_phase_1.sh`).
2. CHANGELOG.md records Phase 1 milestone with full traceability to requirement IDs and decisions.
3. Phase 1 success criteria from ROADMAP.md (9 SC, expanded scope) are addressable by `verify_phase_1.sh`:
   - SC #1 (topic list shows /joint_states /tf /tf_static, no _sim/_real on production) — verify steps 5 + 7
   - SC #2 (joint name set + ordering vs Gazebo) — verify step 5 + Open Q #1 inspection
   - SC #3 (TF tree diff = zero) — verify step 6
   - SC #4 (zero pink/black/missing-texture; sweep doc) — verify step 8 + texture-sweep.md
   - SC #5 (no _sim/_real grep matches) — verify step 7
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parity/01-08-SUMMARY.md` with:
- Diff summary of quick_start changes (lines added / removed / reordered)
- Final method-call ordering inside quick_start (just paste the call sequence)
- Confirmation that early-play step is preserved
- CHANGELOG.md entry length / requirement-ID coverage
- Final extension.py line count vs Phase 1 start
- Note that Phase 1 success criteria are now testable end-to-end via `bash exts/aic-dt/scripts/verify_phase_1.sh`
</output>
