---
phase: 01-foundation-parity
plan: 06
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-04, 01-05]
files_modified:
  - exts/aic-dt/scripts/diff_tf_tree.py
  - exts/aic-dt/scripts/sweep_textures.py
  - exts/aic-dt/scripts/verify_phase_1.sh
  - exts/aic-dt/docs/texture-sweep.md
autonomous: true
requirements: [TEX-01, TEX-02, TEX-03]
tags: [verify, sweep, tf-diff, harness, validation]
must_haves:
  truths:
    - "diff_tf_tree.py parses two .gv files and prints frame/edge diff with non-zero exit on mismatch"
    - "sweep_textures.py triggers a scene load via MCP, greps the latest Kit log for texture/MDL warnings, and emits results to texture-sweep.md as a Markdown table"
    - "verify_phase_1.sh probes MCP port 8768 (hybrid runtime per D-15), launches via launch_postload.py if needed, runs end-to-end checks (quick_start, ros2 topic list, view_frames diff, _sim/_real grep, Kit log grep), and exits non-zero on any failure"
    - "verify_phase_1.sh handles missing ~/Documents/aic per D-13 (skips snapshot-update step gracefully)"
    - "verify_phase_1.sh checks DerivedDataCache size before any cold-launch attempt and restores known-good if < 100MB (CLAUDE.md cache discipline)"
    - "texture-sweep.md is generated/appended-to by sweep_textures.py with the (asset, problem, fix, status) row format from CONTEXT.md D-07"
  artifacts:
    - path: "exts/aic-dt/scripts/diff_tf_tree.py"
      provides: "TF tree .gv diff utility — frame-set + edge-set diff, exit 0 on match"
      min_lines: 30
    - path: "exts/aic-dt/scripts/sweep_textures.py"
      provides: "Texture/MDL sweep — load scene, grep Kit log, emit Markdown table"
      min_lines: 50
    - path: "exts/aic-dt/scripts/verify_phase_1.sh"
      provides: "Phase 1 hybrid-runtime verify harness"
      min_lines: 80
    - path: "exts/aic-dt/docs/texture-sweep.md"
      provides: "Texture sweep findings + fix log per D-07/TEX-03"
  key_links:
    - from: "exts/aic-dt/scripts/verify_phase_1.sh"
      to: "exts/aic-dt/scripts/diff_tf_tree.py"
      via: "invoked with reference .gv + sim .gv"
      pattern: "diff_tf_tree.py"
    - from: "exts/aic-dt/scripts/sweep_textures.py"
      to: "exts/aic-dt/docs/texture-sweep.md"
      via: "generates Markdown table output"
      pattern: "texture-sweep.md"
---

<objective>
Build the tooling that proves Phase 1's success criteria are met. Three scripts + one doc:

1. **`diff_tf_tree.py`** (D-08): parses two `.gv` files (live aic_eval reference vs Isaac Sim's frames.gv), prints frame-set + edge-set diff, exits 0 on match. Reference impl is in RESEARCH.md lines 692-728.
2. **`sweep_textures.py`** (D-07): triggers scene load via MCP, greps the newest Kit log for texture/MDL warnings, classifies each row, emits Markdown to `texture-sweep.md`. Per D-07 the regex set is `MDL`, `texture`, `missing`, `pink`, `fallback`, `not found`; refine after first run.
3. **`verify_phase_1.sh`** (D-15): hybrid-runtime harness — detects MCP port 8768 liveness, launches Isaac Sim via `launch_postload.py` for cold cache if absent, runs end-to-end checks (quick_start, setup_tf_publisher, setup_joint_state_publisher, ros2 topic list with passive-sensor filter, view_frames diff, _sim/_real grep on extension.py, Kit log grep). Exits 0 if all pass.
4. **`texture-sweep.md`** (TEX-03): the shipping doc that `sweep_textures.py` appends to. Initial state is the header-only stub.

All work is in `scripts/` and `docs/` — no `extension.py` edits. Depends on Plan 01 (snapshot artifacts), Plan 02 (vendored assets), Plan 04 (extension.py renames + AIC_OBJECTS), Plan 05 (TF + JointState atoms exist).

Per CLAUDE.md operating discipline, the verify script integrates `prime_usd_cache.py` cache management.

Per RESEARCH Open Question #5, `tf2_tools view_frames` may not be on the host. The script attempts host-side first; if absent, logs a skip message — manual diff via container is documented.

Per RESEARCH Open Question #1, the verify script also probes `aic_controller`'s joint-state subscriber (read-only inspection of `~/Documents/aic/aic_controller/src/`) to determine if joint name ordering is functionally required.

Purpose: Build the "did Phase 1 actually succeed?" proof artifact.
Output: 3 scripts + 1 doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-parity/01-CONTEXT.md
@.planning/phases/01-foundation-parity/01-RESEARCH.md
@.planning/phases/01-foundation-parity/01-PATTERNS.md
@.planning/phases/01-foundation-parity/aic_frames_live.gv
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/diff_tf_tree.py — TF tree .gv diff utility</name>
  <files>exts/aic-dt/scripts/diff_tf_tree.py</files>
  <read_first>
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "Example 3: TF tree diff (Python)" lines 690-728 — full reference impl)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "diff_tf_tree.py" lines 382-418)
    - .planning/phases/01-foundation-parity/aic_frames_live.gv (sample input file format)
  </read_first>
  <action>
Create `exts/aic-dt/scripts/diff_tf_tree.py` using the reference implementation from RESEARCH.md lines 692-728 verbatim. The script:

1. Has shebang `#!/usr/bin/env python3` and a docstring referencing D-08.
2. Imports only `re` and `sys` — no graphviz / pygraphviz dependency.
3. Defines `parse_edges(path)` that opens the .gv file and returns a `set` of `(parent, child)` tuples by matching the regex `r'\s*"([^"]+)"\s*->\s*"([^"]+)"'` per line.
4. In `main()`: argv handling (exactly 2 positional args; usage error → exit 2); compute `ref` and `sim` edge sets; derive frame sets via `{f for e in edges for f in e}`; compute `missing_frames`, `extra_frames`, `missing_edges`, `extra_edges` as set differences.
5. If all four diff sets are empty, print `PASS: TF trees match (N frames, M edges)` and exit 0.
6. Otherwise print `FAIL: TF tree diff (reference=..., sim=...)` and the four diff lists (sorted), exit 1.

After creating the file, `chmod +x exts/aic-dt/scripts/diff_tf_tree.py`.

Self-test: `python3 exts/aic-dt/scripts/diff_tf_tree.py .planning/phases/01-foundation-parity/aic_frames_live.gv .planning/phases/01-foundation-parity/aic_frames_live.gv` should print `PASS: TF trees match (31 frames, 30 edges)` and exit 0.
  </action>
  <verify>
    <automated>chmod +x exts/aic-dt/scripts/diff_tf_tree.py && python3 -c "import ast; ast.parse(open('exts/aic-dt/scripts/diff_tf_tree.py').read())" && python3 exts/aic-dt/scripts/diff_tf_tree.py .planning/phases/01-foundation-parity/aic_frames_live.gv .planning/phases/01-foundation-parity/aic_frames_live.gv | grep -q "PASS:"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `exts/aic-dt/scripts/diff_tf_tree.py` and `test -x` passes
    - File parses as valid Python (`python3 -c "import ast; ast.parse(...)"`)
    - Self-test (same .gv as both args) exits 0 with stdout containing `PASS:`
    - File contains the literal regex `'\s*"([^"]+)"\s*->\s*"([^"]+)"'`
    - Script accepts exactly 2 positional args; 0 args exits 2 with usage message on stderr
    - File header references D-08
  </acceptance_criteria>
  <done>diff_tf_tree.py functional and self-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/sweep_textures.py + initialize docs/texture-sweep.md</name>
  <files>exts/aic-dt/scripts/sweep_textures.py, exts/aic-dt/docs/texture-sweep.md</files>
  <read_first>
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "Pitfall 4: Texture sibling folders missing" lines 555-560; D-07 spec)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "sweep_textures.py" lines 422-449)
    - .planning/phases/01-foundation-parity/01-CONTEXT.md (D-07 patterns)
    - CLAUDE.md (real Kit log location)
    - ~/.claude/skills/isaac-sim-extension-dev/scripts/mcp_test.py (first 50 lines — `send_cmd` MCP socket pattern)
  </read_first>
  <action>
**Part A: Create `exts/aic-dt/scripts/sweep_textures.py`** per D-07. The script:

1. CLI: `argparse` with `--port` (default 8768), `--out` (default `exts/aic-dt/docs/texture-sweep.md`), `--skip-load` flag (skip quick_start, just grep current log).
2. Constants:
   - `KIT_LOG_GLOB = os.path.expanduser('~/.nvidia-omniverse/logs/Kit/Isaac-Sim Full/5.0/kit_*.log')`
   - `PATTERNS = [r'MDL', r'texture', r'missing', r'pink', r'fallback', r'not\s*found']`
3. `send_cmd(port, cmd_type, params, timeout)` function — TCP socket to localhost:port, sends `{"type": cmd_type, "params": ...}` JSON, reads chunks until JSON parses (the existing MCP framing convention). 300s timeout default.
4. `find_newest_kit_log()` — `glob.glob(KIT_LOG_GLOB)`, sort by mtime desc, return `[0]`. Raise FileNotFoundError if empty.
5. `grep_warnings(log_path)` — read the log, return list of `(line_no, line)` tuples where any of `PATTERNS` matches (case-insensitive).
6. `classify(line)` — heuristic extract: search for `assets/<...>` or `/World/<...>` substring; pick the first matching pattern as the "problem" keyword; return `(asset, problem, raw_line)`.
7. `emit_markdown(out_path, hits, log_path)` — append (NEVER overwrite) a new section `## Sweep run <ISO timestamp>` with table header `| Asset | Problem | Fix | Status |` and one row per hit with status `unresolved`. If file doesn't exist, write the file-level header first.
8. `main()`: if not `--skip-load`, send `quick_start` MCP cmd; if status != success, exit 1. Then find newest log, grep, emit. Print summary.

Make executable.

**Part B: Create `exts/aic-dt/docs/texture-sweep.md`** with header-only stub:

```
# Texture / MDL Sweep Log (Phase 1 D-07 / TEX-03)

Each section below is a sweep run. Asset = path or prim. Status: unresolved | resolved | accepted-cosmetic.

## Sweep methodology

Per D-07 (CONTEXT.md), the sweep is a scripted log-grep loop:
1. Load M1 scene via MCP `quick_start`
2. Grep `~/.nvidia-omniverse/logs/Kit/"Isaac-Sim Full"/5.0/kit_*.log` for `MDL`, `texture`, `missing`, `pink`, `fallback`, `not found`
3. Each warning becomes a row below.
4. Iterate fix → re-load → re-grep until zero asset-related warnings.

Run via: `python3 exts/aic-dt/scripts/sweep_textures.py`

## Pre-run baseline (Plan 06 landing)

This file is initialized with no rows. The first sweep run will append a "Sweep run <timestamp>" section.
```
  </action>
  <verify>
    <automated>chmod +x exts/aic-dt/scripts/sweep_textures.py && python3 -c "import ast; ast.parse(open('exts/aic-dt/scripts/sweep_textures.py').read())" && test -f exts/aic-dt/docs/texture-sweep.md && grep -q "Texture / MDL Sweep Log" exts/aic-dt/docs/texture-sweep.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `exts/aic-dt/scripts/sweep_textures.py` and is executable
    - File parses as valid Python
    - File contains literal `KIT_LOG_GLOB` referencing `~/.nvidia-omniverse/logs/Kit/Isaac-Sim Full/5.0/kit_*.log`
    - File contains all D-07 patterns: `MDL`, `texture`, `missing`, `pink`, `fallback`, `not\s*found` (or `not found`)
    - File contains a `send_cmd` function targeting `localhost`
    - File defaults `--port` to 8768
    - `exts/aic-dt/docs/texture-sweep.md` exists; contains literal `Texture / MDL Sweep Log`, `D-07`, `TEX-03`
    - Doc references `python3 exts/aic-dt/scripts/sweep_textures.py` invocation
  </acceptance_criteria>
  <done>sweep_textures.py callable; texture-sweep.md initialized.</done>
</task>

<task type="auto">
  <name>Task 3: Create scripts/verify_phase_1.sh — hybrid-runtime verification harness</name>
  <files>exts/aic-dt/scripts/verify_phase_1.sh</files>
  <read_first>
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (sections "Architecture Patterns — System Architecture Diagram" verify_phase_1.sh box lines 336-346; "Open Questions #1, #5"; assumption A6)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "verify_phase_1.sh" lines 279-378)
    - ~/.claude/skills/isaac-sim-extension-dev/scripts/isaacsim_launch.sh (first 100 lines — port-detect + launch pattern)
    - ~/.claude/skills/isaac-sim-extension-dev/scripts/mcp_test.py (full — MCP socket protocol)
    - ~/.claude/skills/isaac-sim-extension-dev/scripts/prime_usd_cache.py (read `status` and `restore` argparse handling, ~30 lines)
    - CLAUDE.md (cache management discipline, launch flow sections)
  </read_first>
  <action>
Create `exts/aic-dt/scripts/verify_phase_1.sh` per D-15. Bash, `set -euo pipefail`, executable. The script orchestrates these steps in order:

**Header / config block:**
- Shebang, docstring with usage, modes (Attached / Cold / `--no-launch` / `--no-snapshot`), reference to D-15 / D-13 / CLAUDE.md cache discipline
- `set -euo pipefail`
- Variables: `PORT=8768`, `EXT_NAME=aic-dt`, `REPO_ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"`, `SNAPSHOT_DIR`, `REF_FRAMES`, `DIFF_TOOL`, `SWEEP_TOOL`, `LAUNCHER_HELPER`, `POSTLOAD_SCRIPT`, `ISAACSIM_BIN="$HOME/env_isaaclab/bin/isaacsim"`, `CACHE_TOOL`, `CACHE_DIR="$HOME/.cache/ov/DerivedDataCache"`, `KIT_LOG_GLOB`, `EXT_PY="$REPO_ROOT/exts/aic-dt/aic_dt/extension.py"`
- Arg parsing: `--no-launch`, `--no-snapshot` flags
- `FAIL=0` accumulator; `fail()` and `pass()` helpers

**Step 0 — cache health (CLAUDE.md):**
- `du -sm "$CACHE_DIR"` → if size < 100 MB, run `python3 "$CACHE_TOOL" restore known-good`

**Step 1 — port-detect liveness probe:**
- `check_socket()` function: `python3 -c "import socket; s=socket.socket(); s.settimeout(3); s.connect(('localhost',$PORT))"` returns 0/1
- If responsive → attached mode (LAUNCHED=0)
- Else if `--no-launch` set → fail and exit 1
- Else: `nohup "$ISAACSIM_BIN" --exec "$POSTLOAD_SCRIPT" > /tmp/aic_dt_verify.log 2>&1 &`; poll port for 60×3s

**Step 2 — quick_start:**
- Invoke MCP `quick_start` via Python heredoc `python3 - <<EOF ... EOF`. Parse JSON response. Pass if status=success, fail otherwise.

**Step 3 — cache snapshot after success:**
- `python3 "$CACHE_TOOL" snapshot || true` (best-effort)

**Step 4 — TF + JointState publisher atoms:**
- Loop over `setup_tf_publisher` and `setup_joint_state_publisher`, call each via MCP, confirm status=success.

**Step 5 — ros2 topic list passive-sensor check:**
- If host has `ros2`: run `ros2 topic list` and grep for `/joint_states`, `/tf`, `/tf_static`. Pass if all three present.
- Else: try `docker run --rm --net=host ros:humble bash -c 'source /opt/ros/humble/setup.bash && ros2 topic list'` as fallback (Open Q #5).
- If neither works: log skip message; do NOT fail (manual verification documented).

**Step 6 — TF tree diff (D-08):**
- `(cd /tmp && ros2 run tf2_tools view_frames -o sim_frames)` if available
- If `/tmp/sim_frames.gv` exists: `python3 "$DIFF_TOOL" "$REF_FRAMES" "$SIM_FRAMES"` — pass on exit 0, fail on exit 1
- Else: skip with message (Open Q #5)

**Step 7 — _sim/_real grep:**
- `HITS=$(grep -nE '(_sim|_real)\b' "$EXT_PY" | grep -v 'initialize_simulation_context_async' || true)` — pass if empty, fail if not

**Step 8 — Texture/MDL sweep (D-07):**
- `python3 "$SWEEP_TOOL" --port "$PORT" --skip-load || true`
- `LOG_FILE=$(ls -t $KIT_LOG_GLOB 2>/dev/null | head -1)`
- `TEX_HITS=$(grep -iE '(missing|pink|fallback|not found).*texture|MDL.*not found' "$LOG_FILE" | wc -l)`
- Print count; pass if 0, log warning if not (don't fail outright — sweep is iterative per D-07)

**Step 9 — D-13 missing-AIC-repo handling:**
- `if [ ! -d "$HOME/Documents/aic" ]; then echo "  ~/Documents/aic absent — skipping snapshot-update step (D-13)"; fi`

**Step 10 — Open Q #1 joint-ordering inspection:**
- If `~/Documents/aic/aic_controller/src` exists: grep for `joint_state` / `joint_states` / `JointState` references; classify whether subscribers use name-indexed (`name.find(...)`, `name.index(...)`) vs positional (`position[N]`) access; print summary recommendation.

**Final summary:**
- If FAIL=0: print success banner, exit 0
- Else: print failure banner, exit 1

Make executable. Self-test: `bash -n exts/aic-dt/scripts/verify_phase_1.sh` exits 0.

Note: this script is NOT run end-to-end during this plan's execution. It's created here and exercised during `/gsd-verify-work`. The acceptance criteria checks structure (syntax + content), not end-to-end behavior.
  </action>
  <verify>
    <automated>chmod +x exts/aic-dt/scripts/verify_phase_1.sh && bash -n exts/aic-dt/scripts/verify_phase_1.sh</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `exts/aic-dt/scripts/verify_phase_1.sh` and is executable
    - `bash -n exts/aic-dt/scripts/verify_phase_1.sh` exits 0
    - File contains `set -euo pipefail`
    - File contains `PORT=8768`
    - File contains `prime_usd_cache.py` reference (cache discipline)
    - File contains `restore known-good` literal
    - File contains `launch_postload.py` reference (cold-cache fallback)
    - File contains the literal `quick_start` MCP command string
    - File contains `setup_tf_publisher` AND `setup_joint_state_publisher` (Plan 05 atoms)
    - File contains `_sim` AND `_real` grep step on extension.py
    - File contains the Kit log glob path `~/.nvidia-omniverse/logs/Kit/Isaac-Sim Full/5.0/kit_*.log`
    - File contains `--no-launch` arg parsing
    - File contains the literal check `! -d "$HOME/Documents/aic"` (D-13)
    - File invokes `diff_tf_tree.py` for the TF diff step
    - File contains a final summary block printing PASS or FAIL
  </acceptance_criteria>
  <done>verify_phase_1.sh syntax-valid, executable, integrates cache discipline + Phase 1 success-criteria checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verify script → MCP socket on 8768 | Same socket as production MCP atoms; verify is a client only. |
| Verify script → Kit log file | Read-only grep. No secrets in logs (USD prim paths only). |
| Verify script → Docker (optional view_frames fallback) | Same trust assumption as Plan 01's snapshot script. |
| Sweep script → Markdown output | Append-only to `exts/aic-dt/docs/texture-sweep.md` — preserves history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Verify script reads Kit log path that local user could write to | accept | Sim-side dev script on a single-user host. Trusted local user. ASVS L1. |
| T-06-02 | Spoofing | Sweep triggers `quick_start` via MCP — local clients can do same | accept | MCP socket binds 127.0.0.1 only (per CLAUDE.md / extension.py). No remote exposure. |
| T-06-03 | Information disclosure | sweep_textures.py writes log content into a committed Markdown doc | mitigate | classify() heuristic extracts asset path only, not raw log line. Reviewer should glance for unexpected content during PR review. |
| T-06-04 | Denial of service | Verify script's quick_start has 300s timeout; cold cache may exceed | mitigate | Cache discipline in step 0: restore known-good first if size < 100 MB. Warm path is ~5s; restored cache is ~5s. |
| T-06-05 | Elevation of privilege | None — pure user-level operations | accept | No sudo / setuid involved. |
</threat_model>

<verification>
- All three scripts executable + syntax-valid (`python3 ast.parse` for .py, `bash -n` for .sh)
- `python3 exts/aic-dt/scripts/diff_tf_tree.py .planning/phases/01-foundation-parity/aic_frames_live.gv .planning/phases/01-foundation-parity/aic_frames_live.gv` exits 0 with `PASS:`
- `exts/aic-dt/docs/texture-sweep.md` exists with header
- All scripts reference correct paths from CLAUDE.md (Kit log glob, prime_usd_cache, postload script, real ext folder)
</verification>

<success_criteria>
1. Three Phase-1 verification tooling scripts ship under `exts/aic-dt/scripts/`.
2. `texture-sweep.md` doc shipped with header (rows populated by first sweep run during `/gsd-verify-work`).
3. `verify_phase_1.sh` integrates: cache discipline, port-detect, cold-launch fallback via launch_postload.py, MCP atom invocations (quick_start + setup_tf_publisher + setup_joint_state_publisher), ros2 topic check, TF diff via diff_tf_tree.py, _sim/_real grep, sweep, AIC-repo-missing handling, joint-ordering inspection.
4. Each script independently testable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parity/01-06-SUMMARY.md` with:
- Self-test results for each script
- Whether scripts have been run end-to-end yet (likely no — verifier's job)
- Environmental gotchas surfaced (tf2_tools availability, Docker, ROS host install)
- Stub for future iteration on the texture-sweep regex set
</output>
