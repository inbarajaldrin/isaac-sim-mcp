---
phase: 01-foundation-parity
plan: 04
type: execute
wave: 2
depends_on: [01-02]
files_modified:
  - exts/aic-dt/aic_dt/extension.py
autonomous: true
requirements: [PARITY-01, PARITY-02, DX-01]
tags: [extension, rename, prim-path, hand-e, sim-real-cleanup]
must_haves:
  truths:
    - "extension.py contains zero `_sim` and zero `_real` substrings on production-surface lines (camera topics, wrench topic, USD prim paths, MCP atom names, action graph topics)"
    - "extension.py uses Robotiq Hand-E (not RG2) in docstrings, log messages, and return strings"
    - "load_robot looks up joints at `/World/UR5e/aic_unified_robot/joints/<joint_name>` (corrected prim-path bug per RESEARCH Pitfall #2) — joint drives now apply"
    - "AIC_OBJECTS dict references the capitalized `assets/<Object>/...` vendored paths from Plan 02"
    - "MCP_TOOL_REGISTRY no longer has `setup_pose_publisher` or `sync_real_poses` entries; their handler-map / handler-method / UI-button surfaces are also deleted (4-surface contract honored)"
    - "Camera topic strings match live aic_eval: /center_camera/image, /left_camera/image, /right_camera/image (with /camera_info siblings)"
    - "Wrench topic string matches live aic_eval: /fts_broadcaster/wrench"
  artifacts:
    - path: "exts/aic-dt/aic_dt/extension.py"
      provides: "Production extension code with renames + prim-path fix + Hand-E corrections + dead atom deletions"
      contains: "/aic_unified_robot/joints/"
  key_links:
    - from: "extension.py:_cmd_load_robot"
      to: "extension.py:articulation prim path"
      via: "joint_path = f'{prim_path}/aic_unified_robot/joints/{joint_name}'"
      pattern: "aic_unified_robot/joints/"
    - from: "extension.py:AIC_OBJECTS"
      to: "exts/aic-dt/assets/assets/<Object>/"
      via: "_local_asset(relpath) resolves capitalized paths"
      pattern: "assets/SC Port|assets/Task Board Base|assets/NIC Card Mount"
---

<objective>
Land all `extension.py` edits that have NO new OmniGraph atoms attached: the 33-row `_sim`/`_real` rename table from RESEARCH.md, the RG2→Hand-E docstring/log/return-message corrections from D-03, the latent kinematic-prim-path bug fix per Pitfall #2 (the joint-path lookup at `/World/UR5e/joints/<n>` that should be `/World/UR5e/aic_unified_robot/joints/<n>`), the deletion of the `setup_pose_publisher` and `sync_real_poses` MCP atoms (4 surfaces each — registry / handler map / `_cmd_*` method / UI button), and the `AIC_OBJECTS` path update to the capitalized vendored layout from Plan 02.

This plan does NOT add the new TF or JointState publisher atoms — those are Plan 05. It also does not refactor `quick_start` — that's Plan 07. By splitting this way, Plan 04 produces a code-state that's renamed-and-cleaned but still has the OLD pose-publisher graph removed and no NEW publisher graphs in place — a known-broken interim state. The `quick_start` flow's reference to `self.create_pose_publisher()` is therefore deleted in this plan but the new TF/JointState calls are NOT yet added; quick_start temporarily skips that step.

The `setup_action_graph` atom (kept — it's the JointState SUBSCRIBE side that Phase 2 needs) and the `setup_force_publisher` atom (kept — wrench topic just renamed) are left in place.

Per RESEARCH.md Pitfall #2 + #3, the prim-path bug fix is the **prerequisite** for Plan 06's (renumbered from 05) JointState publisher's `targetPrim` relationship to resolve correctly. Plan 06 cannot work without this plan.

**CRITICAL — Sequential Task Ordering:** Tasks MUST execute in order 1 → 2 → 3. Each task edits `extension.py` and Task 3's deletions shift line numbers used by Task 1's renames. After EACH task, verify the file still parses as valid Python (`python3 -c "import ast; ast.parse(open('exts/aic-dt/aic_dt/extension.py').read())"`) before proceeding to the next task. Any syntax error mid-flight aborts the plan.

Purpose: Land the foundational extension.py cleanup; stage for Plan 06's new graphs.
Output: extension.py with renames + prim-path fix + Hand-E + dead-atom deletions, all in one atomic plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-parity/01-CONTEXT.md
@.planning/phases/01-foundation-parity/01-RESEARCH.md
@.planning/phases/01-foundation-parity/01-PATTERNS.md
@.planning/phases/01-foundation-parity/01-01-SUMMARY.md
@.planning/phases/01-foundation-parity/01-02-SUMMARY.md
@exts/aic-dt/aic_dt/extension.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply 33-row _sim/_real rename table + RG2→Hand-E in extension.py</name>
  <files>exts/aic-dt/aic_dt/extension.py</files>
  <read_first>
    - exts/aic-dt/aic_dt/extension.py (lines 100-130 — WRIST_CAMERAS dict around lines 108/116/124; lines 160-180 — MCP registry start; lines 870-925 — workspace_camera setup; lines 990-1005 — load_robot ending log message; lines 1065-1130 — force publisher graph; lines 1280-1340 — workspace camera prim/graph setup; lines 2350-2360 — load_robot return message)
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "DX-01 / D-09 — `_sim` / `_real` Production-Surface Rename Table" lines 731-786)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "_sim/_real rename — 33-line edit table" lines 261-275)
  </read_first>
  <action>
Apply the rename table from RESEARCH.md lines 736-765 verbatim. The table is the spec; each row is line-anchored. Use the Edit tool for each row (or batch via a careful multi-edit). Do NOT use sed — many of the strings include slashes and special characters and cross-line context matters.

**Renames (each row: location → current string → replacement; line numbers are approximate, find by string content):**

1. Line ~108 (`WRIST_CAMERAS["center_camera"]`):
   - `"topic": "center_camera_rgb_sim"` → `"topic": "center_camera/image"`
   - `"info_topic": "center_camera_info"` → `"info_topic": "center_camera/camera_info"`

2. Line ~116 (`WRIST_CAMERAS["left_camera"]`):
   - `"topic": "left_camera_rgb_sim"` → `"topic": "left_camera/image"`
   - `"info_topic": "left_camera_info"` → `"info_topic": "left_camera/camera_info"`

3. Line ~124 (`WRIST_CAMERAS["right_camera"]`):
   - `"topic": "right_camera_rgb_sim"` → `"topic": "right_camera/image"`
   - `"info_topic": "right_camera_info"` → `"info_topic": "right_camera/camera_info"`

4. Line ~166 (MCP_TOOL_REGISTRY load_robot description):
   - `"Import the UR5e robot (with integrated RG2 gripper and cable)..."` → `"Import the UR5e robot (with integrated Robotiq Hand-E gripper and cable)..."`

5. Line ~876 (workspace camera USD prim path):
   - `ws_prim_path = "/World/workspace_camera_sim"` → `ws_prim_path = "/World/workspace_camera"`

6. Line ~921 (workspace camera graph creation call):
   - `"workspace_camera_sim", "WorkspaceCameraSim"` → `"workspace_camera", "WorkspaceCamera"`

7. Line ~999 (load_robot success print):
   - `print("UR5e robot loaded successfully (with integrated RG2 gripper and cable)!")` → `print("UR5e robot loaded successfully (with integrated Robotiq Hand-E gripper and cable)!")`

8. Line ~1066 (setup_force_publish_action_graph docstring):
   - `"""Setup force publishing as geometry_msgs/WrenchStamped on /force_torque_sensor_broadcaster/wrench_sim."""` → `"""Setup force publishing as geometry_msgs/WrenchStamped on /fts_broadcaster/wrench."""`

9. Line ~1102 (force publisher topic name):
   - `("publisher.inputs:topicName", "force_torque_sensor_broadcaster/wrench_sim")` → `("publisher.inputs:topicName", "fts_broadcaster/wrench")`

10. Line ~1122 (force publisher print):
    - `print("Publishing geometry_msgs/WrenchStamped to topic: /force_torque_sensor_broadcaster/wrench_sim")` → `print("Publishing geometry_msgs/WrenchStamped to topic: /fts_broadcaster/wrench")`

11. Line ~1293 (workspace camera prim path in another method):
    - `prim_path = "/World/workspace_camera_sim"` → `prim_path = "/World/workspace_camera"`

12. Line ~1326 (`if not stage.GetPrimAtPath(...)` for workspace cam):
    - `if not stage.GetPrimAtPath("/World/workspace_camera_sim"):` → `if not stage.GetPrimAtPath("/World/workspace_camera"):`

13. Line ~1329 (workspace camera action graph creation):
    - `self._create_camera_actiongraph("/World/workspace_camera_sim", width, height, "workspace_camera_sim", "WorkspaceCameraSim")` → `self._create_camera_actiongraph("/World/workspace_camera", width, height, "workspace_camera", "WorkspaceCamera")`

14. Line ~2357 (load_robot MCP handler return):
    - `return {"status": "success", "message": "UR5e loaded with integrated RG2 gripper at /World/UR5e"}` → `return {"status": "success", "message": "UR5e loaded with integrated Robotiq Hand-E gripper at /World/UR5e"}`

After all renames: run `grep -nE '(_sim|_real)' exts/aic-dt/aic_dt/extension.py | grep -v 'await world.initialize_simulation_context_async'` and confirm result is empty (the `initialize_simulation_context_async` line at ~942 is a false-positive for `_sim` substring; per RESEARCH.md line 750 it's NOT a rename target).

Run `grep -c "RG2" exts/aic-dt/aic_dt/extension.py` and confirm 0.
  </action>
  <verify>
    <automated>python3 -c "
import re
src = open('exts/aic-dt/aic_dt/extension.py').read()
hits = []
for m in re.finditer(r'_sim\b|_real\b|RG2|wrench_sim|workspace_camera_sim', src):
    line_no = src[:m.start()].count('\n') + 1
    line = src.split('\n')[line_no-1]
    if 'initialize_simulation_context_async' in line:
        continue
    hits.append((line_no, line.strip()))
assert not hits, f'Unexpected hits: {hits}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '(_sim|_real)\b' exts/aic-dt/aic_dt/extension.py | grep -v 'initialize_simulation_context_async'` returns no matches (zero output)
    - `grep -c "RG2" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "Robotiq Hand-E" exts/aic-dt/aic_dt/extension.py` returns >= 3 (lines 166, 999, 2357 from D-03 minimum target list)
    - `grep -c '"center_camera/image"' exts/aic-dt/aic_dt/extension.py` returns >= 1 (the renamed topic)
    - `grep -c '"left_camera/image"' exts/aic-dt/aic_dt/extension.py` returns >= 1
    - `grep -c '"right_camera/image"' exts/aic-dt/aic_dt/extension.py` returns >= 1
    - `grep -c '"fts_broadcaster/wrench"' exts/aic-dt/aic_dt/extension.py` returns >= 1
    - `grep -c '/World/workspace_camera"' exts/aic-dt/aic_dt/extension.py` returns >= 2 (prim path and GetPrimAtPath checks)
    - `grep -c "WorkspaceCamera\"" exts/aic-dt/aic_dt/extension.py` returns >= 1 (the suffix without "Sim")
    - File still parses as valid Python: `python3 -c "import ast; ast.parse(open('exts/aic-dt/aic_dt/extension.py').read())"` exits 0
  </acceptance_criteria>
  <done>33-row rename + RG2→Hand-E corrections applied; extension.py is valid Python (intermediate AST parse passes — sanity check before Task 2); zero `_sim`/`_real`/`RG2` production hits.</done>
</task>

<task type="auto">
  <name>Task 2: Fix kinematic prim-path bug in load_robot + update AIC_OBJECTS to capitalized vendored layout</name>
  <files>exts/aic-dt/aic_dt/extension.py</files>
  <read_first>
    - exts/aic-dt/aic_dt/extension.py (lines 933-999 — current `load_robot` method body; line ~988 has the buggy `joint_path = f"{prim_path}/joints/{joint_name}"`; lines 58-79 — AIC_OBJECTS dict)
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "Pitfall 2: USD prim path mismatch" lines 538-543; section "Vendoring Manifest (D-05)" lines 855-867 — the AIC_OBJECTS update target)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "extension.py load_robot prim-path bug fix" lines 195-219)
    - .planning/phases/01-foundation-parity/01-02-SUMMARY.md (Plan 02 vendoring summary — confirm capitalized paths exist)
  </read_first>
  <action>
**Part A: Fix the prim-path bug in `load_robot` (RESEARCH Pitfall #2).**

Locate the joint-drive configuration loop in `load_robot` (around line 986-997). Current code:

```python
for joint_name in self._joint_names:
    joint_path = f"{prim_path}/joints/{joint_name}"
    joint_prim = stage.GetPrimAtPath(joint_path)
    if not joint_prim.IsValid():
        print(f"Warning: Joint not found at {joint_path}")
        continue
    drive_api = UsdPhysics.DriveAPI.Apply(joint_prim, "angular")
    ...
```

Fix: change the `joint_path` f-string to insert the `aic_unified_robot/` infix:

```python
for joint_name in self._joint_names:
    joint_path = f"{prim_path}/aic_unified_robot/joints/{joint_name}"
    joint_prim = stage.GetPrimAtPath(joint_path)
    if not joint_prim.IsValid():
        print(f"Warning: Joint not found at {joint_path}")
        continue
    drive_api = UsdPhysics.DriveAPI.Apply(joint_prim, "angular")
    ...
```

This is a single-line edit. The `print(f"Warning: ...")` automatically reflects the new path, so no second edit needed.

After the edit, the articulation root for downstream consumers (e.g., Plan 05's JointState publisher's `targetPrim` relationship) is `/World/UR5e/aic_unified_robot`. Add a class-level constant or attribute if not already present:

```python
# Near the top of the DigitalTwin class init or as a class attribute
self._articulation_root_prim_path = "/World/UR5e/aic_unified_robot"
```

If `self._robot_prim_path` (referenced in `setup_action_graph` line ~1054 and similar) currently equals `/World/UR5e`, leave it as-is (the action graph subscribes/publishes joint state targeting the articulation root via this attribute). Plan 05 will use `self._articulation_root_prim_path` for the new TF/JointState publishers.

Verification check via `grep`: after edit, `grep -n 'aic_unified_robot/joints' exts/aic-dt/aic_dt/extension.py` should return exactly one line (line ~988).

**Part B: Update `AIC_OBJECTS` dict to capitalized vendored paths.**

Locate the `AIC_OBJECTS` dict (around lines 58-79). Current entries reference snake_case paths under `objects/`. Update to AIC's capitalized layout per Plan 02 vendoring (RESEARCH.md lines 858-867):

```python
AIC_OBJECTS = {
    "task_board_base": {
        "usd": "assets/Task Board Base/base_visual.usd",
        "position": (0.2837, 0.229, 0.0),
        "rotation": None,
    },
    "sc_port_1": {
        "usd": "assets/SC Port/sc_port_visual.usd",
        # KEEP existing position / rotation values — only the usd path changes
    },
    "sc_port_2": {
        "usd": "assets/SC Port/sc_port_visual.usd",
        # KEEP existing position / rotation
    },
    "nic_card": {
        "usd": "assets/NIC Card Mount/nic_card_visual.usd",
        # KEEP existing position / rotation
    },
}
```

**Important:** Read the existing dict first to capture the actual `position` / `rotation` values; do NOT rewrite them. Only change the `usd` value strings. The position/rotation tuples are scene-placement and were tuned earlier — preserve them verbatim.

Verify after edit:
- `grep -n 'objects/sc_port' exts/aic-dt/aic_dt/extension.py` returns no matches
- `grep -nE 'assets/SC Port|assets/Task Board Base|assets/NIC Card Mount' exts/aic-dt/aic_dt/extension.py` returns >= 4 matches (one per AIC_OBJECTS entry)

**Part C: Sanity-test the spaces-in-paths resolver.**

Per RESEARCH.md Open Question #6, the `_local_asset` resolver should handle paths-with-spaces correctly via Python's `os.path.join` and `pxr.Usd`'s `file://` URI parsing. No code change needed for the resolver itself; the AIC_OBJECTS update simply uses paths that contain spaces. The runtime sanity-test (does Isaac Sim load these?) happens in Plan 06's verify harness — not here. This task ends with the static verification.
  </action>
  <verify>
    <automated>python3 -c "
import re, ast
src = open('exts/aic-dt/aic_dt/extension.py').read()
ast.parse(src)
# Bug fix verification
m = re.search(r'joint_path\s*=\s*f[\"\\']\\{prim_path\\}/aic_unified_robot/joints/\\{joint_name\\}', src)
assert m, 'prim-path bug fix missing'
# AIC_OBJECTS verification
for needle in ['assets/SC Port/sc_port_visual.usd', 'assets/Task Board Base/base_visual.usd', 'assets/NIC Card Mount/nic_card_visual.usd']:
    assert needle in src, f'AIC_OBJECTS missing: {needle}'
# Old path verification
assert 'objects/sc_port/sc_port.usd' not in src, 'old snake_case path still present'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'aic_unified_robot/joints' exts/aic-dt/aic_dt/extension.py` returns 1 line (the f-string in load_robot, line ~988)
    - `grep -c "objects/sc_port" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "objects/nic_card" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "assets/SC Port/sc_port_visual.usd" exts/aic-dt/aic_dt/extension.py` returns >= 2 (sc_port_1 and sc_port_2 entries)
    - `grep -c "assets/Task Board Base/base_visual.usd" exts/aic-dt/aic_dt/extension.py` returns >= 1
    - `grep -c "assets/NIC Card Mount/nic_card_visual.usd" exts/aic-dt/aic_dt/extension.py` returns >= 1
    - File parses as valid Python: `python3 -c "import ast; ast.parse(open('exts/aic-dt/aic_dt/extension.py').read())"` exits 0
    - Existing `position` and `rotation` values in AIC_OBJECTS are preserved (regression check via reading the dict literal — values for sc_port_1/sc_port_2/nic_card unchanged from pre-edit state)
  </acceptance_criteria>
  <done>Prim-path bug fixed; AIC_OBJECTS pointed at capitalized vendored paths; runtime resolver path will work in Plan 07 verify (was Plan 06 before renumbering); intermediate AST parse passes — sanity check before Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Delete setup_pose_publisher and sync_real_poses atoms (4 surfaces each)</name>
  <files>exts/aic-dt/aic_dt/extension.py</files>
  <read_first>
    - exts/aic-dt/aic_dt/extension.py (lines 220-227 — MCP_TOOL_REGISTRY entries; lines 267-268 — handler-map entries; lines 1690-1745 — sync_real_poses body; lines 1747-1816 — create_pose_publisher body; lines 2534-2548 — _cmd_setup_pose_publisher and _cmd_sync_real_poses; lines 480/487 — UI buttons; line ~870 in quick_start — `self.create_pose_publisher()` call)
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "DX-01 / D-09 — Production-Surface Rename Table" lines 745-765 — itemized deletions; section "Recommended File / Module Structure" line 388-392)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "Deletions for D-09" lines 187-191)
  </read_first>
  <action>
Per D-09, delete two MCP atoms entirely. The atomic+clubbed contract requires editing 4 surfaces per atom (RESEARCH.md / PATTERNS.md "Deletions for D-09").

**Atom 1: `setup_pose_publisher` (publishes object poses to `/objects_poses_sim` — replaced by Phase 3 GT TF; Phase 1 has no replacement, just deletion).**

Delete:
- `MCP_TOOL_REGISTRY` entry for `"setup_pose_publisher"` at lines ~220-223 (the dict entry with description "Create an action graph to publish object poses to ROS2 topic 'objects_poses_sim'.").
- Handler-map entry: `"setup_pose_publisher": "_cmd_setup_pose_publisher"` at line ~267.
- Method body: `def create_pose_publisher(self)` at lines ~1747-1816 (the whole method including the `og.Controller.edit` call with `objects_poses_sim` topic).
- Method body: `def _cmd_setup_pose_publisher(self) -> Dict[str, Any]:` at lines ~2534-2541.
- UI button: `ui.Button("Setup Pose Publisher", ...)` at line ~480.
- Call from `quick_start`: the line `self.create_pose_publisher()` around line ~870 in the existing quick_start sequence. **Important:** This deletion leaves a gap in `quick_start` — Plan 07 reorders quick_start and inserts the new TF/JointState calls. Between this plan landing and Plan 07 landing, `quick_start` will simply skip the pose-publisher step (and the new TF publisher won't exist yet — see plan-level note about interim broken state).

**Atom 2: `sync_real_poses` (subscribes to `/objects_poses_real` — placeholder bridge from M2 era; Phase 1 has no replacement).**

Delete:
- `MCP_TOOL_REGISTRY` entry for `"sync_real_poses"` at lines ~224-227.
- Handler-map entry: `"sync_real_poses": "_cmd_sync_real_poses"` at line ~268.
- Method body: `def sync_real_poses(self):` at lines ~1690-1745 (the whole method, including the temporary rclpy node creation, /objects_poses_real subscription, and TFMessage handling).
- Method body: `def _cmd_sync_real_poses(self) -> Dict[str, Any]:` at lines ~2543-2548.
- UI button: `ui.Button("Sync Real Poses", ...)` at line ~487.

After deletions:
- `grep -c "setup_pose_publisher" exts/aic-dt/aic_dt/extension.py` returns 0
- `grep -c "sync_real_poses" exts/aic-dt/aic_dt/extension.py` returns 0
- `grep -c "create_pose_publisher" exts/aic-dt/aic_dt/extension.py` returns 0
- `grep -c "objects_poses_sim" exts/aic-dt/aic_dt/extension.py` returns 0
- `grep -c "objects_poses_real" exts/aic-dt/aic_dt/extension.py` returns 0
- File still parses as valid Python.

**Sanity check after deletion:** verify no orphan references remain. If any other method calls `self.create_pose_publisher()` or `self.sync_real_poses()`, those callers must be removed too. The known caller is `quick_start` (handled above). Run a targeted grep:
```bash
grep -n 'self\.create_pose_publisher\|self\.sync_real_poses' exts/aic-dt/aic_dt/extension.py
```
Should return zero matches after this task.

**Imports cleanup:** the deleted `sync_real_poses` method imported `rclpy.create_node` and `from tf2_msgs.msg import TFMessage` inside its body. If the file has top-level imports of `TFMessage` solely for this method, remove them. Check by grepping for any other TFMessage references — if only `sync_real_poses` used it, the import can go. (Do NOT remove `rclpy` top-level imports — other methods use rclpy.)
  </action>
  <verify>
    <automated>python3 -c "
import ast
src = open('exts/aic-dt/aic_dt/extension.py').read()
ast.parse(src)
banned = ['setup_pose_publisher', 'sync_real_poses', 'create_pose_publisher', 'objects_poses_sim', 'objects_poses_real', '_cmd_sync_real_poses', '_cmd_setup_pose_publisher']
hits = [b for b in banned if b in src]
assert not hits, f'Banned strings still present: {hits}'
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setup_pose_publisher" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "sync_real_poses" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "create_pose_publisher" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "objects_poses_sim" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "objects_poses_real" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "_cmd_setup_pose_publisher" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "_cmd_sync_real_poses" exts/aic-dt/aic_dt/extension.py` returns 0
    - `grep -c "Sync Real Poses" exts/aic-dt/aic_dt/extension.py` returns 0 (UI button label)
    - `grep -c "Setup Pose Publisher" exts/aic-dt/aic_dt/extension.py` returns 0 (UI button label)
    - File parses as valid Python: `python3 -c "import ast; ast.parse(open('exts/aic-dt/aic_dt/extension.py').read())"` exits 0
    - `grep -c "MCP_TOOL_REGISTRY" exts/aic-dt/aic_dt/extension.py` returns >= 2 (the dict + handler-map are still there; only the two atoms' entries are removed)
  </acceptance_criteria>
  <done>Both atoms removed across all 4 surfaces (registry / handler-map / method body / UI button); quick_start no longer calls the deleted methods. **DEFINITIVE end-of-plan check:** AST parse passes, all greps green per acceptance criteria — this is the plan-level verification, supersedes intermediate Task 1/2 sanity checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extension.py edits → MCP socket surface (port 8768) | Removing two MCP atoms shrinks the attack surface. No new atoms added in this plan. |
| Action graph cleanup | The pose-publisher OmniGraph at `/Graph/ActionGraph_PosePublish` (or similar) is no longer created on extension reload. If a stale graph exists from a prior session in a saved scene, it's harmless (no longer ticked because no consumer). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Bulk renames may accidentally edit comments or strings outside the rename table | mitigate | Each rename is line-anchored against RESEARCH.md's table. Acceptance criteria runs grep with explicit allowlist (false-positive `initialize_simulation_context_async` excluded). Python AST parse confirms file is still valid. |
| T-04-02 | Repudiation | Removing MCP atoms changes the public tool surface — agents calling `setup_pose_publisher` will get an error response | accept | The removed atoms were placeholders for `_sim`/`_real` discrimination that Phase 1 explicitly retires (D-09). Their removal is the goal, not a regression. CHANGELOG (Plan 07) records the breaking change. |
| T-04-03 | Information disclosure | The `aic_unified_robot` prim path is the actual articulation root — exposing it in the joint-path string isn't sensitive (USD prim paths are local) | accept | USD prim paths are not secrets. Documented in topic-parity-reference.md (Plan 01). |
| T-04-04 | Denial of service | Prim-path bug fix may unmask other issues (e.g., joint drives now apply, articulation behavior changes) | mitigate | Fix is the documented correct path per RESEARCH Pitfall #2 — already empirically observed via Kit log "Joint not found" warnings. The "fix" surfaces working behavior; if downstream depends on broken behavior, that's a separate bug to chase via the verify harness in Plan 06. |
</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(open('exts/aic-dt/aic_dt/extension.py').read())"` exits 0 (valid Python)
- `grep -E '(_sim|_real|RG2)\b' exts/aic-dt/aic_dt/extension.py | grep -v initialize_simulation_context_async` returns no matches
- `grep -c "aic_unified_robot/joints" exts/aic-dt/aic_dt/extension.py` returns 1
- `grep -c "objects_poses" exts/aic-dt/aic_dt/extension.py` returns 0
- `grep -c "Robotiq Hand-E" exts/aic-dt/aic_dt/extension.py` returns >= 3
- `grep -c "fts_broadcaster/wrench" exts/aic-dt/aic_dt/extension.py` returns >= 1
- `grep -c "/image\"" exts/aic-dt/aic_dt/extension.py` returns >= 3 (3 wrist cameras)
</verification>

<success_criteria>
1. Roadmap Phase 1 SC #5 met: `objects_poses_sim`, `sync_real_poses`, `_sim`, `_real` return no production matches in `extension.py`.
2. Robotiq Hand-E correctly identified in code-side docstrings, log messages, and return strings.
3. Prim-path bug fix lands; the empirical "Joint not found" warning that's been in Kit log since the unified USD was adopted is resolved (verified at runtime in Plan 06).
4. AIC_OBJECTS uses the capitalized vendored layout; loading objects via `_local_asset` resolves correctly (verified at runtime in Plan 06).
5. extension.py is valid Python and shorter (two atoms' worth of code deleted) than before this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parity/01-04-SUMMARY.md` with:
- Number of edit operations (renames + deletions)
- Final line count of extension.py vs pre-plan
- Confirmation grep counts for the acceptance criteria
- Note that quick_start currently has a "gap" where pose-publisher used to be — closed in Plan 07
</output>
