---
phase: 01-foundation-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - exts/aic-dt/assets/assets/
  - exts/aic-dt/assets/objects/
autonomous: true
requirements: [PARITY-02, TEX-01, TEX-02]
tags: [vendoring, assets, usd, textures]
must_haves:
  truths:
    - "Each per-object USD referenced by load_robot/add_objects has its sibling textures/ directory present"
    - "AIC's capitalized folder layout (e.g. 'NIC Card', 'SC Port', 'Task Board Base') is preserved verbatim under exts/aic-dt/assets/assets/"
    - "Snake_case objects/ tree is retired (or empty) — production code paths reference the new capitalized layout only"
    - "Vendored USDs are byte-identical copies of AIC's Intrinsic_assets/<Object>/ tree"
  artifacts:
    - path: "exts/aic-dt/assets/assets/SC Port/sc_port_visual.usd"
      provides: "SC Port visual USD"
    - path: "exts/aic-dt/assets/assets/SC Port/textures/Image_0.png"
      provides: "SC Port texture sibling (was missing in old snake_case layout)"
    - path: "exts/aic-dt/assets/assets/NIC Card Mount/nic_card_visual.usd"
      provides: "NIC Card Mount visual USD"
    - path: "exts/aic-dt/assets/assets/Task Board Base/base_visual.usd"
      provides: "Task Board Base visual USD"
  key_links:
    - from: "exts/aic-dt/assets/assets/SC Port/sc_port_visual.usd"
      to: "exts/aic-dt/assets/assets/SC Port/textures/Image_*.png"
      via: "relative reference inside USD"
      pattern: "./textures/Image_"
---

<objective>
Vendor the precise subset of AIC's `Intrinsic_assets/` tree into `exts/aic-dt/assets/`, preserving AIC's capitalized folder layout (`NIC Card/`, `SC Port/`, `Task Board Base/`) AND copying the sibling `textures/` folders that the per-object USDs reference relatively. The current snake_case `assets/objects/` reorganization is missing texture sibling folders — that's the empirically-verified root cause of pink/black material risk for TEX-01/02 (per RESEARCH.md Pitfall #4 and the "Vendoring Manifest" section). After this plan: every per-object USD's `./textures/Image_*.{png,jpg}` reference resolves under `exts/aic-dt/assets/assets/<Object>/textures/`.

Per D-05, this is a one-time copy. Per D-06, vendored USDs may be edited in place later; no override-layer indirection. Per D-13, after this lands the extension runs without `~/Documents/aic` checked out.

Purpose: Make every texture binding the unified scene's per-object USDs declare actually resolvable.
Output: `exts/aic-dt/assets/assets/<Capitalized>/...` tree with USDs + textures siblings; old snake_case `objects/` tree retired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-parity/01-CONTEXT.md
@.planning/phases/01-foundation-parity/01-RESEARCH.md
@.planning/phases/01-foundation-parity/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor capitalized AIC asset folders with sibling textures/</name>
  <files>exts/aic-dt/assets/assets/NIC Card/, exts/aic-dt/assets/assets/NIC Card Mount/, exts/aic-dt/assets/assets/SC Port/, exts/aic-dt/assets/assets/SC Plug/, exts/aic-dt/assets/assets/Task Board Base/</files>
  <read_first>
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (section "Vendoring Manifest (D-05)" lines 788-868 — has the exact recursive-walk output and the minimal-vendoring-set tree spec)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (section "Asset vendoring (D-05)" lines 517-555)
    - exts/aic-dt/assets/objects/ (current state — `ls -R exts/aic-dt/assets/objects/` to see what's already there in snake_case)
  </read_first>
  <action>
Vendor the exact subset enumerated in RESEARCH.md "Vendoring Manifest (D-05)" lines 830-853. Source root: `~/Documents/aic/aic_utils/aic_isaac/aic_isaaclab/source/aic_task/aic_task/tasks/manager_based/aic_task/Intrinsic_assets/assets/`. Destination root: `exts/aic-dt/assets/assets/`.

Pre-flight check: verify the source path exists. `test -d ~/Documents/aic/aic_utils/aic_isaac/aic_isaaclab/source/aic_task/aic_task/tasks/manager_based/aic_task/Intrinsic_assets/assets`. If missing, abort with a clear error message — D-13 says runtime works without the AIC repo, but **the vendoring step itself requires it**.

Use `cp -r` (recursive) of each whole capitalized folder — NOT selective .usd-only copy (that would re-introduce Pitfall #4: missing sibling textures/).

Exact commands:
```bash
mkdir -p "exts/aic-dt/assets/assets"
INTRINSIC="$HOME/Documents/aic/aic_utils/aic_isaac/aic_isaaclab/source/aic_task/aic_task/tasks/manager_based/aic_task/Intrinsic_assets/assets"

# Each of these is a recursive copy of the whole object folder, preserving textures/ siblings:
cp -r "$INTRINSIC/NIC Card"        "exts/aic-dt/assets/assets/NIC Card"
cp -r "$INTRINSIC/NIC Card Mount"  "exts/aic-dt/assets/assets/NIC Card Mount"
cp -r "$INTRINSIC/SC Port"         "exts/aic-dt/assets/assets/SC Port"
cp -r "$INTRINSIC/SC Plug"         "exts/aic-dt/assets/assets/SC Plug"
cp -r "$INTRINSIC/Task Board Base" "exts/aic-dt/assets/assets/Task Board Base"
```

Expected files after vendoring (per RESEARCH.md manifest lines 836-852):
- `exts/aic-dt/assets/assets/NIC Card/nic_card.usd`
- `exts/aic-dt/assets/assets/NIC Card/nic_card_visual.usd`
- `exts/aic-dt/assets/assets/NIC Card/textures/{Image_0,Image_1,Image_2,NIC_Albedo}.jpg`
- `exts/aic-dt/assets/assets/NIC Card Mount/nic_card_mount_visual.usd`
- `exts/aic-dt/assets/assets/NIC Card Mount/nic_card_visual.usd`
- `exts/aic-dt/assets/assets/NIC Card Mount/textures/{Image_0,Image_1,Image_2,NIC_Albedo}.jpg`
- `exts/aic-dt/assets/assets/SC Plug/sc_plug_visual.usd`
- `exts/aic-dt/assets/assets/SC Port/sc_port.usd` (if present at source — optional per RESEARCH.md)
- `exts/aic-dt/assets/assets/SC Port/sc_port_visual.usd`
- `exts/aic-dt/assets/assets/SC Port/textures/{Image_0,Image_1}.png`
- `exts/aic-dt/assets/assets/Task Board Base/base_visual.usd`

Do NOT vendor: `Intrinsic_assets/scene.usd` (different from `scene/aic.usd`; the broken-payload aggregate per RESEARCH.md line 870), `UR5e+gripper.usd` (separate USD not used by aic-dt), `props/ISO_*.usd` and `props/V1015120_004.usd` (broken sub-references; sweep step decides per RESEARCH.md line 872-875).

Robustness: if any source `cp -r` target does not exist (AIC tree changed), fail loud with the exact missing path. Do not silently skip.

Note on `_local_asset` resolver compatibility: `_local_asset` in `extension.py:48-54` does `os.path.join(_ext_dir, "assets", relpath)` then constructs a `file://` URI. Per Open Question #6 in RESEARCH.md, paths with spaces should resolve correctly because Python's `os.path` handles them and `pxr.Usd` resolves `file://` URIs with spaces fine. The `os.path.exists` precheck handles spaces natively. **No code change needed for this task** — code path updates are in Plan 04.
  </action>
  <verify>
    <automated>test -f "exts/aic-dt/assets/assets/SC Port/sc_port_visual.usd" && test -f "exts/aic-dt/assets/assets/SC Port/textures/Image_0.png" && test -f "exts/aic-dt/assets/assets/NIC Card Mount/nic_card_visual.usd" && test -f "exts/aic-dt/assets/assets/Task Board Base/base_visual.usd" && test -f "exts/aic-dt/assets/assets/SC Plug/sc_plug_visual.usd"</automated>
  </verify>
  <acceptance_criteria>
    - `find "exts/aic-dt/assets/assets/SC Port/textures" -name "Image_*.png" | wc -l` returns >= 1
    - `find "exts/aic-dt/assets/assets/NIC Card Mount/textures" -name "*.jpg" | wc -l` returns >= 1
    - `test -f "exts/aic-dt/assets/assets/SC Port/sc_port_visual.usd"` succeeds
    - `test -f "exts/aic-dt/assets/assets/Task Board Base/base_visual.usd"` succeeds
    - `test -f "exts/aic-dt/assets/assets/SC Plug/sc_plug_visual.usd"` succeeds
    - `test -f "exts/aic-dt/assets/assets/NIC Card/nic_card_visual.usd"` succeeds OR document why missing in summary
    - `md5sum "exts/aic-dt/assets/assets/SC Port/sc_port_visual.usd"` equals `md5sum "$HOME/Documents/aic/aic_utils/aic_isaac/aic_isaaclab/source/aic_task/aic_task/tasks/manager_based/aic_task/Intrinsic_assets/assets/SC Port/sc_port_visual.usd"` (byte-identical)
  </acceptance_criteria>
  <done>Capitalized vendored asset tree present under `exts/aic-dt/assets/assets/` with sibling textures/ folders preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Retire snake_case objects/ tree</name>
  <files>exts/aic-dt/assets/objects/</files>
  <read_first>
    - exts/aic-dt/assets/objects/ (`ls -R exts/aic-dt/assets/objects/` to see current state)
    - .planning/phases/01-foundation-parity/01-CONTEXT.md (D-05: "current snake_case … is retired during the vendoring step")
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (line 855: "After vendoring with capitalized paths, update [AIC_OBJECTS in extension.py]")
  </read_first>
  <action>
The legacy `exts/aic-dt/assets/objects/` (snake_case `nic_card/`, `sc_plug/`, `sc_port/`) tree is retired per D-05. The new capitalized tree under `assets/assets/` is canonical.

This task does NOT modify `extension.py` (that's Plan 04's `AIC_OBJECTS` dict update). This task only retires the disk artifacts.

Steps:
1. List current contents: `ls -R exts/aic-dt/assets/objects/ > /tmp/old_objects_inventory.txt` (preserve the inventory in this plan's SUMMARY for traceability).
2. Verify no production code references the old paths still in extension.py — if it does, that's Plan 04's scope; this plan still proceeds because the production code is broken-by-design until Plan 04 lands. **Sequencing is correct: vendoring (Plan 02 / Wave 1) → code path update (Plan 04 / Wave 2). Old `objects/` tree's removal in Wave 1 means extension.py is briefly broken between Wave 1 and Wave 2, but no automated tests run in Wave 1 to catch this. The production extension is verified only at Plan 06 / verify_phase_1.sh.**
3. Remove the old tree: `rm -rf exts/aic-dt/assets/objects/`
4. Verify: `test ! -d exts/aic-dt/assets/objects`.

If the directory contains a `README.md` or other doc explaining the layout, preserve those (move to `exts/aic-dt/docs/legacy-objects-layout.md` with a note "RETIRED 2026-05-02 per Phase 1 D-05 — see exts/aic-dt/assets/assets/ for current layout"). At time of writing, `exts/aic-dt/assets/objects/` contains only USDs (no docs), so this is a pure removal.
  </action>
  <verify>
    <automated>test ! -d exts/aic-dt/assets/objects</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d exts/aic-dt/assets/objects` succeeds (directory does not exist)
    - `find exts/aic-dt/assets/ -name "objects" -type d | wc -l` returns 0
    - The capitalized vendored tree from Task 1 is intact (re-verify via `test -f "exts/aic-dt/assets/assets/SC Port/sc_port_visual.usd"`)
  </acceptance_criteria>
  <done>Snake_case `objects/` tree retired; capitalized `assets/assets/` tree is the only on-disk asset layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AIC repo (read-only source) → this repo's `assets/` | One-way `cp -r`; no script execution from source side. AIC's `Intrinsic_assets/` is local, plain-USD content. |
| `assets/` directory → Isaac Sim runtime | Resolved via `_local_asset` (extension.py:48-54) as `file://` URIs. No remote fetch, no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Vendored USDs may diverge from AIC source over time | mitigate | Acceptance criteria includes md5sum byte-identity check on at least one representative USD against AIC source. CHANGELOG.md (Plan 07) records the vendoring date and source path. |
| T-02-02 | Information disclosure | None — assets are upstream-public USD/PNG/JPG content | accept | No PII or secrets in AIC's open asset tree. |
| T-02-03 | Denial of service | `rm -rf` on `assets/objects/` could remove unintended content if path is mistyped | mitigate | Step 3 uses literal absolute-relative path `exts/aic-dt/assets/objects/`. Step 1 inventories before removal. Pre-conditions verified via `test -d` first. |
</threat_model>

<verification>
- `find "exts/aic-dt/assets/assets" -name "*.usd" | wc -l` returns >= 5 (at minimum: SC Port visual, SC Plug visual, Task Board Base visual, NIC Card Mount visual, NIC Card visual)
- `find "exts/aic-dt/assets/assets" -path "*/textures/*" | wc -l` returns >= 4 (SC Port has 2 PNGs; NIC Card Mount has 4 JPGs)
- `test ! -d exts/aic-dt/assets/objects` succeeds
- One md5sum byte-identity comparison against AIC source verified
</verification>

<success_criteria>
1. Capitalized vendored tree exists with USDs + sibling textures/ folders.
2. Snake_case `objects/` tree retired.
3. Byte-identity verified against AIC source for at least one USD (proves correct vendoring, not transformation).
4. After Plan 04 updates `AIC_OBJECTS` paths, the existing `_local_asset` resolver loads each USD with no missing-texture warnings (verified in Plan 06 sweep).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parity/01-02-SUMMARY.md` with:
- Total file count vendored
- AIC source path used
- md5sum verification result for SC Port + Task Board Base
- Inventory of retired snake_case `objects/` tree
- Any source files NOT found at expected location and how resolved
</output>
