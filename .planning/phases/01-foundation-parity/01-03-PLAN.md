---
phase: 01-foundation-parity
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - CLAUDE.md
  - exts/aic-dt/docs/README.md
autonomous: true
requirements: [PARITY-01]
tags: [docs, gripper, hand-e, rg2, correction]
must_haves:
  truths:
    - "No reference to 'RG2' as the gripper remains in the doc surface (.planning/, CLAUDE.md, exts/aic-dt/docs/)"
    - "Robotiq Hand-E is correctly identified as the gripper everywhere the docs describe the robot"
    - "CLAUDE.md robot description line reflects Hand-E (with the cable workaround note still accurate)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project vision doc with Hand-E (not RG2)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements with Hand-E in PARITY-01 / SCENE-03"
    - path: ".planning/ROADMAP.md"
      provides: "Roadmap phase descriptions with Hand-E"
    - path: "CLAUDE.md"
      provides: "Repo on-ramp with Hand-E robot description"
    - path: "exts/aic-dt/docs/README.md"
      provides: "Extension README with Hand-E"
  key_links: []
---

<objective>
Correct the "RG2 gripper" misidentification across the doc surface. Per D-03 in CONTEXT.md, the actual gripper is **Robotiq Hand-E**, confirmed by AIC's authoritative URDF xacro `~/Documents/aic/aic_description/urdf/ur_gz.urdf.xacro` which includes `Robotiq Hand-E/robotiq_hande_macro.xacro`. The current docs (and the extension.py code) all say "RG2" — wrong. This plan fixes the **documentation surface only** in Wave 1 (parallel-safe). The extension.py code-side RG2→Hand-E corrections live in Plan 04 (Wave 2) because they ride along with the bigger _sim/_real cleanup edit on the same file.

Per D-03 minimum target list, this plan covers: `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `CLAUDE.md` (repo root), `exts/aic-dt/docs/README.md`. (CHANGELOG.md gets a Phase 1 entry in Plan 07.)

Out of scope per D-03: `exts/aic-dt/aic_dt/tests/test_wrist3_force.py` (test artifact; DX-05 / Phase 4), sibling extensions, agent worktree mirrors, and the historical `01-DISCUSSION-LOG.md`.

Purpose: Make every shipping doc that names the gripper say what's actually loaded.
Output: 5 doc files updated; zero "RG2" references remain in the targeted files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-parity/01-CONTEXT.md
@.planning/phases/01-foundation-parity/01-RESEARCH.md
@.planning/phases/01-foundation-parity/01-PATTERNS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@CLAUDE.md
@exts/aic-dt/docs/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace RG2 → Robotiq Hand-E across .planning/ docs</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/01-foundation-parity/01-CONTEXT.md (D-03 — list of files + line numbers)
    - .planning/phases/01-foundation-parity/01-RESEARCH.md (lines 776-786 — exhaustive grep result with file/line targets)
    - .planning/phases/01-foundation-parity/01-PATTERNS.md (lines 559-580 — RG2→Hand-E correction spec)
    - .planning/PROJECT.md (current state — focus on lines around "UR5e + RG2", "RG2 gripper" mentions)
    - .planning/REQUIREMENTS.md (current state — PARITY-01 line 10, SCENE-03 line 33)
    - .planning/ROADMAP.md (current state — lines 13, 21, 47, 52)
  </read_first>
  <action>
Execute literal string replacements across the three planning docs. Use the rule:

**Replacement rules (apply in order, longest match first):**
1. `RG2 Gripper` → `Robotiq Hand-E gripper`
2. `UR5e + RG2` → `UR5e + Robotiq Hand-E`
3. `RG2 gripper` → `Robotiq Hand-E gripper`
4. `integrated RG2` → `integrated Robotiq Hand-E`
5. `RG2,` → `Robotiq Hand-E,`
6. ` RG2 ` (whole word) → ` Robotiq Hand-E ` — fallback for any remaining standalone occurrences in prose
7. `RG2.` → `Robotiq Hand-E.`

Files and known target lines (per RESEARCH.md lines 776-786):

**`.planning/PROJECT.md`** — lines 18, 28 (per RESEARCH; verify by grep):
- Line ~18 in Validated section: "UR5e + RG2 + cable" → "UR5e + Robotiq Hand-E + cable"
- Line ~28 in Active M1 section: "for the UR5e + RG2 + 3 wrist cameras + cable + task board" → "for the UR5e + Robotiq Hand-E + 3 wrist cameras + cable + task board"

**`.planning/REQUIREMENTS.md`** — lines 10, 33:
- PARITY-01 (line ~10): "UR5e + RG2 + 3 wrist cameras" → "UR5e + Robotiq Hand-E + 3 wrist cameras"
- SCENE-03 (line ~33): "plug body is attached to the RG2 gripper" → "plug body is attached to the Robotiq Hand-E gripper"

**`.planning/ROADMAP.md`** — lines 13, 21, 47, 52:
- Phase 1 line item (~13): "Same UR5e + RG2 + cameras" → "Same UR5e + Robotiq Hand-E + cameras"
- Phase 1 detail (~21): "exact same UR5e + RG2 + 3 wrist cameras" → "exact same UR5e + Robotiq Hand-E + 3 wrist cameras"
- Phase 3 detail (~47): "the plug body is attached to the RG2 at" → "the plug body is attached to the Robotiq Hand-E at"
- Phase 3 SC #2 (~52): "co-moving with the RG2 gripper" → "co-moving with the Robotiq Hand-E gripper"

Execute the edits using the Edit tool, finding each exact source string and replacing. After all edits, run `grep -in "RG2" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md` and confirm result is empty (zero matches).

Edge case: the literal string `RG2` may exist in a context that should NOT be edited (e.g., a phase 4 historical retrospective referencing the original mistake — none currently exist, but be conservative). If grep finds any occurrence inside a quoted historical block or a frontmatter `name:` field, leave it; otherwise replace.
  </action>
  <verify>
    <automated>test "$(grep -ic 'RG2' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RG2" .planning/PROJECT.md` returns 0
    - `grep -c "RG2" .planning/REQUIREMENTS.md` returns 0
    - `grep -c "RG2" .planning/ROADMAP.md` returns 0
    - `grep -c "Robotiq Hand-E" .planning/PROJECT.md` returns >= 2
    - `grep -c "Robotiq Hand-E" .planning/REQUIREMENTS.md` returns >= 2
    - `grep -c "Robotiq Hand-E" .planning/ROADMAP.md` returns >= 4
    - PARITY-01 in `.planning/REQUIREMENTS.md` mentions "Robotiq Hand-E" by exact name
    - SCENE-03 in `.planning/REQUIREMENTS.md` mentions "Robotiq Hand-E gripper"
  </acceptance_criteria>
  <done>Zero "RG2" mentions remain in `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Replace RG2 → Robotiq Hand-E in CLAUDE.md and exts/aic-dt/docs/README.md</name>
  <files>CLAUDE.md, exts/aic-dt/docs/README.md</files>
  <read_first>
    - CLAUDE.md (current state — search for all "RG2" occurrences; per RESEARCH.md lines 22, 159, 224)
    - exts/aic-dt/docs/README.md (current state — RG2 in feature line 8 per RESEARCH.md line 782)
    - .planning/phases/01-foundation-parity/01-CONTEXT.md (D-03 minimum target list)
  </read_first>
  <action>
Apply the same RG2→Robotiq Hand-E replacement rules from Task 1 to:

**`CLAUDE.md`** (repo root) — lines around 22, 159, 224 (verify via grep):
- Robot description line ~22: "**Robot:** UR5e + RG2 + cable at (-0.18, -0.122, 0)" → "**Robot:** UR5e + Robotiq Hand-E + cable at (-0.18, -0.122, 0)"
- quick_start expected output ~159: any RG2 in the printed message → Robotiq Hand-E
- Asset table ~224: RG2 reference → Robotiq Hand-E
- Verified-working-flow output ~159 (the echoed `quick_start` message): "AIC scene with UR5e, wrist cameras, task board objects, and simulation running." — verify this DOES NOT contain RG2 (it currently doesn't, per inspection); but the surrounding context might. Re-verify via grep.

**`exts/aic-dt/docs/README.md`** — line ~8 feature line per RESEARCH.md:
- "RG2 Gripper" feature line → "Robotiq Hand-E Gripper"
- Any other RG2 references in the README → Robotiq Hand-E

Cable note in CLAUDE.md: the line "cable subtree wedges PhysX simulation post-play when active" mentions the cable workaround. After the gripper rename, verify this paragraph still reads coherently — Hand-E doesn't change the cable workaround logic, so leave the cable text as-is and just fix the gripper noun.

After edits, verify:
- `grep -c "RG2" CLAUDE.md` returns 0
- `grep -c "RG2" exts/aic-dt/docs/README.md` returns 0

Special handling for `CLAUDE.md`: this file ALSO is the user's repo on-ramp doc. Do NOT introduce structural changes to it — only the literal RG2→Hand-E corrections at the targeted lines. The full CLAUDE.md "topic table refresh" or "cable note expansion" mentioned in the parent PHASE planning context is **not in this task's scope** — those land in Plan 07 alongside the CHANGELOG entry.

Edge case for `exts/aic-dt/docs/README.md`: the README may also call the extension "ur5e-dt" in places (per DX-05). Per CONTEXT.md, "AIC scope mention (DX-05 partial)" is a Phase 1 nicety. **For this plan: do the RG2→Hand-E correction. DX-05's full README rewrite is Phase 4.** Leave any "ur5e-dt" framing untouched in this task.
  </action>
  <verify>
    <automated>test "$(grep -ic 'RG2' CLAUDE.md exts/aic-dt/docs/README.md 2>/dev/null | awk -F: '{sum+=$2} END {print sum}')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RG2" CLAUDE.md` returns 0
    - `grep -c "RG2" exts/aic-dt/docs/README.md` returns 0
    - `grep -c "Robotiq Hand-E" CLAUDE.md` returns >= 1
    - `grep -c "Robotiq Hand-E" exts/aic-dt/docs/README.md` returns >= 1
    - `CLAUDE.md` robot line still contains the literal `(-0.18, -0.122, 0)` and `Port:` and `8768` strings (regression check — only RG2 noun replaced, no structural changes)
    - The cable note paragraph in `CLAUDE.md` still contains `SetActive(False)` and `wedge` strings
  </acceptance_criteria>
  <done>Zero "RG2" mentions in CLAUDE.md and exts/aic-dt/docs/README.md; structural content otherwise unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc files | Plain-text Markdown edits. No code execution path. No external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Bulk find/replace risk — replacing 'RG2' too aggressively could touch unintended substrings (e.g., a path containing the literal "RG2" in retained sibling extension paths) | mitigate | Files are explicitly enumerated; sibling extensions and test files explicitly out of scope per D-03 minimum target list. Acceptance criteria runs grep with file scope limited to the 5 target files. |
| T-03-02 | Repudiation | Doc edits without commit traceability | mitigate | Each edit lands in a separate plan commit with clear message; CHANGELOG.md (Plan 07) records the correction date and reason. |
</threat_model>

<verification>
- `grep -rn "RG2" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md CLAUDE.md exts/aic-dt/docs/README.md` returns no matches
- All five target files contain `Robotiq Hand-E` at least once
- No structural sections of CLAUDE.md (paths, port numbers, command examples) modified
</verification>

<success_criteria>
1. Doc surface uniformly identifies the gripper as Robotiq Hand-E.
2. Code-side RG2→Hand-E corrections (in extension.py) deferred to Plan 04 (Wave 2).
3. CHANGELOG entry recording the correction lands in Plan 07.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-parity/01-03-SUMMARY.md` with:
- File-by-file count of replacements made
- Final grep result confirming zero RG2 across the 5 target files
- Any RG2 occurrences found in unexpected places that were left untouched (with rationale)
</output>
