---
plan: 07-05
phase: 7
name: Phase 6 regression gate - 3/3 consecutive clean passes
wave: 5
depends_on: [07-04]
files_modified:
  - .planning/phases/07-control-gui-service-audit-ik-cleanup/07-PHASE6-REGRESSION.md
autonomous: false
requirements: []
---

<objective>
Non-negotiable merge gate: re-run Phase 6's 3-consecutive-clean-passes drop motion verification for all three cups AFTER Plans 07-01 through 07-04 have landed. Any regression blocks Phase 7 from merging. Implements CONTEXT.md decision D-14.

**This plan produces no code changes.** Its single artifact is a PASS/FAIL log that proves (or disproves) that the cleanup preserved Phase 6's verified behavior.

**Why a separate plan:** Making the gate its own plan makes it non-negotiable, traceable in the phase manifest, and visible to downstream `/gsd-verify-phase`. Burying the gate inside Plan 07-04's acceptance criteria makes it easy to skip during revisions.
</objective>

<tasks>

<task id="05-01">
<title>Locate and understand the Phase 6 verification procedure</title>
<read_first>
- /home/aaugus11/Documents/isaac-sim-mcp/.planning/phases/06-collision-free-drop-motion-verification-loop/ (every file in the directory — the procedure lives here)
- /home/aaugus11/Documents/isaac-sim-mcp/.planning/STATE.md §"Accumulated Context" ([Phase 06] entry: "Verified 3/3 consecutive clean passes for all 3 cups (drop_0, drop_1, drop_2)")
</read_first>
<action>
1. List the Phase 6 directory: `ls /home/aaugus11/Documents/isaac-sim-mcp/.planning/phases/06-collision-free-drop-motion-verification-loop/`.

2. Read every `.md` file in that directory — CONTEXT.md, PLAN.md files, VERIFICATION.md (if present), RESEARCH.md (if present), SUMMARY.md (if present). Extract the exact verification procedure: what "one clean pass" means, which services are called in what order, what "clean" means (zero collisions, all cups have blocks inside, etc.), what success looks like.

3. Document the extracted procedure inline in this plan's `<procedure>` block below before proceeding to task 05-02. If no explicit procedure file exists, derive it from the Phase 6 PLAN.md task list and acceptance criteria.

4. Note any Phase-6-era environment assumptions that may have drifted (e.g., specific launch commands, parameter values, MoveIt config overrides). The regression re-run must match the Phase 6 environment as closely as possible — otherwise a failure is ambiguous (Phase 7 regression vs environmental drift).
</action>
<acceptance_criteria>
- The `<procedure>` block in this plan file (edit it in place) contains a numbered step list extracted from Phase 6's artifacts.
- The procedure names the specific services called (`~/grasp_home`, `~/drop_point`, `~/drop_sweep`, `~/drop_release`, or equivalents) and the order of operations.
- The procedure defines "clean" explicitly — the criteria that make a pass pass or fail.
</acceptance_criteria>
</task>

<task id="05-02">
<title>Execute 3/3 verification for all 3 cups</title>
<read_first>
- The `<procedure>` block written in task 05-01
- ~/Documents/isaac-sim-mcp/CLAUDE.md §Bring-up Sequence
- .planning/phases/06-collision-free-drop-motion-verification-loop/* (cross-reference during execution)
</read_first>
<action>
1. **Preflight.** Run the preflight check from CLAUDE.md. Ensure Isaac Sim is up with soarm101-dt, the ROS2 control stack is launched, and the scene is in a clean `quick_start` state. Record the commit SHA of `~/Projects/Exploring-VLAs/vla_SO-ARM101` (`git -C ~/Projects/Exploring-VLAs/vla_SO-ARM101 rev-parse HEAD`) — this identifies the post-Plan-04 state under test.

2. **Create the log file.** `touch /home/aaugus11/Documents/isaac-sim-mcp/.planning/phases/07-control-gui-service-audit-ik-cleanup/07-PHASE6-REGRESSION.md`. Write a header:
   ```markdown
   # Phase 7 Regression Gate — Phase 6 3/3 Re-run

   **Tested commit:** {SHA}
   **Date:** {ISO date}
   **Procedure source:** {path(s) from task 05-01}

   ## Runs
   ```

3. **Execute the procedure 3 times for each of drop_0, drop_1, drop_2** (9 total passes). For each pass:
   - Call the documented services in order.
   - Capture the control-stack log for the pass.
   - Determine PASS or FAIL per the "clean" criteria from task 05-01.
   - Write to the regression log:
     ```markdown
     ### Pass {N}/3 — drop_{color}
     Services: {list}
     Result: PASS | FAIL
     Notes: {any observations, collision warnings, etc.}
     Log excerpt:
     ```
     {relevant log lines}
     ```
     ```

4. **Stop immediately on first failure.** If any pass fails, write `## OVERALL: FAIL` and stop — do not run remaining passes. Include diagnostic details (which solver was invoked, what log line showed the failure) in the regression log.

5. **If all 9 passes succeed:** write `## OVERALL: PASS (9/9 clean)` at the end of the log. Phase 7 is cleared for merge.

6. **Do NOT modify `control_gui.py` during this task.** This plan is pure verification. If a failure surfaces a real regression, the correct response is: stop the plan, open a new diagnostic task in Plan 07-04, and re-run 07-05 after the fix.
</action>
<acceptance_criteria>
- File `/home/aaugus11/Documents/isaac-sim-mcp/.planning/phases/07-control-gui-service-audit-ik-cleanup/07-PHASE6-REGRESSION.md` exists.
- The file contains exactly the strings `## Runs` and either `## OVERALL: PASS (9/9 clean)` or `## OVERALL: FAIL`.
- If `## OVERALL: PASS`: the file has 9 run entries (3 per cup × 3 cups), each marked `Result: PASS`.
- The `Tested commit:` line matches the current HEAD of `~/Projects/Exploring-VLAs/vla_SO-ARM101`.
- Phase 7 is NOT declared complete (in STATE.md or elsewhere) until this file shows `OVERALL: PASS`.
</acceptance_criteria>
</task>

</tasks>

<procedure>
## Phase 6 Verification Procedure (filled in by task 05-01 before 05-02 runs)

*This block is a placeholder to be filled in by task 05-01 after reading the Phase 6 artifacts. Until filled, task 05-02 cannot start. Example shape:*

1. With scene in quick_start state and `/drop_poses` publishing, select `drop_0` in the Drop listbox.
2. Call `~/grasp_home` (or equivalent home-pose service).
3. Call `~/drop_point`. Wait for completion.
4. Call `~/drop_sweep`. Wait for completion.
5. Visually confirm (via RViz or MCP inspection) that no cup was knocked over, no collision was reported, and the arm is in a clean post-sweep configuration.
6. Call `~/drop_release`.
7. Call `~/grasp_home` to return.
8. Record the pass as PASS if all steps completed without collision warnings or timeout errors, FAIL otherwise.
9. Repeat for `drop_1` and `drop_2`. Each cup gets 3 consecutive passes.

*Task 05-01 overrides this placeholder with the actual Phase 6 procedure.*
</procedure>

<verification>
## Goal-Backward Verification

**Decision addressed:** D-14 from CONTEXT.md (Phase 6 3/3 regression as non-negotiable merge gate).

**Why 3/3 and not 1/1:** Phase 6's original success criterion was 3 consecutive clean passes per cup (9 total) — the 3x multiplier catches intermittent collision cases that a single-pass smoke test would miss. Phase 4's OMPL config (simplify_solutions=false, 50 planning attempts, 10% cylinder padding) was tuned specifically to make this gate reproducible.

**Non-negotiable means:** If Plan 07-05 shows `OVERALL: FAIL`, Phase 7 CANNOT merge. Plans 07-01 / 07-02 / 07-03 may be merge-independently, but Plan 07-04 specifically is reverted until the failure is root-caused.
</verification>

<must_haves>
- Phase 6 procedure extracted and documented in the plan's `<procedure>` block (05-01).
- 9 passes executed (3 cups × 3 passes) with PASS/FAIL recorded per pass (05-02).
- Final verdict written as `OVERALL: PASS (9/9 clean)` or `OVERALL: FAIL` in the regression log.
- Tested commit SHA captured so the verdict is reproducible.
- No code changes to `control_gui.py` within this plan — pure verification.
</must_haves>
