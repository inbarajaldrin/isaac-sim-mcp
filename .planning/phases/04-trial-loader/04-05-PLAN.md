---
phase: 04-trial-loader
plan: 05
type: execute
wave: 5
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified:
  - exts/aic-dt/docs/README.md
  - exts/aic-dt/docs/CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/04-trial-loader/04-SUMMARY.md
  - .planning/phases/04-trial-loader/04-05-SUMMARY.md
autonomous: true
requirements: [DX-05, TRIAL-01, TRIAL-02, TRIAL-03, TRIAL-04, TRIAL-05]
must_haves:
  truths:
    - "exts/aic-dt/docs/README.md is a near-total rewrite per Q7: AIC scope, MCP port 8768, cross-repo relationship, canonical launch sequence, quick_start + load_trial atom references"
    - "exts/aic-dt/docs/CHANGELOG.md gains a Phase 3 entry (currently absent — verified by 04-RESEARCH.md) AND a Phase 4 / M1 entry"
    - "REQUIREMENTS.md TRIAL-01..05 + DX-05 flip from [ ] to [x] (or remain [~] with explicit gap notes if 04-04 verdict was BLOCKED)"
    - "PARITY-07 flips from [~] to [x] iff 04-03 dry-run AND 04-04 parity report both showed insertion_event firing"
    - "ROADMAP.md Phase 4 progress table updates to reflect Plans 01..05 closed"
    - "STATE.md updates: phase-active → phase-complete OR milestone-complete; current_position set; last_activity logged"
    - "If 04-04 verdict was READY: .planning/.m1_shipped is touched per CONTEXT.md D-10"
    - "Phase 4 closes with 04-SUMMARY.md aggregating all 5 plan outcomes"
  artifacts:
    - path: "exts/aic-dt/docs/README.md"
      provides: "AIC-scoped extension README (replacing ur5e-dt scaffold)"
    - path: "exts/aic-dt/docs/CHANGELOG.md"
      provides: "Phase 3 + Phase 4 / M1 release entries"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status flips for Phase 4 requirement IDs + traceability table"
    - path: ".planning/phases/04-trial-loader/04-SUMMARY.md"
      provides: "Phase-level summary aggregating all 5 plans"
  key_links:
    - from: "REQUIREMENTS.md status flips"
      to: ".planning/phases/04-trial-loader/04-04-SUMMARY.md M1 ship-gate verdict"
      via: "Read verdict; flip to [x] if READY, leave [~] with gap note if BLOCKED"
      pattern: "M1 ship-gate verdict"
    - from: "STATE.md milestone status"
      to: ".planning/.m1_shipped marker file"
      via: "touch .planning/.m1_shipped iff verdict is READY"
      pattern: ".m1_shipped"
---

<objective>
Phase 4 closure: ship DX-05 (README rewrite + CHANGELOG M1 entry), flip
REQUIREMENTS.md status for all Phase-4 requirement IDs based on the actual
04-04 verdict, update ROADMAP + STATE, author the phase-level 04-SUMMARY.md,
and (if M1 ship-gate verdict is READY) `touch .planning/.m1_shipped`.

Purpose: D-10 + D-11 implementation. This plan is paperwork-heavy
(documentation + status flips) — no new code, no Isaac Sim runs. The M1 ship
moment is recorded here.

Output: README rewrite, CHANGELOG x2 entries, REQUIREMENTS flips, ROADMAP +
STATE updates, two SUMMARYs (this plan's + the phase-level one),
optional .m1_shipped marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/04-trial-loader/04-CONTEXT.md
@.planning/phases/04-trial-loader/04-RESEARCH.md
@.planning/phases/04-trial-loader/04-01-SUMMARY.md
@.planning/phases/04-trial-loader/04-02-SUMMARY.md
@.planning/phases/04-trial-loader/04-03-SUMMARY.md
@.planning/phases/04-trial-loader/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite exts/aic-dt/docs/README.md (DX-05)</name>
  <files>exts/aic-dt/docs/README.md</files>
  <read_first>
    - exts/aic-dt/docs/README.md (current state — confirms ur5e-dt scaffold + port 8766 wrong reference)
    - .planning/phases/04-trial-loader/04-RESEARCH.md (Q7 — full enumeration of the 8 sections required for the rewrite)
    - exts/aic-dt/docs/topic-parity-reference.md (Phase 1 cross-phase audit table — README should reference)
    - exts/aic-dt/docs/cable-physics-strategy.md (Phase 3 SCENE-05 doc — README should reference)
    - CLAUDE.md (the canonical launch sequence + ROS_DOMAIN_ID=7 + cross-repo relationship + cache discipline — README mirrors these in extension-local form)
  </read_first>
  <action>
Full rewrite of `exts/aic-dt/docs/README.md`. Read the existing file once to confirm scope; do NOT preserve the structure (token-swap is insufficient per 04-RESEARCH.md Q7).

Sections in order:

1. **Title:** `# aic-dt Extension`

2. **One-paragraph intro:** the AIC cable-insertion digital twin. UR5e + Robotiq Hand-E + 3 wrist cameras + cable + AIC enclosure + task board. Sim-side companion to `~/Documents/aic` (the ROS-side / engine / policies / controller). MCP socket at port **8768**.

3. **`## Architectural law (read before changing topics)`:**
   - Topic parity is the architectural law — Isaac Sim publishes the EXACT topic names Gazebo's `aic_eval` publishes. No `_sim`/`_real`, no remap nodes, no bridge translators.
   - `~/Documents/aic` is read-only. The platform-transfer compatibility burden lives entirely on the Isaac Sim side.
   - `omni.*` imports are forbidden in `~/Documents/aic`. `rclpy` IS allowed inside `aic-dt` for non-control glue.
   - ROS_DOMAIN_ID=7 for sim isolation (UR5e real driver lives on default domain 0).

4. **`## Launching`:**
   - Lifecycle helper (warm cache):
     ```
     bash -c 'source ~/env_isaaclab/bin/activate && ROS_DOMAIN_ID=7 DISPLAY=${DISPLAY:-:0} \
        bash ~/.claude/skills/isaac-sim-extension-dev/scripts/isaacsim_launch.sh launch aic-dt'
     ```
   - Postload script (cold cache or after wipe):
     ```
     ~/env_isaaclab/bin/isaacsim --exec /home/aaugus11/Documents/isaac-sim-mcp/exts/aic-dt/scripts/launch_postload.py
     ```
   - Cache discipline: `prime_usd_cache.py status` before debugging hangs; `restore known-good` is the recovery action.

5. **`## MCP atomic operations`:**
   Brief table of the headline atoms (all 30+ are documented in `MCP_TOOL_REGISTRY` at top of `aic_dt/extension.py`):
   - `quick_start` — canonical scene-population chain (load → spawn → publishers → play). `ground_truth=True/False` (Phase 4 D-04).
   - `load_trial(config_path, trial_key, ground_truth)` — TRIAL-01: spawn the matching scene from `~/Documents/aic/aic_engine/config/sample_config.yaml`. Phase 4.
   - `load_robot`, per-component `spawn_*` atoms — Phase 1 SCENE-01/04.
   - `attach_cable_to_gripper` — Phase 3 SCENE-03.

   Reference: see `MCP_TOOL_REGISTRY` at top of `aic_dt/extension.py` for the complete atom inventory.

6. **`## Environment variables`:**
   | Var | Default | Purpose |
   |---|---|---|
   | `DISPLAY` | `:0` | X11 for Kit GUI. Required. |
   | `MCP_SERVER_PORT` | `8768` | Hardcoded constant in extension.py — change there, not via env. |
   | `MCP_CLIENT_OUTPUT_DIR` | unset | Writes resources / scene state under `$DIR/resources/` if set. |
   | `ROS_DOMAIN_ID` | `7` | MUST be 7 to isolate from real UR5e driver (domain 0). |
   | `AIC_DT_EXT_FOLDER` | `/home/.../isaac-sim-mcp/exts` | Override hardcoded ext folder in launch_postload.py. |
   | `SCENE_05_DISABLE` | unset | Phase 3 emergency rollback — keeps cable SetActive(False). |

7. **`## Cross-repo relationship`:**
   This repo is sim-side only. The matching ROS-side repo lives at `~/Documents/aic`:
   - `aic_controller/` — C++ ROS 2 impedance controller (consumes /joint_states, /tf, /wrench).
   - `aic_engine/` — C++ trial orchestrator. `config/sample_config.yaml` defines the trials.
   - `aic_example_policies/` — CheatCode + 6 reference policies.
   - `aic_description/`, `aic_assets/` — URDFs + meshes.

   The same `aic_controller` + `aic_engine` + `CheatCode.py` invocations that work against Gazebo MUST work against this Isaac Sim digital twin with zero changes in `~/Documents/aic`.

8. **`## End-to-end verification (Phase 4 / M1)`:**
   Wrapper script: `exts/aic-dt/scripts/run_aic_engine_against_isaac_sim.sh <trial_key>` — brings up Isaac Sim + load_trial + my-eval-isaac + my-solution containers (engine + CheatCode), tears down on exit.

   Parity report: `exts/aic-dt/scripts/parity_report.py` — runs all 3 trials × {Isaac Sim, Gazebo} sequentially, captures `/scoring/insertion_event` + `/aic/gazebo/contacts/off_limit`, emits `parity-report.{json,md}` to `.planning/phases/04-trial-loader/`. Exit 0 iff all trials match — the M1 ship gate.

9. **`## Further reading`:**
   - `topic-parity-reference.md` — cross-phase parity audit (PARITY-12).
   - `cable-physics-strategy.md` — SCENE-05 design rationale.
   - `aic-msgs-setup.md` — Python 3.11 humble workspace build.
   - `CHANGELOG.md` — release history per phase.
   - `~/Documents/isaac-sim-mcp/CLAUDE.md` — repo-root on-ramp for fresh Claude sessions; autonomous-mode policies.

10. **`## License + attribution`:** preserve any license stanza from the existing README. If absent, omit (don't fabricate).

CRITICAL: NO mention of `ur5e-dt`, NO port `8766`. Preserve any project-specific license/attribution footer that was in the original.
  </action>
  <verify>
    <automated>test -f exts/aic-dt/docs/README.md && grep -q "aic-dt Extension" exts/aic-dt/docs/README.md && [ "$(grep -c 'ur5e-dt' exts/aic-dt/docs/README.md)" = "0" ] && [ "$(grep -c '8766' exts/aic-dt/docs/README.md)" = "0" ] && grep -q "8768" exts/aic-dt/docs/README.md && grep -q "load_trial" exts/aic-dt/docs/README.md && grep -q "ROS_DOMAIN_ID" exts/aic-dt/docs/README.md && grep -q "Cross-repo" exts/aic-dt/docs/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md begins with `# aic-dt Extension` (not `# ur5e-dt Extension`).
    - `grep -c 'ur5e-dt' exts/aic-dt/docs/README.md` returns 0.
    - `grep -c '8766' exts/aic-dt/docs/README.md` returns 0 (the wrong port reference removed).
    - Contains the literal `8768` (correct port) at least once.
    - Contains `load_trial` (Phase 4 atom reference).
    - Contains `ROS_DOMAIN_ID` (architectural-law section).
    - Contains `Cross-repo` (the relationship section per Q7 item 3).
    - Contains references to `topic-parity-reference.md` and `CHANGELOG.md`.
    - Sections 1-9 are present (8 minimum per Q7; License optional).
  </acceptance_criteria>
  <done>DX-05 README delivered; legacy ur5e-dt + port 8766 fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Append Phase 3 + Phase 4 / M1 entries to CHANGELOG.md</name>
  <files>exts/aic-dt/docs/CHANGELOG.md</files>
  <read_first>
    - exts/aic-dt/docs/CHANGELOG.md (current state — verify Phase 3 entry is missing per 04-RESEARCH.md)
    - .planning/phases/03-cable-physics/03-SUMMARY.md (Phase 3 deliverables source-of-truth for Phase-3 entry)
    - .planning/phases/04-trial-loader/04-04-SUMMARY.md (M1 ship-gate verdict — drives the M1 entry framing)
    - .planning/phases/04-trial-loader/04-01-SUMMARY.md ... 04-03-SUMMARY.md (per-plan deliverables)
  </read_first>
  <action>
Append two new entries at the TOP of the CHANGELOG (newest first, per the existing pattern). Read existing file to confirm exact section style (e.g. `## [Phase X — Title]` vs `## Phase X` etc) and mirror exactly.

ENTRY 1 — Phase 3 (currently absent per 04-RESEARCH.md):
```markdown
## [Phase 3 — Cable Physics & Ground-Truth Pose] — 2026-05-05

Cable physics strategy chosen + implemented (articulated rigid-body chain
per NVIDIA RigidBodyRopeDemo template); cable subtree flipped from
SetActive(False) to True; ground-truth scoring publishers added.

Requirement IDs delivered: SCENE-02, SCENE-03, SCENE-05, SCENE-06, PARITY-08.
PARITY-07 partial — code complete; live-fire deferred to Phase 4 trial cycle.

### Added
- exts/aic-dt/aic_dt/scoring_publishers.py (`/scoring/tf` + `/objects_poses_real` + `/scoring/insertion_event`)
- attach_cable_to_gripper MCP atom (4-surface DX-02)
- load_robot kwargs: cable_type, attach_cable_to_gripper, gripper_initial_pos
- exts/aic-dt/docs/cable-physics-strategy.md (SCENE-05 design rationale)

### Changed
- exts/aic-dt/assets/robot/aic_unified_robot_cable_sdf.usd (in-place: per-link MassAPI density + per-joint DriveAPI on rotY/rotZ)
- extension.py::load_robot now SetActive(cable=True) by default; SCENE_05_DISABLE=1 for emergency rollback
```

ENTRY 2 — Phase 4 / M1:
```markdown
## [Phase 4 — M1 Ship: Trial Loader & End-to-End Parity] — 2026-05-XX

[<READY|BLOCKED> per .planning/phases/04-trial-loader/04-04-SUMMARY.md M1 ship-gate verdict]

Milestone 1 (Platform Transfer) ships. Every trial in
~/Documents/aic/aic_engine/config/sample_config.yaml runs end-to-end under
aic_example_policies.ros.CheatCode against this Isaac Sim digital twin
and produces the same per-trial pass/fail outcome that Gazebo produces.

Requirement IDs delivered: TRIAL-01, TRIAL-02, TRIAL-03, TRIAL-04, TRIAL-05,
DX-05, PARITY-07 [iff insertion_event live-fire confirmed].

### Added
- load_trial MCP atom (TRIAL-01/02): single atom dispatches sample_config.yaml trials to existing Phase 1-3 spawn atoms
- ground_truth kwarg on quick_start + load_trial: gates _start_aic_scoring_publishers (M2 swap surface)
- exts/aic-dt/docker/my-eval-isaac/{Dockerfile, entrypoint.sh, build.sh}: derived image with engine-only entrypoint (skips Gazebo per 04-RESEARCH.md Q4 Option 4B)
- exts/aic-dt/scripts/run_aic_engine_against_isaac_sim.sh: TRIAL-03 wrapper
- exts/aic-dt/scripts/parity_report.py: TRIAL-04+05 canonical M1 ship-gate verifier
- exts/aic-dt/scripts/verify_kilted_humble_interop.sh, probe_taskboard_prim_paths.py: pre-flight risk de-risking

### Changed
- exts/aic-dt/docs/README.md: full rewrite — removed ur5e-dt framing, reflects AIC scope
- audit_dx02.py PRESENT_ATOMS: extended for load_trial [+ set_port_link_paths if D-13 fallback applied]

### Carry-forwards beyond M1
- M2 pose-source swap (POSE-01..03): `_start_aic_scoring_publishers` is the seam
- Streaming wrapper (~/bin/isaacsim) fix for aic-dt (DX task)
- parity_publishers hot-reload regression (Phase 3 known limitation)
- [If PARITY-07 still [~]:] live insertion_event firing root cause
```

Read 04-04-SUMMARY.md verdict and replace `<READY|BLOCKED>` and the conditional `[iff insertion_event live-fire confirmed]` with the actual verdict from disk.

The actual ship date in the title `2026-05-XX` is `2026-05-05` (per the system context).
  </action>
  <verify>
    <automated>test -f exts/aic-dt/docs/CHANGELOG.md && grep -q "Phase 3 — Cable Physics" exts/aic-dt/docs/CHANGELOG.md && grep -q "Phase 4 — M1 Ship" exts/aic-dt/docs/CHANGELOG.md && grep -q "load_trial" exts/aic-dt/docs/CHANGELOG.md && grep -q "TRIAL-01" exts/aic-dt/docs/CHANGELOG.md && grep -q "DX-05" exts/aic-dt/docs/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md has a Phase 3 section (newly added — was absent).
    - CHANGELOG.md has a Phase 4 / M1 section.
    - Phase 4 entry contains literal `TRIAL-01`, `TRIAL-02`, `TRIAL-03`, `TRIAL-04`, `TRIAL-05`, `DX-05`.
    - Phase 4 entry contains `load_trial` and `parity_report.py`.
    - Phase 4 entry contains a verdict marker (READY or BLOCKED) — copied from 04-04-SUMMARY.md.
    - Newest-first ordering preserved (Phase 4 above Phase 3 above Phase 2 above Phase 1).
  </acceptance_criteria>
  <done>CHANGELOG ships M1; Phase 3 entry retroactively added.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md flips + ROADMAP.md progress + STATE.md milestone update</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state of TRIAL-01..05 + DX-05 + PARITY-07 rows)
    - .planning/ROADMAP.md (Phase 4 progress row + Plans list)
    - .planning/STATE.md (current milestone status + position)
    - .planning/phases/04-trial-loader/04-04-SUMMARY.md (M1 ship-gate verdict drives flip decisions)
    - .planning/phases/04-trial-loader/04-03-SUMMARY.md (PARITY-07 status from dry-run)
  </read_first>
  <action>
### REQUIREMENTS.md

Read 04-04-SUMMARY.md verdict. Determine action per requirement:

- **TRIAL-01:** flip `[ ]` → `[x]` if `load_trial` was successfully exercised in 04-03/04-04 (any trial spawn worked). Closure citation: "Plans 04-02 + 04-03 + 04-04 — load_trial MCP atom + dispatcher + verified live via parity_report".
- **TRIAL-02:** flip `[ ]` → `[x]` if ground_truth flag was tested. Closure: "Plan 04-02 — ground_truth kwarg on load_trial + quick_start; verified via [04-03 dry-run / 04-04 parity report]".
- **TRIAL-03:** flip `[ ]` → `[x]` if `aic_engine` ran successfully against Isaac Sim at least once. Closure: "Plans 04-03 (my-eval-isaac:v1 + run_aic_engine_against_isaac_sim.sh) + 04-04 (parity_report exercised the wrapper across all trials)".
- **TRIAL-04:** flip `[ ]` → `[x]` ONLY if 04-04 verdict is READY (zero mismatches). If BLOCKED, leave `[ ]` with explicit gap note: `_(BLOCKED — N mismatches; see .planning/phases/04-trial-loader/04-04-SUMMARY.md for verdict + parity-report.md for per-trial detail)_`.
- **TRIAL-05:** flip `[ ]` → `[x]`. The script exists and is re-runnable regardless of verdict — that's the requirement. Closure: "Plan 04-04 — parity_report.py + parity-report.{json,md} emitted to .planning/phases/04-trial-loader/".
- **DX-05:** flip `[ ]` → `[x]`. Closure: "Plan 04-05 — README rewritten + CHANGELOG Phase 3 + Phase 4 / M1 entries".
- **PARITY-07:** flip `[~]` → `[x]` ONLY if 04-03 dry-run captured insertion_event firing AND 04-04 confirmed across other trials. If insertion_event never fired in any session, leave `[~]` with closure citation explicitly noting the deferral path: `_(code complete; live-fire deferred to first post-M1 fix-loop session — see 04-04-SUMMARY.md PARITY-07 status)_`.

Update the **Traceability table** at the bottom of REQUIREMENTS.md — same rows, same closure citations.

### ROADMAP.md

- Update Phase 4 row in the progress table from `0/TBD` → `5/5`. Status: `Closed (M1 shipped)` if verdict is READY else `Blocked — see 04-04-SUMMARY.md`.
- Update the `Plans:` list under "### Phase 4: Trial Loader & End-to-End Verification": flip placeholder `**Plans**: TBD` to `**Plans**: 5 plans (sequential waves 1→5)` and enumerate:
  ```
  Plans:
  - [x] 04-01-PLAN.md — Pre-flight risk de-risking (A2 + A4 probes)
  - [x] 04-02-PLAN.md — load_trial MCP atom + ground_truth flag (TRIAL-01/02)
  - [x] 04-03-PLAN.md — my-eval-isaac:v1 Docker image + E2E wrapper + trial_1 dry-run (TRIAL-03 + PARITY-07)
  - [x] 04-04-PLAN.md — parity_report.py + first M1 ship-gate exercise (TRIAL-04/05)
  - [x] 04-05-PLAN.md — DX-05 README + CHANGELOG + REQUIREMENTS flips + phase closure
  ```
- Update the top-level Overview "Phases" list: flip `[ ] **Phase 4: ...**` to `[x]` if M1 shipped, else leave `[ ]` with annotation.

### STATE.md

Read current STATE.md, then:

- Update `stopped_at` field with a fresh entry: e.g. `Phase 4 closed at <verdict>; ROADMAP + REQUIREMENTS flipped; SUMMARY committed; .m1_shipped <touched|withheld>`.
- Update `last_activity` with current timestamp.
- Update `## Current Position`: Phase 4 status → Closed; M1 percentage progress.
- Update `progress` block: `completed_phases: 3` → `4` (or whatever logic the field follows); `percent: 50` → adjust upward.
- If verdict was READY: append a new line under `## Session Continuity` documenting M1 ship.
- Add an entry under `## Phase 4 Plans` mirror of the existing `## Phase 2 Plans` table — 5 plans with their objectives + requirement IDs.

DO NOT touch the autonomous-mode block in CLAUDE.md or the .autonomous_m1_active marker — those are user-managed per CONTEXT D-10.
  </action>
  <verify>
    <automated>grep -q "TRIAL-05.*\[x\]\|TRIAL-05.*Closed" .planning/REQUIREMENTS.md && grep -q "DX-05.*\[x\]\|DX-05.*Closed" .planning/REQUIREMENTS.md && grep -q "04-01-PLAN.md" .planning/ROADMAP.md && grep -q "04-05-PLAN.md" .planning/ROADMAP.md && grep -q "Phase 4 Plans\|04-trial-loader" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md: TRIAL-01, TRIAL-02, TRIAL-03, TRIAL-05, DX-05 all marked [x] with closure citations.
    - REQUIREMENTS.md: TRIAL-04 marked [x] iff 04-04 verdict was READY; else [ ] with gap citation.
    - REQUIREMENTS.md: PARITY-07 marked [x] iff insertion_event live-fired; else [~] with deferral citation.
    - REQUIREMENTS.md traceability table reflects same status changes.
    - ROADMAP.md Phase 4 progress table shows `5/5` plans complete.
    - ROADMAP.md Phase 4 Plans list enumerates 04-01 through 04-05 with [x].
    - STATE.md `last_activity` updated with current date and Phase-4-closure entry.
    - STATE.md has a Phase 4 plans section (mirror of existing Phase 2 plans table).
    - STATE.md `progress.completed_phases` is incremented appropriately.
  </acceptance_criteria>
  <done>Project status surface reflects Phase 4 closure (M1 shipped or M1 blocked, accurately).</done>
</task>

<task type="auto">
  <name>Task 4: Author 04-SUMMARY.md (phase-level) + 04-05-SUMMARY.md (this plan) + conditional .m1_shipped + commit</name>
  <files>.planning/phases/04-trial-loader/04-SUMMARY.md, .planning/phases/04-trial-loader/04-05-SUMMARY.md, .planning/.m1_shipped</files>
  <read_first>
    - .planning/phases/02-controller-loop/02-SUMMARY.md (template for phase-level summary voice)
    - .planning/phases/03-cable-physics/03-SUMMARY.md (more recent phase-level template)
    - .planning/phases/04-trial-loader/04-01-SUMMARY.md ... 04-04-SUMMARY.md (the 4 inputs to the aggregation)
    - .planning/phases/04-trial-loader/04-CONTEXT.md (the 13 D-decisions to verify each was honoured)
    - CLAUDE.md (D-10 .m1_shipped marker contract)
  </read_first>
  <action>
### Sub-deliverable A: 04-SUMMARY.md (phase-level aggregation)

Mirror 03-SUMMARY.md voice + structure. Sections:

1. `# Phase 4 — Trial Loader & End-to-End Verification — SUMMARY`
2. `**Closed:** 2026-05-05 under autonomous M1 mode`
3. `**Status:** <READY — M1 shipped | BLOCKED — N mismatches; see parity-report.md>` per 04-04 verdict.
4. `## Phase goal achievement` — table of REQUIREMENTS rows × status × closure plan reference (mirror Phase 3 SUMMARY format).
5. `## Per-plan trail` — one paragraph per plan summarizing key deliverable + key finding/deviation:
   - 04-01: pre-flight A2/A4 probes; verdicts captured.
   - 04-02: load_trial atom + ground_truth flag; 4-surface DX-02 contract.
   - 04-03: my-eval-isaac:v1 + run_aic_engine_against_isaac_sim.sh; trial_1 dry-run; D-13 fallback (yes/no).
   - 04-04: parity_report.py exercised; M1 ship-gate verdict.
   - 04-05: paperwork — README rewrite, CHANGELOG, REQUIREMENTS flips.
6. `## D-decisions honoured` — verify each of 04-CONTEXT.md's D-01..D-13 was implemented as locked. Flag any deviations explicitly.
7. `## Architectural decisions worth carrying forward`
   - D-06 superseding (research found read-only constraint; built derived Docker image instead).
   - D-13 fallback path: outcome (whether actually invoked).
   - PARITY-07 first live-fire moment (or its deferral).
   - Topic-parity discipline tested end-to-end in this phase for the first time.
8. `## Carry-forwards beyond M1`
   - M2 pose-source swap (the `_start_aic_scoring_publishers` seam).
   - Per-trial UI buttons (deferred per CONTEXT.md).
   - Streaming wrapper fix.
   - parity_publishers hot-reload regression.
   - [If PARITY-07 still [~]:] root-cause investigation.
   - [If TRIAL-04 still [ ]:] specific mismatch debug — this is the post-M1 priority.
9. `## Metrics`
   - Plans: 5; commits: 5; average plan duration; total Phase 4 wall time (compare to Phases 1-3 from STATE.md).

### Sub-deliverable B: 04-05-SUMMARY.md (this plan's summary)

Standard plan-summary structure:
1. `# Phase 4 Plan 05 — Phase 4 closure paperwork — SUMMARY`
2. `## Objective` recap.
3. `## Deliverables` — README rewrite, CHANGELOG x2 entries, REQUIREMENTS flips, ROADMAP + STATE updates, 04-SUMMARY, this plan's SUMMARY, [.m1_shipped marker if verdict READY].
4. `## REQUIREMENTS flips applied` — exact table of which IDs flipped from what to what, with rationale.
5. `## M1 ship-gate decision` — READY → .m1_shipped touched; BLOCKED → marker withheld + post-M1 priority list documented.
6. `## Issues Encountered`.
7. `## Phase 4 close.`

### Sub-deliverable C: .m1_shipped marker (CONDITIONAL)

If 04-04-SUMMARY.md verdict is READY (zero mismatches across all 3 trials):
```bash
touch .planning/.m1_shipped
```

If verdict is BLOCKED: do NOT touch the marker. Document in 04-05-SUMMARY.md why it was withheld + the exact remediation path that would unblock it.

CLAUDE.md autonomous-mode block remains untouched per CONTEXT.md D-10 ("autonomous mode block at the top of CLAUDE.md remains in place as a historical artifact — the .autonomous_m1_active marker can be removed manually post-ship"). Do NOT delete .planning/.autonomous_m1_active either — that is the user's call.

### Final commit

`git add` ALL files modified by this plan: README.md, CHANGELOG.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, 04-SUMMARY.md, 04-05-SUMMARY.md, [.m1_shipped if created].

Commit message:
```
exec(04-05): Phase 4 closure — DX-05 + REQUIREMENTS flips + M1 ship-gate

DX-05: README rewrite (no ur5e-dt) + CHANGELOG Phase 3 + M1 entries.
REQUIREMENTS.md: TRIAL-01..05 + DX-05 flipped per 04-04 verdict.
M1 ship-gate: <READY → .m1_shipped touched | BLOCKED — N mismatches>.

Phase 4 closes; <M1 ships | M1 ship blocked pending fix per 04-SUMMARY.md>.
```

NO Co-Authored-By. Single atomic commit.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-trial-loader/04-SUMMARY.md && test -f .planning/phases/04-trial-loader/04-05-SUMMARY.md && grep -q "Phase 4" .planning/phases/04-trial-loader/04-SUMMARY.md && grep -q "Per-plan trail" .planning/phases/04-trial-loader/04-SUMMARY.md && grep -q "D-decisions honoured" .planning/phases/04-trial-loader/04-SUMMARY.md && grep -q "Carry-forwards beyond M1" .planning/phases/04-trial-loader/04-SUMMARY.md && git log -1 --pretty=format:%s | grep -q "exec(04-05)"</automated>
  </verify>
  <acceptance_criteria>
    - `04-SUMMARY.md` exists with all 9 sections including the per-plan trail and D-decision audit.
    - `04-05-SUMMARY.md` exists with all 7 sections including the M1 ship-gate decision.
    - 04-SUMMARY.md "## Phase goal achievement" table covers all 6 Phase 4 requirement IDs (TRIAL-01..05 + DX-05) plus PARITY-07 carry-forward.
    - 04-SUMMARY.md "## D-decisions honoured" enumerates all 13 D-decisions from CONTEXT.md by ID with status (honoured / superseded / deferred).
    - If verdict was READY: `.planning/.m1_shipped` exists (`test -f` passes).
    - If verdict was BLOCKED: `.m1_shipped` does NOT exist; SUMMARY explicitly documents the blocker.
    - Commit subject matches `^exec(04-05):`.
    - Commit body has no Co-Authored-By.
    - `git log --oneline | head -5` shows the 5 Phase 4 commits in order: 04-01, 04-02, 04-03, 04-04, 04-05 (atomic-commit-discipline confirmation).
  </acceptance_criteria>
  <done>Phase 4 closed; M1 paperwork complete; ship marker accurately reflects verdict.</done>
</task>

</tasks>

<verification>
- 4 tasks complete and committed atomically (1 commit total — paperwork is one atomic unit).
- Phase 4 has 5 commits across 5 plans (04-01 through 04-05).
- README.md no longer contains `ur5e-dt` or `8766`.
- CHANGELOG.md has Phase 3 + Phase 4 entries.
- REQUIREMENTS.md TRIAL-01..05 + DX-05 status reflects 04-04 verdict.
- STATE.md milestone status reflects Phase 4 closure.
- .m1_shipped marker presence matches verdict.
</verification>

<success_criteria>
- [ ] README.md rewritten — 0 ur5e-dt mentions, 0 port-8766 mentions, has port 8768 + load_trial + ROS_DOMAIN_ID + Cross-repo sections
- [ ] CHANGELOG.md has Phase 3 + Phase 4 entries (newest-first); Phase 4 references TRIAL-01..05 + DX-05 + parity_report.py
- [ ] REQUIREMENTS.md flips applied per 04-04 verdict (READY → all [x]; BLOCKED → TRIAL-04 stays [ ] with gap note)
- [ ] PARITY-07 [~] → [x] iff live-fire confirmed; else stays [~] with explicit deferral citation
- [ ] ROADMAP.md Phase 4 row shows 5/5 plans complete
- [ ] STATE.md reflects phase closure (last_activity, progress, current position, Phase 4 plans table)
- [ ] 04-SUMMARY.md aggregates all 5 plans with D-decision audit + carry-forwards
- [ ] 04-05-SUMMARY.md documents this plan's deliverables
- [ ] If READY: `.planning/.m1_shipped` exists
- [ ] If BLOCKED: SUMMARY enumerates remediation path
- [ ] Single atomic `exec(04-05): ...` commit with no Co-Authored-By
</success_criteria>

<output>
After completion, create:
- `.planning/phases/04-trial-loader/04-SUMMARY.md` (Task 4 sub-A)
- `.planning/phases/04-trial-loader/04-05-SUMMARY.md` (Task 4 sub-B)
- Conditionally: `.planning/.m1_shipped` (Task 4 sub-C iff verdict is READY)
</output>
