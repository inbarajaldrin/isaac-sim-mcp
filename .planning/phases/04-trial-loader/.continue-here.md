---
context: phase
phase: 04-trial-loader
task: 4
total_tasks: 5
status: paused_user
last_updated: 2026-05-07T12:19:44.523Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure across iter-1 through iter-7 of Plan 04-03.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **kilted ↔ humble fastrtps type-hash incompatibility for stock ROS messages** — verified via iter-4 (4 dry-run iterations + env permutations: FASTDDS_BUILTIN_TRANSPORTS=UDPv4, --ipc=host, ROS_LOCALHOST_ONLY toggle, direct rclpy bypass). Same RMW + same domain + --net=host: container subscribes to /clock + /joint_states from Isaac Sim's humble bridge → receives ZERO messages over 12s. `ros2 topic info` reports `Topic type hash: INVALID` for cross-distro stock messages. **Structural mitigation:** host-build the AIC ROS packages against humble (this was done for aic_engine + aic_adapter in commit 7943536). Future cross-distro probes MUST subscribe to a STOCK message type from the OTHER end's distro, not just verify same-workspace custom messages.

- [ ] CONSTRAINT: **A2 verdict is "PASS for custom messages only"** — Plan 04-01 A2 probe verified `aic_control_interfaces/JointMotionUpdate` (custom, matched ABI by same-workspace build) and humble-side host subscriber. It did NOT verify cross-distro stock message decode. **Structural mitigation:** new probes added to verify_kilted_humble_interop.sh must include a stock message subscribe (sensor_msgs/JointState or rosgraph_msgs/Clock) from inside the kilted container against humble's publisher. The pre-flight gate failed to catch this in Wave 1.

- [ ] CONSTRAINT: **AIC competition stack uses zenoh transport for model↔eval lifecycle service discovery** — `~/Documents/aic/scripts/run_cheatcode.sh:47-49` sets `AIC_ROUTER_ADDR=localhost:7447` + `RMW_IMPLEMENTATION=rmw_zenoh_cpp` for the participant-policy container. Host humble fastrtps engine + kilted zenoh model = no /aic_model/get_state discovery. **Structural mitigation:** model side must either run on the same transport (zenohd on host, or model rebuilt humble-native), OR be replaced with a stub for plumbing verification. Choice gates the M1 ship.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Custom-message-only RMW probe | Probing cross-distro RMW interop using only same-workspace custom messages — pass verdict masks stock-message type-hash incompat | blocking | verify_kilted_humble_interop.sh must subscribe to a stock message type (sensor_msgs/JointState, rosgraph_msgs/Clock) from inside the OTHER distro's container against the host's publisher; both subscriber and publisher distros must be tested, not just one direction |
| Trusting `ros2 topic info` for cross-distro flow | `topic info` reports publisher count=1 RELIABLE even when type-hash mismatch causes 0-message delivery; treating "info shows publisher" as "messages will flow" hides the real failure | blocking | All cross-distro RMW probes MUST attempt actual message decode with a 10-30s timeout, not just rely on topic-info enumeration |
| Premature derived-image build | Plan 04-03 originally built my-eval-isaac:v1 derived image based on RESEARCH D-06 supersession before Wave 1 verified cross-distro stock-message flow; ~30min wasted before iter-4 surfaced the real problem | advisory | Future research-finding-driven Docker work must explicitly call out which Wave 1 probe verified the assumption that drives the build |
| Iter-1..3 env-permutation sweep | Spent 3 dry-run iterations toggling FASTDDS_BUILTIN_TRANSPORTS, --ipc=host, ROS_LOCALHOST_ONLY before recognizing the type-hash issue; should have read fastrtps Topic type hash error first | advisory | If `ros2 topic info --verbose` shows `Topic type hash: INVALID`, jump immediately to cross-distro investigation — don't sweep transport/SHM permutations |
| Architectural-law reflexive rejection of bridges | Initial planning rejected option (c) domain_bridge as "no bridges per parity law" without distinguishing topic-name parity from DDS-domain bridging | advisory | The "no bridges" rule prohibits topic-name remapping/translation, NOT DDS-domain bridging. Re-evaluate option (c) and option (a) zenoh-fastdds bridge in this light if (b) and (c) stub fail |

<current_state>
Phase 4 is 60% closed. Plans 04-01 (pre-flight probes) and 04-02 (load_trial atom + ground_truth flag) are fully done with SUMMARY.md committed. Plan 04-03 has all deliverables landed across 4 atomic commits — the iter-4 fastrtps RMW interop hard blocker was RESOLVED via host-build pivot (aic_engine + aic_adapter compiled into ~/aic_humble_ws/install/ against /opt/ros/humble; iter-7 dry-run reaches "Engine Stopped" with full lifecycle). However, a new downstream blocker surfaced during iter-7: `my-solution:v1` (the CheatCode policy container) requires zenoh transport (`AIC_ROUTER_ADDR=localhost:7447`) for service discovery, and exits immediately when not provided. Host humble fastrtps engine + kilted zenoh model can't see each other.

Plans 04-04 (parity_report.py + M1 ship-gate exercise) and 04-05 (DX-05 + REQUIREMENTS flips + .m1_shipped touch) remain blocked behind this model-zenoh issue. PARITY-07 plumbing (recursive PhysxContactReportAPI tag-walk + D-13 live port paths from spawn) is verified intact but no live fire because policy never drives robot.

User pause requested via /gsd-pause-work. WIP commit captures: 04-03-SUMMARY.md (iter-5/6/7 + new blocker section), dryrun_trial_1.txt (iter-5/6/7 + DRY-RUN VERDICT), run_aic_engine_against_isaac_sim.sh (host-build pivot wrapper), config.json (workflow.use_worktrees=false), .planning/.user_pause deletion.
</current_state>

<completed_work>

Completed Tasks:
- Plan 04-01 (Wave 1) — pre-flight A2/A4 probes — Done. A2=PASS (insufficient — see constraints above). A4=MISMATCH (live spawn paths /World/TaskBoard/SCPort_0/NICCardMount_0/NICCard mismatch hardcoded _PORT_LINK_PATHS). Commits ff6ca28 + 400ac01 + 41117c4.
- Plan 04-02 (Wave 2) — load_trial MCP atom (4-surface DX-02) + ground_truth kwarg + adapter helpers (_extract_cable_kwargs + _dispatch_rail_block) + audit_dx02.py extension to 30 PRESENT atoms. Commits aa14256 + 82fc1a7 + d3d6764.
- Plan 04-03 (Wave 3) deliverables — Done with hard-won pivot:
  - Task 1: my-eval-isaac:v1 Docker derived image (engine-only entrypoint atop ghcr.io/intrinsic-dev/aic/aic_eval:latest). DEPRECATED post-pivot, kept under exts/aic-dt/docker/my-eval-isaac/ for archival. Commit 50792b8.
  - Task 2: D-13 1-surface setter (AicScoringPublishers.set_port_link_paths + _effective_port_link_paths method, NOT a 4-surface MCP atom per Wave 1 SUMMARY clarification) + run_aic_engine_against_isaac_sim.sh wrapper. Commit e3aa68a.
  - Task 3: trial_1 dry-run (4 docker iterations BLOCKED by iter-4 fastrtps RMW incompat) + 04-03-SUMMARY.md + dryrun_trial_1.txt + HARD BLOCKER escalation. Commit 041105b.
  - Pivot: host-build aic_engine + aic_adapter against humble in ~/aic_humble_ws/. 5 source pivots captured in exts/aic-dt/source_pivots/ (gz_math_vendor→gz-math7, ScoringTier2 humble rosbag2 API, libfmt drop-in for std::format, set+u in subshells for AMENT_TRACE_SETUP_FILES). aic_engine reaches "Engine Stopped" with full lifecycle. iter-4 RMW blocker RESOLVED. Commit 7943536.
</completed_work>

<remaining_work>

- Phase 4 closure paperwork (commit pending /gsd-pause-work): 04-03-SUMMARY.md iter-5/6/7 + new blocker section, dryrun_trial_1.txt iter-5/6/7 + verdict, host-build pivot wrapper, config.json workflow.use_worktrees=false, .planning/.user_pause deletion.
- Resolve model-zenoh downstream blocker (3 mitigation options — see HANDOFF.json blockers[1]).
- Plan 04-04 (Wave 4): parity_report.py + dual JSON+MD output + first M1 ship-gate exercise (3 trials × 2 sims). Engine wrapper ready (host-build pivot). Model side gated.
- Plan 04-05 (Wave 5): DX-05 README rewrite + CHANGELOG M1 entry + REQUIREMENTS flips per 04-04 verdict + ROADMAP/STATE updates + 04-SUMMARY.md (phase-level) + conditional .m1_shipped touch (only if 04-04 verdict=READY).
- M1 ship gate: every sample_config.yaml trial passes against Isaac Sim with same outcome as Gazebo; touch .planning/.m1_shipped.
</remaining_work>

<decisions_made>

- **D-06 (CONTEXT) SUPERSEDED:** Original "reuse aic_eval Docker container, skip Gazebo bringup" → no `start_gazebo:=false` flag in `aic_gz_bringup.launch.py`. Plan 04-03 first attempted derived `my-eval-isaac:v1` image (Option 4B from RESEARCH.md). Then iter-4 surfaced fastrtps cross-distro type-hash incompat. Final pivot: host-build `aic_engine` + `aic_adapter` as native humble processes (mitigation option b from HANDOFF.json blocker). Cleanest path; preserves architectural law; no Docker for engine.
- **D-13 is a 1-surface helper, NOT a 4-surface MCP atom.** `AicScoringPublishers.set_port_link_paths(paths)` method + `_start_aic_scoring_publishers(port_link_paths=...)` kwarg. `_cmd_load_trial` computes paths from spawn-call sites. Per Plan 04-01 SUMMARY clarification.
- **A2 verdict was insufficient.** Wave 1 A2 probe verified custom messages (matched ABI by same-workspace build) and humble-side host subscriber. Did NOT verify cross-distro stock-message decode. iter-4 dry-run is what surfaced the real issue. Future cross-distro RMW probes must include a stock-message decode test from inside the OTHER end's distro.
- **Recursive PhysxContactReportAPI tag-walk** instead of root-only tagging. Original code tagged only the top prim path; rigid-body collision children under `/World/TaskBoard/SCPort_0/sc_port_visual` were untagged → no contact events. Rule 1 fix: walk descendants and tag any RigidBodyAPI-bearing prim.
- **5 source pivots for kilted→humble portability** captured in `exts/aic-dt/source_pivots/`:
  1. `aic_scoring/CMakeLists.txt`: gz_math_vendor (kilted-only) → libgz-math7-dev (system humble package)
  2. `aic_adapter/src/aic_adapter.cpp`: C++20 `<format>` → libfmt (g++-11 has no `<format>`, added in g++-13)
  3. `aic_scoring/src/ScoringTier2.cc`: kilted `create_generic_subscription` 2-arg lambda → humble 1-arg lambda
  4. `aic_scoring/src/ScoringTier2.cc`: kilted `bagWriter.write` 5-arg → humble deprecated 4-arg overload
  5. `simulation_interfaces` package: humble has no source distribution; extracted via `apt-get download` + `dpkg-deb -x` (no sudo)
- **`set +u` in launch subshells.** Outer `set -euo pipefail` propagates; `/opt/ros/humble/setup.bash` references unbound `AMENT_TRACE_SETUP_FILES` → bash error.
- **Phase 4 sequential mode.** `workflow.use_worktrees: false` — Phase 4 plans hard-serialize and have heavy real-world side effects (Docker, Isaac Sim launch, ROS subscribers); worktree isolation adds merge overhead with no parallelism win.
</decisions_made>

<blockers>
- **phase-4-03-rmw-interop** (RESOLVED): kilted↔humble fastrtps type-hash incompat. Resolution: host-build pivot (commit 7943536). Verification: iter-7 trial_1 dry-run reaches "Engine Stopped" with full lifecycle.
- **phase-4-03-model-zenoh** (OPEN, blocks M1 ship gate): my-solution:v1 (CheatCode policy container) requires zenoh transport for service discovery. Mitigation options:
  - (a) Stand up zenohd on host (binary at `~/Documents/aic/.pixi/envs/default/bin/zenohd`) + add fastdds↔zenoh bridge for engine ↔ model. ~30min infra.
  - (b) Build aic_model + CheatCode policy humble-native (mirror the 04-03 pivot for the model side). Clone aic_example_policies + my-solution Dockerfile contents into a humble env or `~/aic_humble_ws`. ~60-90min build. Cleanest M1 path. Risk: my-solution:v1 may have transitive zenoh deps in CheatCode.py itself.
  - (c) Stub lifecycle node mimicking aic_model's `/aic_model/get_state` + `/aic_model/configure` contract for PARITY-07 fire-test only. ~20min. Drives gripper toward port_1 deterministically; verifies `/scoring/insertion_event` fires; does NOT verify CheatCode trial outcome. Useful as a discriminator: PARITY-07 fires under stub → bug is in model-zenoh path; PARITY-07 doesn't fire → bug is in contact-pair plumbing.
</blockers>

## Required Reading (in order)

1. `./CLAUDE.md` — autonomous M1 mode block at top; two-launcher reality; ROS_DOMAIN_ID=7; MCP socket protocol; NO Co-Authored-By in commits.
2. `.planning/HANDOFF.json` — structured next_action + completed_tasks + decisions + open blockers.
3. `.planning/STATE.md` — current phase, last activity (note: STATE.md frontmatter may lag this handoff; the .continue-here.md is canonical for Phase 4 progress).
4. `.planning/phases/04-trial-loader/04-CONTEXT.md` — 13 D-decisions; D-06 marked superseded by RESEARCH.
5. `.planning/phases/04-trial-loader/04-RESEARCH.md` — 803 lines; D-06 supersession rationale + sample_config.yaml schema audit.
6. `.planning/phases/04-trial-loader/04-03-SUMMARY.md` — full iter-1..7 trail; the iter-5/6/7 host-build pivot section is the most critical reading; new blocker section at the end.
7. `.planning/phases/04-trial-loader/dryrun_trial_1.txt` — DRY-RUN VERDICT (host-build pivot) section + the 3 mitigation options for model-zenoh.
8. `~/Documents/aic/scripts/run_cheatcode.sh` lines 47-49 — model container env vars (AIC_ROUTER_ADDR + RMW_IMPLEMENTATION=rmw_zenoh_cpp).
9. `exts/aic-dt/source_pivots/README.md` — the 5 patches needed for kilted→humble portability; reference for mitigation (b).
10. `~/.claude/skills/isaac-sim-extension-dev/SKILL.md` — for any Isaac Sim API question.
11. `~/.claude/skills/nvidia-suite-docs/SKILL.md` — for Isaac Sim 5.0 / OpenUSD / OmniGraph live docs.

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN:** Probing cross-distro RMW interop using only same-workspace custom messages → masks stock-message type-hash incompat. **STRUCTURAL MITIGATION:** Cross-distro RMW probes must include a stock-message subscribe (sensor_msgs/JointState, rosgraph_msgs/Clock) from inside the OTHER distro's container against the host's publisher.
- **ANTI-PATTERN:** Treating `ros2 topic info` "publisher count=1 RELIABLE" as "messages will flow" → type-hash mismatch causes 0-message delivery while topic-info still enumerates. **STRUCTURAL MITIGATION:** All cross-distro probes MUST attempt actual message decode with a 10-30s timeout.
- **ANTI-PATTERN:** Building Docker derived images on a research-finding-driven assumption before Wave 1 verifies the assumption. **STRUCTURAL MITIGATION:** Future research-driven Docker work must explicitly cite which Wave 1 probe verified the assumption that drives the build.
- **ANTI-PATTERN:** Sweeping transport/SHM env permutations when `ros2 topic info --verbose` already shows `Topic type hash: INVALID`. **STRUCTURAL MITIGATION:** If type hash is INVALID, jump to cross-distro / build-from-source investigation; don't sweep FASTDDS_BUILTIN_TRANSPORTS, --ipc=host, ROS_LOCALHOST_ONLY first.

## Infrastructure State

- **Isaac Sim:** Was running on PID `<see ps aux | grep isaacsim>` after iter-7. Cache snapshot post-success: `~/.cache/ov/DerivedDataCache.bak.1777986848` (153.5 MB). May have been killed since 2026-05-05; restart via `bash -c 'source ~/env_isaaclab/bin/activate && ROS_DOMAIN_ID=7 DISPLAY=${DISPLAY:-:0} bash ~/.claude/skills/isaac-sim-extension-dev/scripts/isaacsim_launch.sh launch aic-dt'`.
- **Host humble workspace:** `~/aic_humble_ws/install/` contains `aic_engine`, `aic_adapter`, `aic_scoring`, `aic_engine_interfaces`, `aic_task_interfaces`, `aic_model_interfaces`, `aic_control_interfaces`, `simulation_interfaces` (extracted from apt deb). Idempotent rebuild via `bash exts/aic-dt/scripts/build_aic_engine_host.sh`.
- **Docker images:** `ghcr.io/intrinsic-dev/aic/aic_eval:latest` (kilted bringup, USED FOR aic_model only post-pivot), `my-eval-isaac:v1` (DEPRECATED, kept for archival), `my-solution:v1` (CheatCode policy, exits without zenoh — the open blocker).
- **ROS_DOMAIN_ID=7** isolation maintained throughout (UR5e real driver runs on default 0).
- **Zenoh router binary** available at `~/Documents/aic/.pixi/envs/default/bin/zenohd` (kilted-built; can run on host without sudo).
- **`.planning/.autonomous_m1_active`** still present — autonomous M1 mode active. `.planning/.user_pause` deleted (this pause captured in /gsd-pause-work). `.planning/.m1_shipped` does NOT exist.

## Pre-Execution Critique Required

- **Design artifact:** `.planning/HANDOFF.json` blockers[1] mitigation_options (a/b/c) — model-zenoh resolution path.
- **Critique focus:**
  1. Does mitigation (b) host-build aic_model preserve the architectural law that says ~/Documents/aic is read-only? (Cloning into ~/aic_humble_ws/src and applying source pivots is the same pattern used for engine-side; the AIC repo itself stays untouched.)
  2. Does mitigation (a) zenohd-bridge violate the "no bridges" parity law? (Topic-name parity is preserved; only the DDS layer is bridged — this is a subtler distinction. The user's read-only constraint on ~/Documents/aic is the load-bearing rule, not "no bridges in any layer".)
  3. Does mitigation (c) stub lifecycle deliver enough to test PARITY-07 plumbing? (Yes for the contact-pair fix verification, but not for M1 ship gate which requires actual CheatCode pass/fail outcome match against Gazebo.)
- **Gate:** Do NOT begin executing a mitigation option until the user explicitly picks one OR explicitly delegates the choice to autonomous mode (per CLAUDE.md autonomous-M1 policy "fix bugs and continue", a fresh resume can default to (c) → (b) sequence: 20-min discriminator, then commit to humble-native build).

<context>
The Phase 4 work has been a study in mismatched assumptions across the AIC stack. Wave 1's A2 probe declared "PASS" too early — same-workspace custom-message verification masks stock-message type-hash incompat across ROS distros. Iter-4 spent the wall clock on transport/SHM permutations before recognizing the type-hash issue. The pivot to host-build aic_engine + aic_adapter against humble was the right call (mitigation option b from the original blocker list); 5 source pivots captured the kilted→humble portability surface for reproducibility.

The new model-zenoh blocker is the same architectural pattern at a different layer: AIC competition stack uses zenoh for cross-container model↔eval lifecycle, and humble fastrtps engine doesn't speak zenoh. Three mitigation paths identified, ordered by recommended sequence: (c) stub for plumbing verification → (b) humble-native model build for M1 ship gate → (a) zenoh-fastdds bridge as fallback if (b) reveals deep zenoh dependencies in CheatCode.py.

The user's autonomous-M1 policy ("fix bugs and continue till M1 ships") authorizes proceeding without explicit pick — but this pause was triggered by /gsd-pause-work explicitly, not autonomous-mode self-pause. Resuming should re-confirm the user's intent: continue autonomous, or pick a specific mitigation.

Key insight: Plan 04-03's iter-7 SUCCESS (engine reaches Engine Stopped under host-build) is recorded but the Plan 04-04 + 04-05 closure paperwork wasn't written because the model-zenoh blocker emerged on the same iter-7 run. So Phase 4 is at an unusual midpoint — engine half done, model half blocked, paperwork pending.
</context>

<next_action>
Start with: Read `.planning/HANDOFF.json` blockers[1] mitigation_options. Then ask user (if interactive) or pick autonomous default (c → b sequence). For (c) stub lifecycle: write `exts/aic-dt/scripts/stub_aic_model_for_parity07.py` (Python rclpy node) that creates `/aic_model/get_state` + `/aic_model/configure` services to mimic aic_model's contract, drives the gripper toward port_1 via `/aic_controller/pose_commands` deterministically, and verifies `/scoring/insertion_event` fires. Run trial_1 with stub instead of my-solution:v1 container in run_aic_engine_against_isaac_sim.sh; capture verdict in `.planning/phases/04-trial-loader/dryrun_trial_1.txt` as a new iteration block. If PARITY-07 fires → commit to (b) for M1 ship gate; if not → debug contact-pair plumbing first.
</next_action>
