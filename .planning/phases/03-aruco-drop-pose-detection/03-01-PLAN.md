---
phase: 03-aruco-drop-pose-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/Desktop/ros2_ws/src/aruco_camera_localizer/config/aruco_config.json
  - ~/Desktop/ros2_ws/src/aruco_camera_localizer/aruco_camera_localizer/merged_localization_aruco.py
  - ~/Documents/isaac-sim-mcp/exts/soarm101-dt/so_arm101_dt/extension.py
  - ~/Projects/Exploring-VLAs/vla_SO-ARM101/src/so_arm101_control/so_arm101_control/control_gui.py
autonomous: true
requirements: [CAM-01, CAM-02, CAM-03]

must_haves:
  truths:
    - "Running merged_localization_aruco.py --drop --robot so_arm101 publishes drop_0, drop_1, drop_2 transforms on /drop_poses"
    - "Running merged_localization_aruco.py --drop with no --robot arg still works for JETANK (backward compat)"
    - "Isaac Sim publish_drop_poses creates wrapper prims named drop_0, drop_1, drop_2 — not drop_red/green/blue"
    - "aruco_config.json preserves all JETANK marker_rows unchanged under robots.jetank"
  artifacts:
    - path: "~/Desktop/ros2_ws/src/aruco_camera_localizer/config/aruco_config.json"
      provides: "Multi-robot marker config"
      contains: "active_robot"
    - path: "~/Desktop/ros2_ws/src/aruco_camera_localizer/aruco_camera_localizer/merged_localization_aruco.py"
      provides: "Robot-aware config loader"
      exports: ["load_aruco_config"]
    - path: "~/Documents/isaac-sim-mcp/exts/soarm101-dt/so_arm101_dt/extension.py"
      provides: "Phase 1 sim extension with drop_{id} naming"
      contains: "drop_{aruco_id}"
  key_links:
    - from: "merged_localization_aruco.py --robot arg"
      to: "aruco_config.json robots[active_robot]"
      via: "load_aruco_config() reads active_robot then robots[active_robot][marker_rows]"
    - from: "extension.py _cmd_publish_drop_poses"
      to: "wrapper prim names"
      via: "wrapper_path = f\"/World/Containers/drop_{aruco_id}\""
---

<objective>
Wire up aruco_camera_localizer to detect SO-ARM101 cup markers and publish /drop_poses with consistent drop_0/drop_1/drop_2 child_frame_ids. Fix the Phase 1 Isaac Sim extension to use the same naming.

Purpose: Phase 3 deliverable — real-hardware pose source from wrist camera. Also aligns Phase 1 and Phase 3 naming so Phase 2 drop listbox sees identical frame IDs regardless of source.
Output: Updated aruco_config.json, load_aruco_config(), and extension.py _cmd_publish_drop_poses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/aaugus11/Documents/isaac-sim-mcp/.planning/PROJECT.md
@/home/aaugus11/Documents/isaac-sim-mcp/.planning/ROADMAP.md
@/home/aaugus11/Documents/isaac-sim-mcp/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure aruco_config.json for multi-robot support</name>
  <files>/home/aaugus11/Desktop/ros2_ws/src/aruco_camera_localizer/config/aruco_config.json</files>
  <read_first>
    Read the full current file at /home/aaugus11/Desktop/ros2_ws/src/aruco_camera_localizer/config/aruco_config.json before writing.
  </read_first>
  <action>
    Replace the flat structure with a multi-robot structure. The new JSON must have this exact shape:

    {
      "_comment": "position_offset values are in ArUco marker axis frame of reference",
      "active_robot": "so_arm101",
      "robots": {
        "jetank": {
          "marker_rows": {
            "top": {
              "marker_ids": [1, 4, 7, 10, 13],
              "position_offset": {"X": 0.0, "Y": -0.15, "Z": 0.15}
            },
            "middle": {
              "marker_ids": [2, 5, 8, 11, 14],
              "position_offset": {"X": 0.0, "Y": -0.095, "Z": 0.1}
            },
            "bottom": {
              "marker_ids": [3, 6, 9, 12, 15],
              "position_offset": {"X": 0.0, "Y": -0.025, "Z": 0.05}
            }
          }
        },
        "so_arm101": {
          "marker_rows": {
            "cups": {
              "marker_ids": [0, 1, 2],
              "position_offset": {"X": 0.0, "Y": 0.083, "Z": -0.039}
            }
          }
        }
      }
    }

    JETANK marker_rows values (top/middle/bottom) must be bit-for-bit identical to current values — do not alter offsets or IDs. The active_robot default is "so_arm101" (per D-01: SO-ARM101 config is the current working context).
  </action>
  <verify>
    python3 -c "
import json
with open('/home/aaugus11/Desktop/ros2_ws/src/aruco_camera_localizer/config/aruco_config.json') as f:
    c = json.load(f)
assert c['active_robot'] == 'so_arm101'
assert set(c['robots'].keys()) == {'jetank', 'so_arm101'}
assert c['robots']['jetank']['marker_rows']['top']['marker_ids'] == [1,4,7,10,13]
assert c['robots']['so_arm101']['marker_rows']['cups']['marker_ids'] == [0,1,2]
assert c['robots']['so_arm101']['marker_rows']['cups']['position_offset'] == {'X':0.0,'Y':0.083,'Z':-0.039}
print('aruco_config.json OK')
"
  </verify>
  <acceptance_criteria>
    - File contains top-level keys: _comment, active_robot, robots
    - robots.jetank.marker_rows has top/middle/bottom with original IDs and offsets unchanged
    - robots.so_arm101.marker_rows.cups has marker_ids [0,1,2] and position_offset X=0.0, Y=0.083, Z=-0.039
    - active_robot = "so_arm101"
    - Validation script exits 0 with "aruco_config.json OK"
  </acceptance_criteria>
  <done>aruco_config.json validates cleanly with both robot configs present and JETANK data identical to original</done>
</task>

<task type="auto">
  <name>Task 2: Update load_aruco_config() for multi-robot structure and add --robot CLI arg</name>
  <files>/home/aaugus11/Desktop/ros2_ws/src/aruco_camera_localizer/aruco_camera_localizer/merged_localization_aruco.py</files>
  <read_first>
    Read lines 57-140 of /home/aaugus11/Desktop/ros2_ws/src/aruco_camera_localizer/aruco_camera_localizer/merged_localization_aruco.py to see parse_args(), load_aruco_config(), and the args.drop usage block before editing.
  </read_first>
  <action>
    Two changes in merged_localization_aruco.py:

    CHANGE 1 — Update load_aruco_config() signature and body (currently around line 80):

    Old signature: `def load_aruco_config():`
    New signature: `def load_aruco_config(robot: str = None):`

    Old body reads: `for row_name, row_data in aruco_config["marker_rows"].items():`

    New body logic (replace the for-loop preamble):
    ```python
    # Multi-robot support: if "robots" key exists, select the active robot's config
    if "robots" in aruco_config:
        active = robot if robot else aruco_config.get("active_robot", "jetank")
        robot_cfg = aruco_config["robots"].get(active)
        if robot_cfg is None:
            raise KeyError(f"Robot '{active}' not found in aruco_config.json robots dict")
        marker_rows = robot_cfg["marker_rows"]
    else:
        # Backward compat: old flat format (JETANK-only aruco_config.json)
        marker_rows = aruco_config["marker_rows"]

    marker_to_row = {}
    for row_name, row_data in marker_rows.items():
        ...  # rest of loop unchanged
    ```

    CHANGE 2 — Add --robot arg to parse_args() (currently around line 58):

    After the existing `--drop` argument add:
    ```python
    parser.add_argument("--robot", type=str, default=None,
                        help="Robot config to use from aruco_config.json (e.g., 'jetank', 'so_arm101'). "
                             "Overrides active_robot field in config.")
    ```

    CHANGE 3 — Pass robot arg when load_aruco_config() is called (currently around line 131):

    Old call: `marker_to_row = load_aruco_config()`  (or similar)
    New call: `marker_to_row = load_aruco_config(robot=args.robot)`

    Do not change anything else in the file.
  </action>
  <verify>
    python3 -c "
import sys
sys.path.insert(0, '/home/aaugus11/Desktop/ros2_ws/src/aruco_camera_localizer/aruco_camera_localizer')
# Patch get_package_share_directory to return our config dir
import unittest.mock as mock
def fake_pkg_share(name):
    return '/home/aaugus11/Desktop/ros2_ws/src/aruco_camera_localizer'
with mock.patch.dict('sys.modules', {'ament_index_python.packages': mock.MagicMock(get_package_share_directory=fake_pkg_share)}):
    import importlib, merged_localization_aruco as m
    # so_arm101 config
    result = m.load_aruco_config(robot='so_arm101')
    assert 0 in result and 1 in result and 2 in result, f'Expected 0,1,2 in result, got {list(result.keys())}'
    # jetank config
    result_jt = m.load_aruco_config(robot='jetank')
    assert 1 in result_jt and 4 in result_jt and 13 in result_jt
    print('load_aruco_config OK')
"
  </verify>
  <acceptance_criteria>
    - load_aruco_config(robot='so_arm101') returns dict with keys 0, 1, 2
    - load_aruco_config(robot='jetank') returns dict with keys 1..15 (JETANK IDs)
    - load_aruco_config() with no arg uses active_robot from config (so_arm101)
    - --robot CLI arg exists in parse_args() output
    - Old flat-format fallback path still present in code (backward compat guard)
  </acceptance_criteria>
  <done>load_aruco_config() handles both multi-robot and legacy flat config formats; --robot arg wires through to robot selection</done>
</task>

<task type="auto">
  <name>Task 3: Fix child_frame_id naming in Isaac Sim extension (_cmd_publish_drop_poses)</name>
  <files>/home/aaugus11/Documents/isaac-sim-mcp/exts/soarm101-dt/so_arm101_dt/extension.py</files>
  <read_first>
    Read lines 4767-4870 of /home/aaugus11/Documents/isaac-sim-mcp/exts/soarm101-dt/so_arm101_dt/extension.py to see the full _cmd_publish_drop_poses method before editing. Also read lines 382-385 to see the MCP_TOOL_REGISTRY entry for publish_drop_poses.
  </read_first>
  <action>
    Two targeted changes in extension.py:

    CHANGE 1 — Rename wrapper prims from drop_{color} to drop_{aruco_id} (around line 4812):

    Old line:
        `wrapper_path = f"/World/Containers/drop_{color}"`

    New line:
        `wrapper_path = f"/World/Containers/drop_{aruco_id}"`

    The variable `aruco_id` is already in scope from the loop: `for color, aruco_id in color_to_id.items()`.

    Also update the docstring (lines 4770-4774) to reflect the new naming:
    - Old: "Creates wrapper Xform prims (drop_red, drop_green, drop_blue)"
    - New: "Creates wrapper Xform prims (drop_0, drop_1, drop_2)"
    - Old: "child_frame_ids are drop_red/green/blue"
    - New: "child_frame_ids are drop_0, drop_1, drop_2"

    Also update the print statement on line 4825:
    Old: `print(f"[drop_poses] Created wrapper {wrapper_path} at ArUco marker transform")`
    Keep as-is (wrapper_path already uses the new variable, so the print is automatically correct).

    CHANGE 2 — Update MCP_TOOL_REGISTRY description (around line 383):

    Old description: "...Each transform's child_frame_id is drop_red, drop_green, or drop_blue..."
    New description: "...Each transform's child_frame_id is drop_0, drop_1, or drop_2..."

    Do not change any other lines.
  </action>
  <verify>
    python3 -c "
import re
content = open('/home/aaugus11/Documents/isaac-sim-mcp/exts/soarm101-dt/so_arm101_dt/extension.py').read()
assert 'drop_{aruco_id}' in content, 'wrapper_path must use aruco_id'
assert 'drop_{color}' not in content, 'drop_{color} must be removed'
assert 'drop_0, drop_1, drop_2' in content or 'drop_0/drop_1/drop_2' in content, 'Registry description must mention drop_0/1/2'
assert 'drop_red, drop_green, or drop_blue' not in content, 'Old color naming must be gone from description'
print('extension.py naming OK')
"
  </verify>
  <acceptance_criteria>
    - wrapper_path line uses `drop_{aruco_id}` not `drop_{color}`
    - MCP_TOOL_REGISTRY publish_drop_poses description references drop_0/drop_1/drop_2
    - No remaining references to drop_red/drop_green/drop_blue in _cmd_publish_drop_poses or MCP_TOOL_REGISTRY
    - Validation script exits 0 with "extension.py naming OK"
  </acceptance_criteria>
  <done>Isaac Sim extension publishes drop_0/drop_1/drop_2 transforms, consistent with ArUco localizer naming</done>
</task>

<task type="auto">
  <name>Task 4: Add drop ID → color label mapping in SO-ARM101 control_gui drop listbox</name>
  <files>/home/aaugus11/Projects/Exploring-VLAs/vla_SO-ARM101/src/so_arm101_control/so_arm101_control/control_gui.py</files>
  <read_first>
    Read the _populate_drop_list method in control_gui.py to see how drop targets are displayed in the listbox. Also read _drop_callback to see how _drop_data is populated.
  </read_first>
  <action>
    1. Add a DROP_ID_LABELS dict near the top of the class or as a module-level constant:
    ```python
    # ArUco marker ID → human-readable color label for drop targets
    DROP_ID_LABELS = {
        "drop_0": "red",
        "drop_1": "green",
        "drop_2": "blue",
    }
    ```

    2. In _populate_drop_list, update the listbox entry formatting to include the color label:
    When inserting into the drop listbox, change from showing just `name (x, y, z)` to:
    `name [color] (x, y, z)` — e.g., `drop_0 [red] (0.15, 0.08, 0.12)`

    Use: `label = DROP_ID_LABELS.get(name, "")` and if label is non-empty, include `[{label}]` after the name.
  </action>
  <verify>
    python3 -c "
content = open('/home/aaugus11/Projects/Exploring-VLAs/vla_SO-ARM101/src/so_arm101_control/so_arm101_control/control_gui.py').read()
assert 'DROP_ID_LABELS' in content, 'DROP_ID_LABELS dict must exist'
assert '\"drop_0\": \"red\"' in content or '\"drop_0\":\"red\"' in content, 'drop_0 must map to red'
print('Drop labels OK')
"
  </verify>
  <acceptance_criteria>
    - DROP_ID_LABELS dict maps drop_0→red, drop_1→green, drop_2→blue
    - Drop listbox entries show color labels (e.g., "drop_0 [red]")
    - Validation script exits 0
  </acceptance_criteria>
  <done>Drop listbox shows human-readable color labels for each ArUco marker ID</done>
</task>

</tasks>

<verification>
Run all three verify commands sequentially. All must exit 0 with no assertion errors.

Final integration check — confirm both sources produce matching names:
- Isaac Sim (extension.py): publishes drop_0, drop_1, drop_2
- ArUco localizer (merged_localization_aruco.py): publishes drop_0, drop_1, drop_2 (frame naming is f"drop_{marker_id}" in the existing drop pose loop — marker IDs 0,1,2 produce drop_0, drop_1, drop_2)
- Both use the same child_frame_ids, so Phase 2 drop listbox receives consistent names regardless of which source is active
</verification>

<success_criteria>
1. aruco_config.json: top-level "robots" key with jetank (original IDs 1-15) and so_arm101 (IDs 0-2 with Y=0.083, Z=-0.039 offset)
2. load_aruco_config(robot='so_arm101') returns {0: ..., 1: ..., 2: ...}
3. load_aruco_config(robot='jetank') returns JETANK's original 15-ID dict
4. extension.py _cmd_publish_drop_poses creates drop_0, drop_1, drop_2 wrapper prims
5. No drop_red / drop_green / drop_blue strings remain in the MCP registry or wrapper_path line
</success_criteria>

<output>
After completion, create `/home/aaugus11/Documents/isaac-sim-mcp/.planning/phases/03-aruco-drop-pose-detection/03-01-SUMMARY.md`
</output>
