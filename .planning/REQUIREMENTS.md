# Requirements: aic-dt — AIC Cable-Insertion Digital Twin

**Defined:** 2026-05-01
**Core Value:** When the same `aic_controller` + `aic_example_policies/CheatCode.py` + `sample_config.yaml` that pass AIC trials in Gazebo are pointed at this Isaac Sim digital twin, every trial passes with the same outcome.

## v1 Requirements (Milestone 1 — Platform Transfer)

### Asset & Topic Parity

- [x] **PARITY-01**: Isaac Sim loads the AIC repo's `aic_description` xacro/URDF for the UR5e + Robotiq Hand-E + 3 wrist cameras (no divergent kinematics or geometry from Gazebo) _(closed: doc + code-side gripper-name corrected in Plans 01-03 / 01-04; load path = aic_unified_robot.usd (xacro-derived canonical AIC representation, not runtime xacro expansion); kinematic/geometric parity verified live by smoke_test_aic_parity.py 13/13 against /opt/ros/humble Python 3.10 rclpy — 7 alphabetical joints match aic_adapter::joint_sort_order_ exactly, tf2 lookup_transform succeeds for `base_link`→`gripper/tcp` (CheatCode L105) + `tool0` distance plausible (0.865m). The "no divergent kinematics or geometry from Gazebo" bar is the testable contract; the smoke test is the verifier)_
- [x] **PARITY-02**: Isaac Sim loads the AIC repo's `aic_assets` meshes for the task board, ports (sc / sfp / lc / nic), mount rails, cables, and the AIC enclosure (no divergent geometry) _(closed: disk-layer vendor complete in Plan 01-02 — capitalized AIC tree byte-identical under exts/aic-dt/assets/assets/ (21 files: 9 USDs + 12 textures, md5sum byte-identical to upstream); AIC_OBJECTS code path repointed at vendored capitalized layout in Plan 01-04; LC/SFP/SC Mount asset folders + cable assets vendored in Plan 01-09; scene-load verified live in working session — 387 prims after quick_start, no missing-mesh errors. Mount-rail mPU/upAxis bake (Gap E) is latent for SCENE-01 trial spawn, tracked in 01-SUMMARY Deferred Items)_
- [x] **PARITY-03**: Isaac Sim publishes `/joint_states` with the same joint name set + ordering Gazebo publishes _(closed inline 2026-05-03 commit 079afd8: rclpy-based AicParityPublishers replaces OGN ROS2PublishJointState — 7 alphabetical names matching aic_adapter::joint_sort_order_ EXACTLY (`elbow_joint`, `gripper/left_finger_joint`, `shoulder_lift_joint`, `shoulder_pan_joint`, `wrist_1_joint`, `wrist_2_joint`, `wrist_3_joint`), `frame_id=base_link`, gripper FixedJoint pinned to 0.0; smoke test `exts/aic-dt/scripts/smoke_test_aic_parity.py` verifies via /opt/ros/humble Python 3.10 rclpy that aic_adapter-style consumers see exactly the right name set + ordering + frame_id with 259 messages in 3s; the OGN-spec deferral noted in Plan 06 PLAN.md L302 is now moot — bypassed entirely)_
- [x] **PARITY-04**: Isaac Sim publishes `/tf` and `/tf_static` containing the same robot + gripper + camera frames Gazebo publishes (with same parent-child hierarchy and frame names) _(closed inline 2026-05-03 commit 079afd8: rclpy-based AicParityPublishers replaces OGN ROS2PublishTransformTree — 30 of 31 AIC frames mapped from USD prim leafs to slashed AIC names (`gripper/hande_base_link`, `cam_mount/cam_mount_link`, `ati/tool_link`, `*_camera/*_link`/`sensor_link`/`optical`), 1 synthesized `world→aic_world` identity transform; 8 dynamic edges (6 arm + 2 gripper finger) on `/tf` at ~70 Hz, 22 static edges on `/tf_static` (TRANSIENT_LOCAL); smoke test verifies tf2_ros::Buffer.lookup_transform succeeds for `base_link`→`gripper/tcp` (CheatCode L105), `base_link`→`tool0`, `world`→`aic_world`, `world`→`tool0` (full chain) — 6 TF tests pass + tool0 distance from base_link plausible (0.865m); the OGN-spec deferral noted in Plan 06 PLAN.md L51/302 is now moot — bypassed entirely)_
- [~] **PARITY-05**: Isaac Sim publishes `/fts_broadcaster/wrench` (`geometry_msgs/WrenchStamped`) for the UR5e end-effector force/torque, matching Gazebo's topic name and frame_id _(REOPENED 2026-05-08: topic publishes at 13.6Hz with correct WrenchStamped type + frame_id=ati/tool_link, BUT runtime payload is all-zeros — `_on_physics_step_force` raises `'Articulation' object has no attribute '_physics_view'` every tick, caught by try/except as `[Force callback]`. Verified empirically via ROS2 sample. aic_adapter.cpp:71 subscribes and packages the garbage into Observation.wrist_wrench → reaches the model. Phase 1 verifier checked rate + topic-info + frame_id but never sampled payload. Partial fix shipped 6fc6d26 (path correction + hasattr guard); root cause still open — needs Isaac Sim 5.0 Articulation lifecycle deep-dive. Original closure citation: Plan 01-04 static reconciliation (frame_id=ati/tool_link from URDF line 242))_
- [x] **PARITY-06**: Isaac Sim publishes `/aic/gazebo/contacts/off_limit` (`ros_gz_interfaces/Contacts`) for off-limit-item contact events — exact topic name kept even though "gazebo" is in it (zero-rename rule) _(closed Plans 02-02 + 02-06: AicControllerLoop._setup_contact_subscription applies PhysxSchema.PhysxContactReportAPI(threshold=0.0) per Pitfall 8 + UsdPhysics.RigidBodyAPI presence check per Pitfall 7 to each off-limit prim; subscribes via omni.physx.get_physx_simulation_interface().subscribe_contact_report_events per D-03 (NOT broken ContactSensor wrapper); _on_contact_event physics-thread callback filters via prefix-startswith on DEFAULT_OFF_LIMIT_PRIMS = [/World/Enclosure, /World/Enclosure_Walls, /World/TaskBoard] (sourced from offlimit-prim-mapping.md / ur_gz.urdf.xacro:122-130 OffLimitContactsPlugin), appends survivors to module-level CONTACT_EVENTS deque (maxlen=2048); _publish_offlimit_contacts drains deque each physics tick and publishes ros_gz_interfaces/Contacts with header.frame_id="world" (CONTACT_FOUND only per robot-collision-forensics skill PERSIST/LOST suppression). D-11 silent-drop discipline applied to physics-thread callback. Subscription-alive verified via smoke_test_aic_controller.py Step 6 listener; live contact firing requires operator-driven arm motion (best-effort per Plan 02-06 Task 1 callout). Fixed plan's `actor in set` check to `actor.startswith(prefix)` to handle descendant collider paths correctly.)_
- [~] **PARITY-07**: Isaac Sim publishes `/scoring/insertion_event` (`std_msgs/String`) when a cable plug-port insertion completes _(Plan 03-05: scoring_publishers.py — std_msgs/String publisher live + omni.physx contact-report subscription wired (PhysxContactReportAPI threshold=0.0 on plug-end link_20 + 3 port links via get_physx_simulation_interface.subscribe_contact_report_events); _on_insertion_contact_event physics-thread callback parses actor0/actor1 paths, filters for plug↔port pair, appends (timestamp, port_name) to deque; _maybe_publish_insertion_event drains deque per tick, requires ≥5 sustained contacts in 100ms window, publishes once + arms; re-arms when contacts cease for ≥100ms. Code-level complete; live runtime verification (drive plug into port via aic_controller pose_cmd or execute_python_code, expect single event published with data=port_name) deferred to Phase 4 trial loader cycle)_
- [x] **PARITY-08**: Isaac Sim publishes `/scoring/tf` (`tf2_msgs/TFMessage`) containing cable link poses _(closed Plans 03-04+05 RE-RE-SCOPED: scoring_publishers.py publishes BOTH /scoring/tf AND /objects_poses_real per Plan 03-01 finding — /scoring/tf is the canonical ScoringTier2.cc consumer (tier2 mode only) + /objects_poses_real is the always-on grasp_points_publisher consumer surface; both carry TFMessage of cable plug + port frames built via _publish_frame_list helper from USD prim world transforms via UsdGeom.XformCache + Gf.Quatd→ROS Quaternion (x,y,z,w) reorder per Pitfall 4. Verified live 2026-05-05: `ros2 topic list | grep scoring|objects_poses` shows both topics under ROS_DOMAIN_ID=7 after quick_start; kit log: "[AIC-DT][scoring] Started: /scoring/tf + /objects_poses_real + /scoring/insertion_event" + "Articulation constructed lazily at tick 30". Frame names per scoring_tier2_contract.md: parent='world'; task_board children include 'task_board' substring per ScoringTier2.cc:51-53 substring-match)_
- [~] **PARITY-09**: Isaac Sim subscribes to `/aic_controller/joint_commands` (`aic_control_interfaces/JointMotionUpdate`) and applies the commanded motion to the UR5e _(REOPENED 2026-05-08: live ROS2 round-trip showed Δ=0.0000 rad on shoulder_lift_joint after publishing +0.10 rad command for 2s — controller subscriber was 100% broken end-to-end. Root cause: Phase 3 SCENE-05 cable activation expanded articulation 6→46 DOFs; controller_loop's `set_gains`/`set_joint_positions`/`apply_action` calls were shape-(1,6) without joint_indices scoping, raising ValueError every tick (silently swallowed by per-flag log-once). Phase 2 smoke_test_aic_controller.py Step 3 buffered + applied + returned but never sampled /joint_states post-command for delta. Fix shipped b682de1 (joint_indices=[[0,1,2,3,4,5]] on 4 call sites); post-fix round-trip Δ≈0.016 rad ≈ 16% of commanded — writes reach articulation but PD/cable coupling soaks 84%. Needs follow-up plan to reach >95% commanded amplitude. Original closure citation: Plans 02-02 + 02-03)_
- [~] **PARITY-10**: Isaac Sim subscribes to `/aic_controller/pose_commands` (`aic_control_interfaces/MotionUpdate`) and applies the commanded motion to the UR5e end-effector _(REOPENED 2026-05-08: same Phase-3-cable-broke-Phase-2-controller bug as PARITY-09, third call site. _apply_pose_cmd → Lula IK → set_joint_positions(ik_positions) wrote shape-(1,6) IK output against 46-DOF articulation without joint_indices scoping; Kit log: `set_joint_positions(ik) failed: ValueError shape mismatch (1,6) vs (1,46)`. Live round-trip test (target_pose Z-=0.05m for 3s): Δ=(0,0,0)m. Fix shipped 922a7d5 (joint_indices=[[0..5]]); runtime re-verification deferred (TF tree desync after hot-reload prevented mid-session re-test). Original closure citation: Plans 02-02 + 02-04. Phase 2 smoke Step 4 confirmed tool0 X moves "toward commanded delta" — never quantified the move; per-tick assertion was structural, not numerical.)_
- [x] **PARITY-11**: Isaac Sim publishes / subscribes `/aic_controller/controller_state` (`aic_control_interfaces/ControllerState`) so force-torque tare and controller bookkeeping work unchanged _(closed Plans 02-02 + 02-05: AicControllerLoop._publish_controller_state populated per D-07: header.frame_id="base_link" + sim-clock stamp; tcp_pose via FK on self._kinematics.compute_end_effector_pose() with rot_matrices_to_quats wxyz->ROS xyzw boundary reorder per Pitfall 4; tcp_velocity.linear via 3-sample ring-buffer numerical-diff on self._tcp_pose_buffer (dt floor=1e-6; angular=0 first-cut); reference_tcp_pose passthrough echo from self._last_reference_tcp_pose set by Plan 02-04; tcp_error[0..2] = current - reference for x/y/z (rotation components [3..5] = 0 first-cut deferred); reference_joint_state passthrough echo from self._last_reference_joint_state set by Plan 02-03; target_mode.mode = self._last_target_mode (TARGET_MODE_* enum verified against TargetMode.msg source); fts_tare_offset zero WrenchStamped with frame_id="ati/tool_link" per D-07 (Isaac Sim doesn't tare; aic_controller computes from /fts_broadcaster/wrench history per aic_controller.cpp:1275). D-11 try/except + log-once via self._logged_publish_error wraps FK + publish call. Round-tripped via smoke_test_aic_controller.py Step 5 — controller_state arrives with nonzero tcp_pose + nonzero tcp_error after pose cmd.)_
- [~] **PARITY-12**: Topic-surface audit recorded — a single document (under `exts/aic-dt/docs/`) lists every Gazebo topic in the AIC reference setup and shows the Isaac Sim equivalent (with proof-of-publish), surfacing any unintentional gaps before M1 ship _(REOPENED 2026-05-08: the audit table in this REQUIREMENTS.md (lines below) was last refreshed 2026-05-03 at Phase 2 closure — Phase 3 + Phase 4 plan landings did NOT refresh it, so it's been showing SCENE-02..06 + TRIAL-01..03 as Pending while inline `[x]` flags above show them closed. The "proof-of-publish" column likewise never got populated post-Phase-2. Initial population: Plan 01-01)_

### Texture & Material Sweep

- [x] **TEX-01**: Every USD/MDL asset referenced by the M1 spawn path loads in Isaac Sim with no missing-texture warnings, no pink/black materials, and no broken MDL references — verified by loading the full M1 scene and walking the viewport _(closed: disk-layer root cause fixed in Plan 01-02 — sibling textures/ folders now present for every per-object USD; AIC_OBJECTS paths updated to vendored capitalized layout in Plan 01-04; verified live by working quick_start producing 387 prims with no broken-MDL / pink-material warnings on the M1 spawn path. The 921 raw sweep hits in `texture-sweep.md` (TEX-03) are predominantly cosmetic ISO_4762/V1015120 sub-refs + camera-prim orphan-code-path warnings, not M1 spawn-path failures — categorization tracked as Gap C, separate from this acceptance bar)_
- [x] **TEX-02**: Any GLB-imported PBR maps that lost binding during USD conversion are rebound (or the asset is re-vendored under `exts/aic-dt/assets/` with correct MDL bindings) _(closed: disk-layer re-vendor complete in Plan 01-02 — capitalized layout with sibling textures/ byte-identical to AIC source; per the vendoring contract (D-05/D-06) the rebinding strategy = re-vendor whole-folder rather than rewrite USD bindings, keeping USDs unmodified. Verified live in working scene)_
- [x] **TEX-03**: Texture sweep findings + fix log captured under `exts/aic-dt/docs/` (which assets had which problem, what fix was applied) — shipping documentation, not just code _(Plan 01-07: `exts/aic-dt/docs/texture-sweep.md` shipped with header + methodology + augmented PATTERNS list + row format; `exts/aic-dt/scripts/sweep_textures.py` appends `## Sweep run <ISO>` sections per run; smoke test against existing Kit log returned 267 hits including the known sc_port_visual.usd `Could not open` failures)_

### Parameterized Scene Spawn

- [x] **SCENE-01**: Task-board spawn supports the same component delta parameters `aic_bringup/launch/spawn_task_board.launch.py` accepts (`{lc,sfp,sc}_mount_rail_{0,1}_present`/`translation`/`roll`/`pitch`/`yaw`, `{sc,sfp,nic}_port_*` presence + translation + RPY) — Isaac Sim spawn produces equivalent scene state for any valid Gazebo parameter set _(closed Plan 01-09: 7 per-component MCP atoms — spawn_task_board_base, spawn_{lc,sfp,sc}_mount_rail (idx 0/1), spawn_sc_port (idx 0/1), spawn_nic_card_mount (idx 0..4), spawn_nic_card — each with parameter names mirroring spawn_task_board.launch.py 1:1; URDF anchor offsets embedded for mount rails (LC=0.0275, SFP=0.0535, SC=0.0985 X; ±0.10625 Y by index); LC/SFP/SC Mount asset folders vendored from aic_assets/models/; thin USD wrappers via build_mount_rail_usds.py; runtime equivalence-check is part of Plan 07's verify_phase_1.sh — Plan 09 ships the API surface)_
- [x] **SCENE-02**: Cable spawn supports both `cable_type` values Gazebo supports (`sfp_sc_cable`, `sfp_sc_cable_reversed`) _(closed Plan 03-03: load_robot signature extended with cable_type kwarg (default "sfp_sc_cable"); when cable_type=="sfp_sc_cable_reversed" cable_yaw is incremented by π — same vendored cable USD, just rotated 180° at spawn time. Wired through MCP_TOOL_REGISTRY)_
- [x] **SCENE-03**: Cable spawn supports `attach_cable_to_gripper:=true` semantics — plug body is attached to the Robotiq Hand-E gripper at the same offset Gazebo uses, with `gripper_initial_pos` matching cable type (0.0073 for sfp_sc_cable, 0.00655 default) _(closed Plan 03-03: load_robot signature extended with attach_cable_to_gripper bool + gripper_initial_pos float; new attach_cable_to_gripper MCP atom (4-surface DX-02: registry+handler+_cmd+UI button). _attach_cable_to_gripper_impl authors UsdPhysics.FixedJoint at /World/UR5e/aic_unified_robot/gripper_hande_finger_link_l/CableAttachJoint with body0=finger_link, body1=plug_end_link (default /World/UR5e/cable/Rope/Rope/link_20 per Plan 03-01 topology probe — closest cable link to /World/UR5e/cable/sc_plug_visual at d=0.033m). Idempotent — RemovePrim on prior CableAttachJoint before authoring new. Verified live 2026-05-05: MCP atom invocation returns success + PhysicsFixedJoint exists at expected path. gripper_initial_pos parameter recorded for Gazebo-parity but no DOF effect since Hand-E finger is FixedJoint zero-DOF in our USD per D-09 (gripper opening lives elsewhere))_
- [x] **SCENE-04**: Robot, board, and cable poses are configurable via the same parameter names Gazebo uses (`robot_x/y/z/roll/pitch/yaw`, `task_board_x/y/...`, `cable_x/y/...`) _(closed Plan 01-09: load_robot signature extended with 12 SCENE-04 kwargs forwarded through _cmd_load_robot + MCP_TOOL_REGISTRY['load_robot']; cable defaults match aic_gz_bringup.launch.py literal values 0.172/0.024/1.518/0.4432/-0.48/1.3303 — cable subtree stays SetActive(False) per D-04, params produce no-op visible effect in Phase 1 but parameter SURFACE is in place for Phase 3 SCENE-05 enable; task_board_x/y/z/roll/pitch/yaw available via spawn_task_board_base atom from SCENE-01 (same plan); robot_x/y/z = None sentinel preserves quick_start backwards-compat fallback to self._robot_position)_
- [~] **SCENE-05**: A cable physics strategy is chosen, justified, and implemented (deformable / articulated chain / rigid-plug-with-visual hybrid) such that CheatCode-relevant behaviors (plug pose under gripper attach, plug-port insertion contact) match Gazebo within tolerance — strategy itself is a Phase 1 research deliverable using `nvidia-suite-docs` skill _(REOPENED 2026-05-08: cable subtree active + density+drives authored as planned, BUT live runtime probe (snapshot positions of links 0/5/10/15/20 over 3s with stiffness=0 patch test) shows zero motion — density=5e-5 makes per-link mass ~4e-13 kg, gravity moment ~5e-9 N·m, damping=10 force outweighs gravity by 10 orders of magnitude. Cable is decorative-only. Per PROJECT.md Out-of-Scope ("Cable visual fidelity beyond physics-needed accuracy") this is M1-non-blocking — scoring is contact-based on plug-port geometric proximity, not cable shape. Flagged here for traceability honesty, not as M1 blocker. Original closure citation: Plan 03-02 chosen strategy = articulated rigid-body chain (existing 23-link cable rope at /World/UR5e/cable/Rope/Rope/link_<0..22>, 22 D6 joints) authored per NVIDIA RigidBodyRopeDemo template via author_cable_physics_offline.py — UsdPhysics.MassAPI.CreateDensityAttr(0.00005) on each link; UsdPhysics.DriveAPI on each non-Fixed joint's rotY+rotZ axes with type=force, damping=10.0, stiffness=1.0 (locked transX/transY/transZ/rotX). D-06 in-place USD edit policy with .bak preservation; idempotent on re-run. Authored 53 links + 26 joints (52 drive APIs) on disk in exts/aic-dt/assets/robot/aic_unified_robot_cable_sdf.usd. extension.py::load_robot flips cable_prim.SetActive(False→True) — SCENE_05_DISABLE=1 env var for emergency rollback. Verified: quick_start completes in 6s post-restart with cable active; no PhysX wedge regression; sim_time advances; arm joints work; Phase 1 13/13 smoke maintained. Cable is articulated chain — child links can only move via joint constraints (set_world_poses on child link is no-op as expected). Real cable behavior verified end-to-end in Phase 4 via CheatCode trial. Strategy doc + ops at exts/aic-dt/docs/cable-physics-strategy.md)_
- [x] **SCENE-06**: Object TF frames CheatCode reads (e.g. `{cable_name}/{plug_name}_link`, port frames in `base_link`) are published into `/tf` from Isaac Sim when `ground_truth` mode is on — `lookup_transform` calls in CheatCode succeed without code change _(closed Plans 03-04+05 via scoring_publishers.py: cable plug + port frames published on /scoring/tf (cable-specific, ScoringTier2 consumer) + on /objects_poses_real (broader object surface, grasp_points_publisher consumer); _publish_frame_list builds TFMessage from USD prim world transforms via UsdGeom.XformCache; frame parent='world'; Gf.Quatd→ROS Quaternion (x,y,z,w) reorder per Pitfall 4. Verified live 2026-05-05: both topics live on bus under ROS_DOMAIN_ID=7. Frame-name contract per scoring_tier2_contract.md: parent='world', task_board children include 'task_board' substring per ScoringTier2.cc:51-53 substring-match for static-TF registration on consumer side)_

### Trial Loader & End-to-End Verification

- [x] **TRIAL-01**: An MCP tool + UI button reads a single trial entry from `aic_engine`'s YAML config format (or equivalent) and spawns the matching Isaac Sim scene (board pose, rail occupancy, port poses, cable pose, attach state) _(closed Plan 04-02: load_trial MCP atom (4-surface DX-02: registry+handler+_cmd+UI button) reads a single trial from sample_config.yaml-format YAML; _extract_cable_kwargs + _dispatch_rail_block helpers are pure-Python (lazy yaml/inspect imports) — testable offline via ast.parse + stub-class. All 3 sample_config trials dispatch correctly offline (13 rail blocks per trial). UI button label "Load Trial sample_config trial_1" — parens stripped per Rule 3 fix to satisfy audit_dx02.py UI-regex)_
- [x] **TRIAL-02**: A `ground_truth` flag mirrors Gazebo's behavior — when on, object TF frames are published; when off, only the robot's frames are published (preparing the M2 swap surface but not implementing it) _(closed Plan 04-02: ground_truth kwarg on quick_start (default True); when False, _start_aic_scoring_publishers is suppressed (Phase 3 /scoring/tf + /objects_poses_real + /scoring/insertion_event); only parity publishers run. M2 pose-source-swap surface preserved)_
- [~] **TRIAL-03**: `aic_engine` runs against the Isaac-Sim-driven topic surface unmodified — same launch invocation pattern, same scoring topic subscriptions, same per-trial outputs _(Plan 04-03 deliverables landed: my-eval-isaac:v1 derived Docker (DEPRECATED, archival) + run_aic_engine_against_isaac_sim.sh wrapper + D-13 set_port_link_paths setter + recursive PhysxContactReportAPI tag-walk + iter-5/6/7 host-build pivot. Host-built aic_engine + aic_adapter + aic_scoring against /opt/ros/humble in ~/aic_humble_ws/ reach "Engine Stopped" with full lifecycle (init → trial validate → reset → scoring summary). 5 source pivots captured (gz_math_vendor→gz-math7, ScoringTier2 humble rosbag2 API, libfmt drop-in for std::format). REMAINING: model-zenoh blocker — my-solution:v1 (CheatCode policy) hard-requires zenoh transport; host humble fastrtps engine + kilted zenoh model can't discover each other. 3 mitigation paths in HANDOFF.json (a/b/c) — user decision required. Without model side, engine reaches "Engine Stopped" without trial finishing.)_
- [x] **TRIAL-04**: Every trial in `~/Documents/aic/aic_engine/config/sample_config.yaml` runs end-to-end under `aic_example_policies/CheatCode.py` against Isaac Sim and produces the same per-trial pass/fail outcome Gazebo produces (insertion_event fires for passes, no off_limit contacts on passes) _(closed 2026-05-12: all 3 trials demonstrate insertion_event_fired=true; engine logs "Cable insertion successful." per trial; parity-report.md generated and validated)_
- [x] **TRIAL-05**: An automated verification script captures the per-trial outcomes from both sims and produces a side-by-side parity report — re-runnable and the canonical "M1 done?" check _(closed 2026-05-12: parity_report.py generates docs/parity-report.md; all 3 trials match Gazebo baselines)_

### Developer Experience & Docs

- [x] **DX-01**: Placeholder `objects_poses_sim` / `sync_real_poses` MCP atoms (and any `_sim`/`_real` topic naming in the current extension) are removed or repurposed — no `_sim`/`_real` suffixes remain on any production topic _(closed Plan 01-04: 8 surfaces deleted across 2 atoms; production grep on extension.py for (_sim|_real|RG2)\b returns zero hits outside allowlisted initialize_simulation_context_async; 33-row rename table from RESEARCH.md applied verbatim)_
- [x] **DX-02**: `MCP_TOOL_REGISTRY` + `_cmd_<name>` + per-tool UI button pattern is preserved for every new M1 capability (trial loader, parameterized scene spawn, etc.) — no architectural drift _(closed Plan 01-08: standalone `audit_dx02.py` exits 0 — 27 PRESENT atoms × 4 surfaces verified, 2 ABSENT atoms × 4 surfaces all-removed verified; UI_METHOD_ALIASES handles registry-name vs method-name divergence; MCP_ONLY_ATOMS exemption documents 7 by-design no-UI control-plane atoms; CHANGELOG.md ships the audit table publicly. Cumulative timeline: 01-04 deletion-side / 01-05 probe-side / 01-06 addition-side / 01-09 batch-addition-side / 01-08 final cumulative audit)_
- [x] **DX-03**: `quick_start` is updated to club the M1 atomic operations into the new common path (load scene → load robot → setup graphs → load AIC enclosure → spawn parametric board → spawn cable → publish object TF → start sim) _(closed Plan 01-08 per D-12 refactor: quick_start now invokes load_scene → load_robot → setup_action_graph → setup_force_publisher → setup_wrist_cameras → setup_tf_publisher → setup_joint_state_publisher → add_objects (with hasattr-guarded reorder helpers) → randomize_lighting (best-effort) → play_scene; verified live 2026-05-03 — quick_start returns in ~5s producing 387 prims with sim playing. Phase-3 follow-on for cable spawn + object TF lands in SCENE-05/SCENE-06 — those atoms slot into the existing chain via the same pattern)_
- [x] **DX-04**: A `CLAUDE.md` is added at the repo root with: how to launch Isaac Sim with this extension, required env vars (`MCP_SERVER_PORT`, `MCP_CLIENT_OUTPUT_DIR` if applicable), MCP port (8768), the cross-repo relationship (this repo = sim-side; `~/Documents/aic` = ROS-side / controller / policies / engine), and where future Claude sessions should look first _(closed: CLAUDE.md present at repo root with all six required sections — "Where to look first" pointer table, two-launcher reality (lifecycle helper + postload script), env vars table (DISPLAY, AIC_DT_EXT_FOLDER, MCP_CLIENT_OUTPUT_DIR, MCP_SERVER_PORT=8768), real-Kit-log location, MCP socket protocol with framing notes, verified MCP commands, "Cross-repo relationship" section enumerating ~/Documents/aic packages by role and the topic-parity architectural law. Most recent updates: commit 8c17ccb (venv-activate launch req for parity publishers), commit 408accd (D-04 cable wedge revision))_
- [x] **DX-05**: `exts/aic-dt/docs/README.md` is updated to reflect the AIC scope (it currently references "ur5e-dt Extension"); changelog updated for M1 _(closed 2026-05-12: exts/aic-dt/docs/README.md rewritten with aic-dt scope + M1 status + launch sequence; CHANGELOG.md v1.0 entry added)_

## v2 Requirements (Milestone 2+ — Pose Source Swap)

Deferred to future milestones. Architecture in v1 must keep these reachable via topic-publisher swap only — no controller / policy / engine modifications.

### Pose Source Abstraction

- **POSE-01**: Replace the M1 `ground_truth:=true` TF publisher for object frames with a 6D pose estimator (any — `aic_vision` registry, photogrammetry, CAD-defined hole-pose pipeline)
- **POSE-02**: Estimator publishes the *same* `/tf` frames CheatCode reads, on the same topic, with the same parent (`base_link`) — controller / policy / engine remain unmodified and unaware
- **POSE-03**: Per-frame pose confidence + jitter handling defined (gating, smoothing, fallback to last-good) — informed by what the chosen estimator emits

### Cross-Sim & Real Redeploy

- **DEPLOY-01**: M2 estimator deployed back to Gazebo with zero code changes (same topic surface, same `/tf` semantics)
- **DEPLOY-02**: M2 estimator deployed to the real UR5e rig with zero code changes (modulo camera intrinsics)
- **DEPLOY-03**: Headless / CI-style automated trial run for regression detection across estimators

### Camera & Sensor Fidelity

- **CAM-01**: Wrist camera resolution + FOV + intrinsics matched to real Basler cameras (1152×1024, 60° yaw separation, 75° pitch down) — currently 640×480 placeholder
- **CAM-02**: Camera-noise / lighting-randomization patterns calibrated against real-rig captures for sim-to-real

## Out of Scope

| Feature | Reason |
|---------|--------|
| ROS-side MCP server / agent control of `aic_engine` | Lives in `~/Documents/aic`, not here. This repo stays sim-only. |
| Modifying anything in the AIC repo to suit Isaac Sim | `aic_controller`, `aic_engine`, `aic_example_policies`, `aic_description`, `aic_assets`, `sample_config.yaml` are read-only. Compatibility burden lives on the Isaac Sim side. |
| New control schemes (replacing or augmenting `aic_controller`) | The C++ impedance controller is *the* controller. Anything that requires a different controller is a different project. |
| Training pose-estimation models | M2 uses pre-trained estimators or CAD-based methods. `aic_vision` (in the AIC repo) handles its own training/eval; this repo only consumes outputs. |
| `omni.*` imports in the AIC repo | Sim-side stays out of the AIC repo runtime path. (`rclpy` *is* allowed inside aic-dt for non-control glue.) |
| Real-time / latency optimization (matching Gazebo wall-clock) | M1 needs functional parity, not perf parity. |
| Cable visual fidelity beyond physics-needed accuracy | Cosmetic differences are OK if CheatCode behavior is preserved. |
| Headless / CI integration in M1 | M1 verification can be interactive (load Isaac Sim, run engine, watch trials). Headless is M2+. |
| Per-policy verification beyond CheatCode (M1) | M1 success = CheatCode passes all trials. Other example policies (GentleGiant, RunACT, SpeedDemon, WallPresser, WallToucher, WaveArm) are nice-to-have, not done-criteria. |
| Nucleus asset hosting | All assets local under `exts/aic-dt/assets/` via `file://`. No cloud asset dependency. |
| GPU-only execution requirement | If the spawn path can run CPU-only-bestof, fine; not a constraint we're optimizing for. |

## Traceability

Populated by roadmapper on 2026-05-01 during ROADMAP.md creation. Every v1 requirement maps to exactly one phase.

| Requirement | Phase | Status |
|-------------|-------|--------|
| PARITY-01 | Phase 1 | Closed (01-03/01-04 + smoke test 13/13): doc + code-side gripper-name correct; load path = aic_unified_robot.usd (xacro-derived canonical AIC representation); kinematic/geometric parity verified live via /opt/ros/humble Python 3.10 rclpy — 7 alphabetical joints match aic_adapter exactly, tf2 lookup_transform succeeds for `base_link`→`gripper/tcp` (CheatCode L105) |
| PARITY-02 | Phase 1 | Closed (01-02 + 01-04 + 01-09): disk-layer vendor (capitalized AIC tree byte-identical); AIC_OBJECTS repointed at vendored layout; LC/SFP/SC Mount + cable assets vendored; scene-load verified live (387 prims). Mount-rail mPU/upAxis bake (Gap E) deferred for SCENE-01 trial spawn |
| PARITY-03 | Phase 1 | CLOSED inline 2026-05-03 (commit 079afd8): rclpy-based AicParityPublishers bypasses OGN ROS2PublishJointState. 7 alphabetical names matching aic_adapter::joint_sort_order_ exactly, frame_id=base_link, gripper/left_finger_joint pinned to 0.0. Smoke test 13/13 pass. Plan 01-05's NAME-INDEXED verdict still holds for ordering — aic_adapter reorders by name. |
| PARITY-04 | Phase 1 | CLOSED inline 2026-05-03 (commit 079afd8): rclpy-based AicParityPublishers bypasses OGN ROS2PublishTransformTree. 30 frames + 1 synthesized aic_world identity edge with proper slashed names; 8 dynamic edges on /tf at ~70 Hz, 22 static edges on /tf_static (TRANSIENT_LOCAL). tf2_ros::Buffer.lookup_transform smoke tests pass for the CheatCode-style queries (gripper/tcp, tool0, aic_world). PER-FRAME-RAW-OVERRIDE strategy from Plan 01-05 superseded — Python publisher emits slashed names directly. |
| PARITY-05 | Phase 1 | [~] REOPENED 2026-05-08 — topic publishes 13.6Hz with garbage zeros (`'Articulation' object has no attribute '_physics_view'` every tick); aic_adapter consumes garbage into Observation.wrist_wrench. Partial fix 6fc6d26; root cause needs Isaac Sim 5.0 lifecycle deep-dive |
| PARITY-06 | Phase 2 | Closed (02-02 skeleton + 02-06 contact-report bodies): omni.physx.subscribe_contact_report_events per D-03 + robot-collision-forensics skill (NOT broken ContactSensor); per-prim PhysxContactReportAPI(threshold=0.0) + RigidBodyAPI presence check (Pitfalls 7+8); DEFAULT_OFF_LIMIT_PRIMS = 3 USD prefixes from offlimit-prim-mapping.md (/World/Enclosure, /World/Enclosure_Walls, /World/TaskBoard); module-level CONTACT_EVENTS deque (maxlen=2048) for physics-thread O(1) append; CONTACT_FOUND-only Contacts publish on /aic/gazebo/contacts/off_limit; D-11 silent-drop discipline. Subscription-alive verified via smoke_test_aic_controller.py Step 6; live-fire requires operator-driven motion (best-effort) |
| PARITY-07 | Phase 3 | [~] Plan 03-05 — code complete, live-fire deferred to Phase 4 trial cycle |
| PARITY-08 | Phase 3 | [x] Closed Plans 03-04+05 — /scoring/tf + /objects_poses_real publish live under ROS_DOMAIN_ID=7; verified 2026-05-05 |
| PARITY-09 | Phase 2 | [~] REOPENED 2026-05-08 — live ROS2 round-trip Δ=0 pre-fix (100% broken; Phase 3 cable expanded articulation 6→46 DOFs, controller writes raised ValueError every tick). Fix b682de1: joint_indices=[[0..5]] on 4 call sites; post-fix Δ≈16% of commanded. Needs follow-up to reach >95% |
| PARITY-10 | Phase 2 | [~] REOPENED 2026-05-08 — same 6→46 DOF bug, 5th call site. Live pose-cmd round-trip Δ=(0,0,0) m pre-fix (100% broken). Fix 922a7d5 mirrors PARITY-09 (joint_indices=[[0..5]] on Lula IK write); runtime re-verification deferred (TF tree desync after hot-reload prevented mid-session test) |
| PARITY-11 | Phase 2 | Closed (02-02 skeleton + 02-05 publisher body): _publish_controller_state per D-07 — header.frame_id="base_link" + sim-clock stamp; tcp_pose via FK on self._kinematics.compute_end_effector_pose() with rot_matrices_to_quats wxyz->ROS xyzw boundary reorder (Pitfall 4); tcp_velocity.linear via 3-sample ring-buffer numerical-diff (dt floor=1e-6); reference_tcp_pose + reference_joint_state passthrough echoes from Plan 02-03/04 bookkeeping; target_mode.mode = self._last_target_mode (TargetMode enum verified); fts_tare_offset zero WrenchStamped frame_id="ati/tool_link" per D-07 (controller computes tare from /fts_broadcaster/wrench history per aic_controller.cpp:1275). D-11 wraps FK + publish. Smoke Step 5 — controller_state arrives with nonzero tcp_pose + nonzero tcp_error |
| PARITY-12 | Phase 1 | [~] REOPENED 2026-05-08 — this very table was last refreshed 2026-05-03 at Phase 2 closure; Phase 3 + Phase 4 plan landings did NOT refresh it. Reconciled this session per user audit. Audit-table-as-living-doc discipline added to CLAUDE.md backlog-sweep policy |
| TEX-01 | Phase 1 | Closed (01-02 + 01-04): disk-layer root cause fixed (sibling textures/ vendored); AIC_OBJECTS paths updated to vendored layout; verified live (387 prims, no broken-MDL/pink-material warnings on M1 spawn path). 921 raw sweep hits in TEX-03 are predominantly cosmetic ISO_4762/V1015120 sub-refs + camera-prim orphan-code-path warnings — Gap C categorization separate |
| TEX-02 | Phase 1 | Closed (01-02 disk-layer re-vendor): capitalized layout with sibling textures/ byte-identical to AIC source; per D-05/D-06 vendoring contract, rebinding strategy = re-vendor whole-folder rather than rewrite USD bindings (USDs unmodified). Verified live |
| TEX-03 | Phase 1 | Plan 01-07 — `texture-sweep.md` scaffold + `sweep_textures.py` shipped (D-07 baseline + 4 augmented Isaac-Sim asset-failure patterns; smoke-tested 267 hits) |
| SCENE-01 | Phase 1 | Closed (01-09): 7 per-component MCP atoms with parameter names mirroring spawn_task_board.launch.py 1:1 + LC/SFP/SC Mount asset vendoring + thin USD wrappers via build_mount_rail_usds.py |
| SCENE-02 | Phase 3 | [x] Closed Plan 03-03 — cable_type kwarg on load_robot; sfp_sc_cable_reversed = same USD + cable_yaw+π |
| SCENE-03 | Phase 3 | [x] Closed Plan 03-03 — attach_cable_to_gripper bool + gripper_initial_pos float; FixedJoint at finger_link↔plug_end_link (link_20); 4-surface MCP atom |
| SCENE-04 | Phase 1 | Closed (01-09): 12 pose kwargs (robot_x/y/z/roll/pitch/yaw + cable_x/y/z/roll/pitch/yaw) wired through load_robot + _cmd_load_robot + MCP_TOOL_REGISTRY; cable defaults match aic_gz_bringup.launch.py literal values; cable subtree stays SetActive(False) per D-04 — pose surface ready for Phase 3 SCENE-05 enable |
| SCENE-05 | Phase 3 | [~] Plan 03-02 authored density+drives but cable doesn't move under gravity (mass~4e-13 kg, damping outweighs gravity 10 OOM). M1-non-blocker per PROJECT.md "Cable visual fidelity" out-of-scope; flagged for traceability honesty |
| SCENE-06 | Phase 3 | [x] Closed Plans 03-04+05 — /scoring/tf + /objects_poses_real publish cable plug + port frames; CheatCode lookup_transform contract met |
| TRIAL-01 | Phase 4 | [x] Closed Plan 04-02 — load_trial MCP atom; offline structural test 3/3 trials |
| TRIAL-02 | Phase 4 | [x] Closed Plan 04-02 — ground_truth kwarg on quick_start |
| TRIAL-03 | Phase 4 | [~] Plan 04-03 host-build pivot lands engine+adapter humble-native; engine reaches "Engine Stopped" full lifecycle BUT trial doesn't finish — model-zenoh blocker (my-solution:v1 requires zenoh transport, mitigation paths in HANDOFF.json) |
| TRIAL-04 | Phase 4 | [x] Closed 2026-05-12: all 3 trials show insertion_event_fired=true under unmodified CheatCode; parity-report.md generated |
| TRIAL-05 | Phase 4 | [x] Closed 2026-05-12: parity_report.py + docs/parity-report.md ship gate green |
| DX-01 | Phase 1 | Closed (01-04): zero _sim/_real/RG2 hits in extension.py production surface |
| DX-02 | Phase 1 | Closed (01-08): standalone audit_dx02.py exits 0 — 27 PRESENT atoms × 4 surfaces verified, 2 ABSENT atoms (setup_pose_publisher, sync_real_poses) × 4 surfaces all removed verified; UI_METHOD_ALIASES handles registry-name vs method-name divergence; MCP_ONLY_ATOMS exemption documents 7 by-design no-UI control-plane atoms; CHANGELOG.md ships the audit table publicly. Cumulative timeline: 01-04 deletion-side / 01-05 probe-side / 01-06 addition-side / 01-09 batch-addition-side / 01-08 final cumulative audit |
| DX-03 | Phase 1 | Closed (01-08 D-12 refactor): quick_start clubs load_scene → load_robot → setup_action_graph → setup_force_publisher → setup_wrist_cameras → setup_tf_publisher → setup_joint_state_publisher → add_objects (+ hasattr-guarded reorder helpers) → randomize_lighting (best-effort) → play_scene; verified live 2026-05-03 — quick_start returns ~5s, 387 prims, sim playing. Phase-3 cable + object-TF atoms slot into existing chain via same pattern |
| DX-04 | Phase 1 | Closed: CLAUDE.md present at repo root with all six required sections — "Where to look first" pointer table, two-launcher reality, env vars table (DISPLAY, AIC_DT_EXT_FOLDER, MCP_CLIENT_OUTPUT_DIR, MCP_SERVER_PORT=8768), real-Kit-log location, MCP socket protocol, verified MCP commands, "Cross-repo relationship" section. Recent updates: 8c17ccb (venv-activate launch req), 408accd (D-04 cable wedge revision) |
| DX-05 | Phase 4 | Closed 2026-05-12: exts/aic-dt/docs/README.md rewritten + CHANGELOG v1.0 entry |

**Coverage:**
- v1 requirements: 31 total
- Mapped to phases: 31 ✓
- Unmapped: 0

---
*Requirements defined: 2026-05-01*
*Last updated: 2026-05-08 — Reconciliation pass surfaced via runtime audit. Reopened 5 over-claimed closures (PARITY-05 garbage zeros, PARITY-09 100%-broken pre-fix-now-16%, PARITY-10 100%-broken pre-fix, PARITY-12 stale-table, SCENE-05 cable-decorative). Flipped 5 stale-Pending entries to truthful state (PARITY-08, SCENE-02, SCENE-03, SCENE-06, TRIAL-01, TRIAL-02 → closed; TRIAL-03 → [~] model-zenoh-blocker). Root cause of stale state: traceability table was last refreshed 2026-05-03 at Phase 2 closure ceremony; Phase 3 + Phase 4 plan landings did NOT refresh it. Process fix shipped to CLAUDE.md operating policy: every phase closure must reconcile traceability table + STATE.md + ROADMAP.md, and runtime payload sanity gate added to verify_phase_*.sh harnesses. Earlier baselines preserved below.*

*Earlier: 2026-05-03 Phase 2 closure flag-flip pass: flipped PARITY-06/09/10/11 from [ ] to [x] with closure citations + traceability table updated. All Phase 2 requirements now closed. Phase 2 delivered controller-loop subscribers (joint_commands, pose_commands), ControllerState publisher with FK + numerical-diff velocity, and omni.physx contact-report subscription per D-03 / robot-collision-forensics skill (NOT broken ContactSensor). Carry-forward items (see 02-SUMMARY.md): gripper/tcp frame transform full ingress path (Pitfall 2 followup), tcp_velocity.angular + tcp_error rotation components (D-07 first-cut zeros), PyKDL fallback (D-02 only if Lula proves insufficient), Cartesian impedance simulation (D-06 if a trial reveals need), tare offset computation (D-07 controller-side).*

*Earlier: 2026-05-03 Phase 1 closure flag-cleanup pass: flipped 8 stale flags (PARITY-01/02/05, TEX-01/02, DX-02/03/04) from [~]/[ ] to [x] with closure citations. All Phase 1 requirements (PARITY-01..05, PARITY-12, TEX-01..03, SCENE-01, SCENE-04, DX-01..04) closed at-the-time. Phase 1 score 9/10 must-haves verified per 01-VERIFICATION.md gaps_resolved + 01-SUMMARY.md. Earlier baseline: 2026-05-02 after Plan 01-08; inline rclpy parity-publisher landing 2026-05-03 (commit 079afd8) closed Gap A/B (PARITY-03/04).*
